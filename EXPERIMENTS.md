# 실험 기록 (Experiment Log)

모든 실험을 시간순으로 기록한다. 각 실험마다: 무엇을 바꿨는지 / 왜 / 결과 점수.
이 파일이 나중에 "실험 보고서"의 원본 자료가 된다. 근거 자료는 docs/research.md.

## 가설 백로그 (우선순위순 — 결과 나올 때마다 갱신)

| 가설 | 내용 | 기대 효과 | 근거 | 상태 |
|---|---|---|---|---|
| H1 | max 토큰 1024→2048 + 소수점 오추출 수정 | +2~4%p | exp01 오답 분석 | 노트북 02 반영 |
| H2 | Self-Consistency n=8 (temp 0.7 다수결) | +5~10%p | Wang et al. 2022, AIMO 우승 사례 | 노트북 02 반영 |
| H3 | RFT: 자체 생성 CoT 중 정답만 골라 QLoRA SFT | +5~15%p | RFT 논문, STaR | ❌ **3연속 실패** (exp06, exp06c, exp09b) — 정체 3/3, lr·데이터량·외부데이터 혼합으로도 회복 안 됨. QLoRA SFT 계열 보류, 근본 전환 필요 |
| H4 | 외부 CoT 데이터 혼합 (NuminaMath-CoT) | +3~8%p | NuminaMath 1위 솔루션 | ❌ 완료 (exp09b, H12로 구체화) — 외부 30k+자체 12.9k 혼합해도 베이스보다 하락 |
| H5 | ~~TIR: 모델이 쓴 파이썬을 실행해 계산 보조~~ | - | - | ❌ **금지** (운영진 Q&A: 추론 시 코드 실행·툴 호출 불허) |
| H6 | GPTQ/AWQ 양자화 + vLLM으로 SC 샘플 수 증대 | 간접 (속도) | NuminaMath T4 최적화 | 속도 병목 시 |
| H7 | GRPO 강화학습 | 불확실 | DeepSeekMath | 후순위 |
| H8 | 멀티 LoRA 어댑터 앙상블 (같은 베이스, 다수결 결합) | +2~5%p | SC의 다양성 확장 | ❌ 완료 (exp19) — base8+qlora8 74.9%(362/483), base8+grpo8 74.7%(361/483), 둘 다 균일 n16(75.6%)보다 낮고 성공 기준(76.6%+) 미달. 정책 다양성 가설 기각 |
| H9 | 검증자(verifier) 어댑터로 Best-of-N 선별 | +3~8%p | 소형모델+강한검증자 연구 | ❌ 완료 (exp18a·18b·18c, v1) — verifier_weighted 75.4%(다수결 74.9% 대비 +0.5%p)로 방향은 맞으나 성공 기준(76.5%+) 미달, best_of_1 72.5%는 오히려 하락. **재도전(exp23a·23b·23c, v2, 근거+Verdict 학습) 완료** — majority/hybrid 73.7%(356/483), verdict_vote 73.3%(354/483), 셋 다 v1(75.4%)보다도 낮음. 성공 기준(76.5%+) 미달, 재도전도 실패. **잔여 조합(exp25, v1×n=32) 완료** — verifier_weighted 75.8%(366/483, n8 대비 +0.4%p)로 표본 확대 효과는 있으나 여전히 성공 기준(76.3%+) 미달 — H9 **최종 기각**(모든 변형 소진) |
| H10 | 반복 RFT: 학습된 모델로 데이터 재생성 → 재학습 (2~3라운드) | +3~8%p | STaR의 반복 루프 | ⏭️ 스킵 (exp08) — exp06c 어댑터가 베이스보다 낮아 조건 미충족 |
| H11 | SC 샘플 수 스케일링 (8→16→32→64) + temperature 탐색 | +1~4%p | 다수결은 표본이 클수록 안정 | ❌ 완료 (exp07, exp17, exp27) — 표본 스케일링: n=8→16 +0.9%p, n=16→32→64는 75.4~75.8% 사이에서 정체(비단조), 성공 기준(76.3%+) 미달. 온도 탐색(exp27): t=0.4 74.1%, t=0.7 75.6%(exp07 기준), t=1.0 74.5%/가중 75.4% — 셋 다 t=0.7이 최고, 온도 조정으로도 추가 이득 없음. H11 **최종 종료**(표본·온도 두 축 모두 소진) |
| H12 | 외부 CoT 데이터 혼합 비율 실험 (NuminaMath-CoT 정수답 부분집합 0/30/70%) | +3~8%p | NuminaMath 우승, H4 구체화 | ❌ 완료 (exp09a·exp09b) — 외부 30,000+자체 12,923 혼합 학습(lr1e-4/ep1)해도 베이스보다 하락(greedy 67.5%, SC 71.8%). 자기증류 가설 기각, 학습 파이프라인 자체(러닝레이트/포맷/LoRA target) 재검토 필요 |
| H13 | 오답 유형 분석 → 취약 유형 표적 데이터 증강 (상용 API 활용 허용 범위) | +2~6%p | 오답 분석 기반 커리큘럼 | ✅ 사전 분석 완료 (exp16) — 30건 표본 중 접근오류 20(67%)·라벨의심 7(23%)·계산실수 3(10%)·추출실패 0. 후처리로 얻을 공짜 점수는 사실상 없음(①=0). H13 본체(표적 증강)는 트랙 A 동결 상태라 보류, 대신 라벨 재검수(H13b) 신설 검토 |
| H14 | 풀이 길이 통제: 짧은 정답 풀이 우선 학습 (긴 풀이는 3B에 역효과) | +1~4%p | Small Models Struggle 논문 | ❌ 완료 (exp06c, lr완화와 결합) — 효과 없음, 여전히 베이스보다 낮음 |
| H15 | DPO: 같은 문제의 정답 풀이(chosen) vs 오답 풀이(rejected) 쌍 학습 | +2~6%p | RFT 부산물 데이터 재활용 | SFT 정체 시 |
| H16 | 로그확률 가중 투표 (모델 출력의 확신도로 표 가중치 — 모델 출력만 사용이라 규칙 적합) | +1~3%p | Entropy-weighted SC (AIMO-3) | ✅ 완료 (exp10) — 같은 SC n=8 표본 내에서 가중 투표가 일반 다수결보다 +0.8%p 높음(74.5% vs 73.7%), 기대 범위(+1~3%p) 하단에 근접 |
| H21 | 쌍대 비교 토너먼트: 접전 문제만 골라 두 후보 풀이를 나란히 제시해 상대 비교(A/B 어느 쪽이 옳은가)로 재판정 — H9(검증자, 절대 채점) 대안 | +1.5%p 이상 | LLM 상대비교>절대평가 통념 | ❌ **최종 기각** (exp37 → exp37b 1차 무효 → exp37b 정정판) — logprobs=0 누락 버그 수정 후 정정판(margin=3, gen-n=32, votes=5)을 실제로 실행: 접전 65문항 중 대표 풀이 양쪽 확보 57문항(버그 수정 확인됨, 더 이상 0건 아님)에서 토너먼트 판정 실행 — tournament 76.0%(367/483) vs baseline 75.8%(366/483), delta **+1문항(+0.21%p)**. 성공 기준(+5문항) 크게 미달 — 이번엔 가설이 실제로 검증됐고, 상대 비교가 절대 채점(H9)보다 낫다는 통념은 이 태스크에서 확인되지 않음 |
| H22 | 분산 축소 스택: 서로 다른 시드 n=32 표본 3세트를 96표로 합쳐 집계하면 재실행 간 LB 변동(exp34b, 0.48%p)이 줄어든다 | 재실행 변동 감소 | exp34b(동일설정 재실행 변동 발견) | ❌ 완료(exp41) — 로컬 예측답 차이는 47.8% 감소(67→35문항)했으나, 독립된 두 96표 스택을 실제 LB에 제출해 비교하니 점수 차이가 오히려 더 큼(0.48%p→0.84%p). LB 골드스탠다드 기준 가설 기각. 로컬 "예측 차이 문항 수"가 정확도 분산의 대리 지표로 부적절함을 확인(n=1 비교쌍, 통계적으로 약함) |

| H23 | 강한 교사(Claude)가 **우리 문제에 대해** 쓴 풀이로 증류 — 학습 6전이 전부 자기증류(RFT) 아니면 외부 공개 데이터였다는 재해석 | +2~5%p | 학습 축에서 유일한 미탐색 변형 | 🔄 진행 중 (exp52 생성) — 대상은 분야별 진단으로 고른 경시형 1,200문제 |
| H24 | 스칼라 보상 헤드 + Bradley-Terry 쌍대 손실 — exp18c(검증자) 실패를 **목적함수 오류**로 재해석(절대 채점이 아니라 문제 내 순위가 과제) | +1.5%p 이상 | 운영진이 파라미터 추가 허용(2026-08-11), exp50 헤드룸 +4.14%p | ❌ 완료 (exp51) — holdout 쌍 정확도 **0.647**(SE 0.031), Best-of-N 실측은 아래 exp51 참조 |
| H25 | implicit PRM: 폐기한 DPO 체크포인트를 채점기로 재해석 — `r = β·log[P_policy/P_ref]`가 곧 보상이므로 **추가 학습 0** | +1.5%p 이상 | arXiv 2412.01981, deep-research 산출 | ❌ **최종 기각** (exp54) — 6개 규칙 전부 기준선 이하, 최악 −6.83%p |

**🚨 정체 3/3 발동 (2026-08-06, exp09b)** → 근본 전환 계획 수립: **docs/pivot-plan.md**. 트랙 A = GRPO 강화학습(H17, KL 제약으로 SFT 실패 원인 회피), 트랙 B = 추론 스택 재설계(H18 자기수정, H19 프롬프트 앙상블). 신규 가설:

| H17 | GRPO: KL 제약 RL — SFT와 달리 기존 조율 보존 + 다양성 유지 | +2~5%p | DeepSeekMath, pivot-plan | ❌ **최종 기각** (exp12 파일럿·exp24 스케일업) — 파일럿(3천문제/400스텝) greedy 69.2%/SC 74.5%/가중 74.9%, 베이스와 사실상 동일(±0.2%p). 스케일업(6천문제/1500스텝)에서도 greedy 69.8%/SC **73.1%**(파일럿보다 더 하락)/가중 74.1% — 성공 기준(SC≥75.7%) 재도전도 미달, "덜 돌려서"가 아니라 GRPO 자체가 SC 다양성을 깎는 방향으로 수렴하는 것으로 결론. exp13에서 트랙 A(학습 트랙) 완전 동결 확정 |
| H18 | 2단계 자기수정 (풀이→같은 모델이 검토·수정) | +1~3%p | Self-Refine 계열 | ❌ 완료 (exp15) — 69.6%(336/483), 비교기준 greedy 69.4% 대비 +0.2%p로 판정기준(±1%p) 이내 노이즈, 개선 없음 |
| H19 | 프롬프트 앙상블 SC (표본마다 다른 시스템 프롬프트로 다양성 확대) | +1~3%p | SC 다양성 이론 | ❌ 완료 (exp14) — 74.3%(359/483), 단일 프롬프트 SC n8(74.7%) 대비 -0.4%p로 판정 기준(±1%p) 이내 노이즈, 개선 없음 |
| H20 | Budget Forcing: EOS 대신 "Wait" 이어붙여 자기 재검토 강제 (표 개별 질 향상, 표 세기 아닌 유일한 축) | +2%p 이상(greedy) | s1 (arXiv:2501.19393) | ❌ **최종 기각** (exp32, exp42) — greedy round0 69.4%→round1 70.0%(+0.6%p)→round2 70.0%(수렴), 성공 기준(+2%p) 미달. SC n=8 결합(exp42)도 round0 73.9%→round1 74.5%(+0.6%p, 방향은 일치)이나 비교 기준 exp05 SC n8(74.7%) 대비 −0.2%p로 판정 기준(+1%p) 미달 — greedy·SC 결합 둘 다 실패 |

**트랙 B1 완료 (exp11, 2026-08-06)**: SC n=16 + 가중투표 복합 스택 측정 — n16 다수결 74.7%(361/483), n16 가중투표 74.3%(359/483, 다수결보다 -0.4%p 낮음). 목표(76%+) 미달, exp10(n8 가중투표 +0.8%p)과 반대 방향 결과로 가중투표 효과의 안정성에 의문 — 복합 스택 채택 보류, 제출은 SC n=8 유지 권장.

**트랙 A 파일럿 완료 (exp12, 2026-08-06)**: GRPO 3000문제/400스텝 파일럿 — greedy 69.2%/SC n8 74.5%/가중 74.9%, 베이스(69.4/74.7)와 사실상 동일(±0.2%p). 성공 기준(SC n8≥75.7%) 미달 — pivot-plan.md 철수 기준에 해당하나 SFT처럼 하락하진 않음(중립). exp13에서 트랙 A 지속 여부 최종 판정 필요.

**트랙 A 최종 판정 (exp13, 2026-08-06)**: pivot-plan.md 기준("파일럿 +1%p 미만이면 학습 트랙 완전 동결") 적용 — exp12 SC n8 -0.2%p로 기준 미달 확정. **학습 트랙(트랙 A) 완전 동결**, 남은 자원은 트랙 B(H18 자기수정, H19 프롬프트 앙상블)에 집중. H8·H9(검증자·앙상블)는 운영진 답변 대기 지속.

**트랙 B2 완료 (exp14, 2026-08-06)**: 프롬프트 앙상블 SC(H19) — 서로 다른 시스템 프롬프트 4종 × 2샘플(8표) 다수결 74.3%(359/483), 단일 프롬프트 SC n8(74.7%) 대비 -0.4%p. 판정 기준(±1%p 이내는 노이즈)에 따라 유의미한 개선 없음 — H19 기각.

**트랙 B3 완료 (exp15, 2026-08-06)**: 2단계 자기수정(H18) — 1차 greedy 68.9%(333/483) → 2차 검토·수정 69.6%(336/483, 답 변경 37건). 비교 기준 exp05 greedy 69.4% 대비 +0.2%p로 판정 기준(±1%p) 이내 노이즈 — H18 기각. 이로써 pivot-plan.md 트랙 B의 두 후보(B2 프롬프트 앙상블, B3 자기수정) 모두 채택 기준(+0.5%p)에 미달, B1(exp11, 복합 스택)도 목표 미달이었던 것과 같은 패턴 — 트랙 B(추론 파이프라인 재설계) 전체가 유의미한 추가 이득을 내지 못함.

> **정체 카운트 3/3 도달 (exp09b, 2026-08-06)**: QLoRA SFT 계열(exp06 lr2e-4, exp06c lr5e-5, exp09b 외부데이터혼합 lr1e-4) 3연속 모두 베이스보다 하락. lr·데이터량·풀이길이·외부데이터 혼합 등 표면적 조정으로는 회복 안 됨 — 근본 전환 필요 시점. 이 판단(방향 선택·문서화)은 로컬 Claude(계획자) 담당, 서버 Claude(실행자)는 큐 등록 대기 중.

> 2026-08-03 공지 반영: 학습·검증에서 train 오류 627문항 제외, 리더보드는 filtered(831문항) 기준. 이전 리더보드 점수(exp01 0.648)와 새 기준 점수는 직접 비교 불가.
> 정형 기록은 `experiments/log.csv` (모든 실험·시행착오 필수 기재), 시각화는 `report.html`.

| # | 날짜 | 방법 | 미니 평가 (train 50) | 리더보드 | 노트북 |
|---|------|------|---------------------|----------|--------|
| 1 | 2026-07-31 | 베이스라인: Qwen2.5-3B-Instruct 그대로, greedy, max 1024 토큰, boxed 추출 | 66.0% (33/50) | **0.648** | kaggle/01_baseline_inference.ipynb |
| 4 | 2026-08-05 | RFT 데이터 생성 (AWS, `remote/generate_rft.py`) — 대상 16,500문제(검증 500·오류 문항 사후필터 전), 문제당 6개 샘플(temp 0.8), 정답 도달 문제 13,190개(79.9%), 채택 풀이 36,733개 → data/sft.jsonl | - | - | remote/generate_rft.py |
| 4b | 2026-08-05 | exp04 sft.jsonl 사후 필터링 — 오류 문항 627개 기준 라인 제거: 36,733줄 → 36,144줄 (589줄 제거) | - | - | - |
| 5 | 2026-08-05 | 베이스 모델 AWS 평가 (`remote/eval_vllm.py --mode both`) — 검증 483문항(500 중 오류 문항 제외), greedy 69.4%, SC n=8 74.7% | - | - | remote/eval_vllm.py |
| 6 | 2026-08-05 | **QLoRA SFT 1차 (H3) — 실패** — r16/lr2e-4/ep2, RFT 36,144 풀이 학습. greedy 68.3%(-1.1%p), SC n8 73.5%(-1.2%p). [wandb](https://wandb.ai/loonaticvibe2-11-jin-jason/huggingface/runs/o5zlvcyp) | - | - | remote/train_qlora.py |
| 6b | 2026-08-06 | 리더보드 제출 파일 생성 — exp06 어댑터가 베이스보다 나빠 **어댑터 없이** `remote/make_submission.py --n 8 --tag base` 실행 (SC n=8, 831문항) → results/submission_base.csv | 74.7% | **0.77015** ✅ | remote/make_submission.py |
| 7 | 2026-08-06 | SC 스케일 (H11, 베이스) — n=4: 72.7%, n=8: 74.7%, **n=16: 75.6%** (+0.9%p vs n8). 표본 2배당 ~+1%p의 완만한 수익 곡선 | 75.6% (n16) | - | remote/eval_vllm.py |
| 7 | 2026-08-06 | SC 샘플 수 스케일링 (H11) — 베이스 모델, `remote/eval_vllm.py --mode sc --n 16`/`--n 4` (검증 483문항) | n=4 72.7%(351) / n=8 74.7%(361,exp05) / n=16 75.6%(365) | - | remote/eval_vllm.py |
| 6c | 2026-08-06 | **QLoRA SFT 재시도 (H3+H14) — 재실패** — r16/lr5e-5/ep1(완만), 문제당 최단 풀이 1개. greedy 67.9%(-1.5%p), SC n8 73.3%(-1.4%p) | 67.9%(greedy)/73.3%(SC) | - | remote/train_qlora.py |
| 8 | 2026-08-06 | 반복 RFT (H10) — exp06c 어댑터가 베이스보다 낮아 조건 불충족, **스킵** | - | - | - |
| 10 | 2026-08-06 | 확신도 가중 투표 (H16, 베이스) — `remote/eval_vllm.py --mode sc --n 8`, 같은 표본에서 일반 다수결 73.7%(356/483) vs 가중 투표 **74.5%**(360/483) | 74.5%(가중) | - | remote/eval_vllm.py |
| 9a | 2026-08-06 | NuminaMath-CoT 외부 데이터 준비 (H12) — `remote/prep_numina.py --take 30000`, 정수답·boxed·길이(50~2500자) 필터 + 검증셋 문제 텍스트 제외 → 외부 30,000개 채택, 자체(`data/sft_short.jsonl`) 12,923개와 병합·셔플 → `data/sft_mix.jsonl` 42,923개 | - | - | remote/prep_numina.py |
| 9b | 2026-08-06 | **QLoRA SFT 외부 혼합 (H12) — 3연속 실패, 정체 3/3** — r16/lr1e-4/ep1, data/sft_mix.jsonl(42,923개=외부30k+자체12.9k). greedy 67.5%(-1.9%p), SC n8 71.8%(-2.9%p), 가중투표 72.0%(-2.7%p) | 67.5%(greedy)/71.8%(SC)/72.0%(가중) | - | remote/train_qlora.py |
| 11 | 2026-08-06 | 트랙 B1: 최적 추론 스택 확정 (베이스) — `remote/eval_vllm.py --mode sc --n 16`. n16 다수결 74.7%(361/483), n16 가중투표 74.3%(359/483) — 가중투표가 오히려 다수결보다 낮음(-0.4%p), 목표 76%+ 미달 | 74.7%(다수결)/74.3%(가중) | - | remote/eval_vllm.py |
| 12 | 2026-08-06 | **트랙 A: GRPO 파일럿 (H17) — 목표 미달, 학습 트랙 동결 후보** — `remote/train_grpo.py` (3000문제, 400스텝, lr5e-6, num_generations=4), train_loss 0.02, [wandb ctiym4ny](https://wandb.ai/loonaticvibe2-11-jin-jason/huggingface/runs/ctiym4ny). 평가: greedy 69.2%(-0.2%p), SC n8 74.5%(-0.2%p), 가중투표 74.9%(+0.2%p) — 베이스(69.4/74.7)와 사실상 동일(잡음 범위), 성공 기준(SC n8≥75.7%) 미달 | 69.2%(greedy)/74.5%(SC)/74.9%(가중) | - | remote/train_grpo.py |
| 13 | 2026-08-06 | **트랙 A 지속/철수 최종 판정** — pivot-plan.md 기준(파일럿 +1%p 미만 시 동결) 적용: exp12 SC n8 74.5%(-0.2%p) < 75.7% → **트랙 A(학습 트랙) 완전 동결**. 향후 트랙 B(H18 자기수정·H19 프롬프트 앙상블)에 집중, H8·H9는 운영진 답변 대기 | - | - | - |
| 14 | 2026-08-06 | 트랙 B2: 프롬프트 앙상블 SC (H19, 베이스) — `remote/eval_prompt_ensemble.py` (프롬프트 4종 × 2샘플=8표 다수결). 74.3%(359/483), 단일 프롬프트 SC n8(74.7%) 대비 -0.4%p로 노이즈 범위(±1%p) 내, 개선 없음 | 74.3% | - | remote/eval_prompt_ensemble.py |
| 15 | 2026-08-06 | 트랙 B3: 2단계 자기수정 (H18, 베이스) — `remote/eval_two_pass.py` (1차 greedy → 2차 검토·수정). 1차 68.9%(333/483), 2차 69.6%(336/483, 답 변경 37건) — 비교기준 greedy 69.4% 대비 +0.2%p로 노이즈 범위(±1%p) 내, 개선 없음 | 69.6%(2차) | - | remote/eval_two_pass.py |
| 16 | 2026-08-06 | 오답 정밀 분석 (H13 사전 단계, 베이스) — `remote/eval_error_analysis.py` (SC n=8 재실행, 오답 상세 기록). 73.7%(356/483), 오답 127건. 30건 표본 유형 분류: 접근오류 20·라벨의심 7·계산실수 3·추출실패 0 | 73.7% | - | remote/eval_error_analysis.py |
| 17 | 2026-08-06 | **SC 표본 수 확대 n=32/64 (H11 마무리, 베이스) — 목표 미달** — `remote/eval_vllm.py --mode sc --n 32`/`--n 64`. n32 다수결 75.4%(364/483)/가중 75.8%(366/483), n64 다수결 75.6%(365/483)/가중 75.4%(364/483). 성공 기준(76.3%+) 미달, n16(75.6%) 대비 추가 이득 없음 — SC 스케일링 정체 확인 | 75.8%(n32 가중, 최고치) | - | remote/eval_vllm.py |
| 17b | 2026-08-06 | 최적 n 제출 파일 생성 — exp17이 성공 기준(76.3%+) 미달이라 **skip** | - | - | - |
| 18a | 2026-08-06 | 검증자 학습 데이터 생성 (H9) — `remote/gen_verifier_data.py`, 6000문제×4샘플, 답 일치 자동 라벨링. 양성 16,240 / 음성 7,760 (총 24,000개) → data/verifier.jsonl | - | - | remote/gen_verifier_data.py |
| 18b | 2026-08-07 | 검증자 어댑터 학습 (H9) — `remote/train_verifier.py`, r16/lr1e-4/ep1, 클래스 균형 1:1(양성 7,760+음성 7,760=15,520), train_loss 0.2288(0.76→0.21로 수렴). [wandb ufarew6n](https://wandb.ai/loonaticvibe2-11-jin-jason/huggingface/runs/ufarew6n) → outputs/verifier/verifier_final | - | - | remote/train_verifier.py |
| 18c | 2026-08-07 | **검증자 Best-of-N 평가 (H9) — 목표 미달** — `remote/eval_bestofn.py --verifier outputs/verifier/verifier_final --n 8` (베이스로 생성 n=8, 검증자로 채점). majority 74.9%(362/483), verifier_weighted **75.4%**(364/483), best_of_1 72.5%(350/483). 성공 기준(verifier_weighted≥76.5%) 미달 | 75.4%(verifier_weighted) | - | remote/eval_bestofn.py |
| 18d | 2026-08-07 | 검증자 선별 제출 파일 생성 — exp18c가 성공 기준(76.5%+) 미달이라 **skip** | - | - | - |
| 19 | 2026-08-07 | **정책 앙상블 (H8) — 목표 미달** — `remote/eval_policy_ensemble.py`, 베이스 n8+QLoRA(exp06) n8=16표, 베이스 n8+GRPO(exp12) n8=16표 합동 다수결. base+qlora 74.9%(362/483), base+grpo 74.7%(361/483) — 균일 n16(75.6%)보다 오히려 낮음, 성공 기준(76.6%+) 미달 | 74.9%(base+qlora) | - | remote/eval_policy_ensemble.py |
| 20 | 2026-08-07 | **4-shot 프롬프트 (총력전 카드②) — 목표 미달, zero-shot보다 하락** — `remote/eval_fewshot.py`, data/sft_short.jsonl에서 기하/대수/정수론/응용 각 1개(train-005807/005399/002653/013800) 예시로 모든 문제 앞에 붙임. greedy 70.2%(339/483, +0.8%p), SC n8 **73.1%**(353/483, **-1.6%p**) — 성공 기준(SC≥75.7%) 미달, zero-shot SC(74.7%)보다도 하락 | 73.1%(SC n8) | - | remote/eval_fewshot.py |
| 21 | 2026-08-07 | **문제 텍스트 정화 (총력전 카드③) — 목표 미달(노이즈 범위)** — `remote/clean_question.py`(번역지시문·이미지링크·Asymptote코드·문두번호 제거) + `remote/eval_question_clean.py`. 검증 483문항 중 정화로 영향받은 문항 25개(5.2%). 전체 greedy: 원문 70.0%(338/483) / 정화 69.4%(335/483, -0.6%p). 영향받은 25문항만: 원문 52.0%(13/25) / 정화 56.0%(14/25, +4.0%p=1문항) | 69.4%(정화, 전체) | - | remote/clean_question.py, remote/eval_question_clean.py |
| 22 | 2026-08-07 | **적응형 예산 (총력전 카드④) — 목표 미달** — `remote/eval_adaptive_budget.py`, n=8 다수결에서 합의 실패(최다득표≤3표) 문제 90개(18.6%)만 추가 n=24로 재표결(합계 32표), 나머지는 n8 유지. n8 baseline 74.5%(360/483), adaptive **75.2%**(363/483), 문제당 평균 투표수 12.47(균일n16보다 22% 적은 예산). 성공 기준(76.3%+) 미달, 균일 n16(75.6%)보다도 낮음 | 75.2%(adaptive) | - | remote/eval_adaptive_budget.py |
| 23a | 2026-08-07 | 검증자 v2 데이터 생성 (H9 재도전) — `remote/gen_verifier_v2_data.py`, exp18a verifier.jsonl에서 어려운음성 3,239(다수결을 속인 오답)+어려운양성 506(표결에서 밀린 정답) 전량 + 쉬운양성 4,000/쉬운음성 2,000 다운샘플=9,745쌍 선별, 베이스 모델이 재검산 근거+Verdict 생성(temp0.7, tries=2), 판정이 라벨과 일치하는 근거만 채택. 채택 6,425(65.9%)/근거실패 3,320(34.1%) → data/verifier_v2.jsonl | - | - | remote/gen_verifier_v2_data.py |
| 23b | 2026-08-07 | 검증자 v2 어댑터 학습 (H9 재도전) — `remote/train_verifier.py`(data=verifier_v2.jsonl, assistant=근거+Verdict 전문), r16/lr1e-4/ep1, 클래스 균형 1:1(양성 2,198+음성 2,198=4,396, 음성이 제한 클래스), train_loss 0.7129→0.2266(평균 0.2523, 275스텝). [wandb yacah786](https://wandb.ai/loonaticvibe2-11-jin-jason/huggingface/runs/yacah786) → outputs/verifier_v2/verifier_final |
| 23c | 2026-08-07 | **검증자 v2 Best-of-N 평가 (H9 재도전) — 목표 미달, H9 최종 기각** — `remote/eval_bestofn_v2.py --verifier outputs/verifier_v2/verifier_final --n 8` (베이스로 생성 n=8, v2 어댑터가 근거+Verdict로 채점). majority 73.7%(356/483), verdict_vote 73.3%(354/483), hybrid 73.7%(356/483). 성공 기준(76.5%+) 미달, 셋 다 v1(exp18c, verifier_weighted 75.4%)보다도 낮음 | 73.7%(majority/hybrid) | - | remote/eval_bestofn_v2.py |
| 23d | 2026-08-07 | 검증자 v2 선별 제출 파일 생성 — exp23c가 성공 기준(76.5%+) 미달이라 **skip** | - | - | - | - | - | remote/train_verifier.py |
| 24 | 2026-08-07 | **GRPO 스케일업 (최종 대형 스윙 카드②) — 목표 미달** — `remote/train_grpo.py`(n_problems=6000, max_steps=1500, lr5e-6), train_loss 0.0177, [wandb c637q5at](https://wandb.ai/loonaticvibe2-11-jin-jason/huggingface/runs/c637q5at). 평가: greedy 69.8%(337/483, +0.4%p), SC n8 **73.1%**(353/483, **-1.6%p**), 가중투표 74.1%(358/483, -0.6%p) — 성공 기준(SC n8≥75.7%) 미달, exp12 파일럿(74.5%)보다도 낮아 스텝 확대가 오히려 역효과 | 73.1%(SC n8) | - | remote/train_grpo.py |
| 25 | 2026-08-07 | **v1 검증자 × n=32 잔여 조합 — 목표 미달, 큐 소진** — `remote/eval_bestofn.py --verifier outputs/verifier/verifier_final --n 32`. majority 75.4%(364/483), verifier_weighted **75.8%**(366/483), best_of_1 72.3%(349/483). exp18c(n8: 74.9/75.4/72.5) 대비 verifier_weighted +0.4%p로 표본 확대는 소폭 도움됐으나 성공 기준(76.3%+) 미달 | 75.8%(verifier_weighted) | - | remote/eval_bestofn.py |
| 26 | 2026-08-07 | 최종 제출 파일 생성 — 확정 최고 로컬 스택(확신도 가중 SC n=32, 75.8%)으로 리더보드 831문항 제출 파일 생성. `uv run python remote/make_submission.py --n 32 --weighted --tag n32w` (베이스 모델, 어댑터 없음) → results/submission_n32w.csv (831행, 정수만, 결측·중복 없음 확인) | 75.8%(로컬 기준, exp25) | 0.78459 ✅ | remote/make_submission.py |
| 27 | 2026-08-09 | **온도 탐색 (H11 온도 축, 베이스) — 목표 미달** — `remote/eval_vllm.py --mode sc --n 16 --temp 0.4`, 이어서 `--temp 1.0`. t=0.4: 다수결/가중 74.1%(358/483), 잘림 2.2%. t=1.0: 다수결 74.5%(360/483)/가중 **75.4%**(364/483), 잘림 1.5%. 기준 t=0.7 n16(75.6%, exp07) 대비 둘 다 낮음(t=0.4 −1.5%p, t=1.0 가중 −0.2%p) — 온도 조정으로 이득 없음, 잘림 비율도 둘 다 5% 미만이라 max_tokens 상향 불필요 | 75.4%(t1.0 가중, 최고치) | - | remote/eval_vllm.py |
| 28 | 2026-08-09 | **온도 혼합 앙상블 (마지막 미검증 차원) — 목표 미달** — `remote/eval_temp_mixture.py --spec 0.4:8,0.7:8,1.0:8` (베이스, 세 온도 각 8표=24표 합동). 단독: t=0.4 74.1%, t=0.7 73.3%, t=1.0 72.9%(모두 n=8). 혼합 24표 다수결/가중 **74.9%**(362/483, 둘이 동률). 비교 기준(단일온도 n=32 가중 75.8%, exp25) 대비 −0.9%p, 성공 기준(76.3%+) 미달 | 74.9%(혼합, 다수결=가중) | - | remote/eval_temp_mixture.py |
| 29-1 | 2026-08-10 | **LB 판별 후보① n=64 가중 — 기존(n32w) 대비 하락** — `remote/make_submission.py --n 64 --weighted --tag n64w` (베이스, temp0.7). 831행/정수/결측·중복 없음 확인, n32w 대비 69문항 다름(구조적으로 다른 후보 기준 충족) | - | 0.77978 | remote/make_submission.py |
| 29-2 | 2026-08-10 | **LB 판별 후보② n=32 단순 다수결(가중 없음) — 기존(n32w) 대비 하락** — `remote/make_submission.py --n 32 --tag n32m` (베이스, temp0.7). 831행/정수/결측·중복 없음 확인, n32w 대비 67문항 다름 | - | 0.77737 | remote/make_submission.py |
| 33 | 2026-08-10 | 리더보드 표본 대량 덤프 (하루 최대 제출 체제의 기반) — `remote/dump_lb_samples.py`, 베이스 모델, 831문항×96샘플(temp0.7/top_p0.8), results/lb_samples.jsonl(5.5MB, 서버 보관·커밋 안 함) | - | - | remote/dump_lb_samples.py |
| 34 | 2026-08-10 | 집계 전략 5종 제출 후보 생성 (덤프 기반, GPU 불필요) — `remote/make_submission_from_dump.py --rule <규칙> --n <n> --tag <태그>` 5회. w48(가중,n48) n32w 대비 63문항 / tr32(trim25,n32) 76문항 / w32b(가중,n32,대조군) 67문항 / dt32(drop_trunc,n32) 70문항 / tb32(tiebreak_conf,n32) 67문항 다름 — 전부 판별 기준(15문항+) 충족, 제출은 로컬 Claude 판단 대기 | - | - | remote/make_submission_from_dump.py |
| 32 | 2026-08-10 | **Budget Forcing (s1 방식, H20) — 목표 미달** — `remote/eval_budget_forcing.py --rounds 2 --n 1` (베이스, greedy, "Wait" 재검토 이어붙이기). round0(기준) 69.4%(335/483) → round1 70.0%(338/483, +0.6%p) → round2 70.0%(338/483, round1과 동일=수렴). 성공 기준(+2%p 이상) 미달로 SC 결합(②) 미실행 | 70.0%(round1=round2) | - | remote/eval_budget_forcing.py |
| 30 | 2026-08-10 | **최종 test 실전 리허설 (운영 리스크 점검, 성능 실험 아님)** — `remote/make_submission.py --n 32 --weighted --tag rehearsal` 831문항 완주 시간·자원 실측. 4.44초/문항, 2,000문항 환산 **약 2시간 28분**, GPU 최대 21,891MiB(95%), CPU RSS 최대 5.07GiB. **핵심 발견: make_submission.py는 중단 시 재개 불가(체크포인트 없음)** — 8/31 당일은 dump_lb_samples.py(재개 가능)+make_submission_from_dump.py 조합 권장 | - | - | results/rehearsal_report.md |
| 31a | 2026-08-10 | 검증셋 표본 1회 덤프 — `remote/dump_samples.py --n 64`, 베이스 모델, 483문항×64샘플(temp0.7/top_p0.8) → results/val_samples.jsonl(커밋). 이전 세션이 백그라운드로 완주해둔 결과(dump_samples.log 정상 종료, GPU 유휴 확인)를 검증 후 기록 대행(재실행 없음) | - | - | remote/dump_samples.py |
| 31b | 2026-08-10 | **집계 전략 12종 스윕 (GPU 불필요) — n=32에서 trim_lowconf25%가 기준 대비 +5문제(76.4%)** — `remote/sweep_aggregation.py`를 n={64,8,16,32}로 실행. n=32: trim_lowconf25%/trim+weighted **76.4%(369/483, +5)** (성공 기준 충족) — 나머지 n(8/16/64)은 최대 +1문제. pass@n 상한(집계로 회수 가능한 최대치): n8 82.0%/n16 84.9%/n32 87.8%/n64 89.9% | **76.4%(n32, trim_lowconf25%)** | - | remote/sweep_aggregation.py |
| 31c | 2026-08-10 | exp31b가 성공 기준(+5문제) 충족 → 리더보드 제출 파일 생성. trim25 전략은 이미 `remote/make_submission_from_dump.py`에 구현·커밋돼 있어 별도 이식 불필요 — exp33 덤프(831문항×96샘플)로 `--rule trim25 --n 32 --tag agg` 실행(GPU 불필요, 수 초). 831행/정수/결측·중복 없음 확인, n32w 대비 76문항 다름 | - | 제출 보류(한도 소진) | remote/make_submission_from_dump.py |
| 35 | 2026-08-10 | Budget Forcing 스케일업 (H20 후속) — 큐 명세대로 exp32 결과(greedy +0.6%p)가 분기 조건인 성공 기준(+2%p 이상)에 미달이라 **skip**, 실행 없이 다음 항목으로 이동 | - | - | - |
| 36 | 2026-08-10 | **샘플링 파라미터 top_p 탐색 — 목표 미달, top_p=0.8 유지** — `remote/eval_vllm.py --mode sc --n 16 --top-p 0.95/1.0` (베이스, temp0.7 고정). top_p=0.95 다수결/가중 74.3%(359/483, -1.3%p), top_p=1.0 다수결 74.7%(361/483,-0.9%p)/가중 74.5%(360/483,-1.1%p) — 기준(top_p=0.8, 75.6%) 대비 둘 다 하락, 개선 없음 | 74.7%(top_p=1.0, 최고치) | - | remote/eval_vllm.py |
| 44 | 2026-08-10 | **trim_lowconf 정밀 탐색 (GPU 불필요) — 성공 기준(+8문제) 미달, n=32 trim25%대(+5, 76.4%)가 여전히 상한** — `remote/sweep_aggregation.py`를 확장(①비율스윕 10/20/25/30/40/50% ②trim+가중결합 ③trunc제외+trim조합 ④길이기반trim ⑤trim+최소득표 폴백)해 n={16,32,64}에서 27개 변형 비교. n=32 최고 76.4%(369/483,+5, exp31b와 동일 상한 재확인), n=16 최고 76.0%(367,+3), n=64 최고 76.0%(367,+2) — 어떤 변형도 성공 기준(+8) 미충족 | 76.4%(n32, exp31b와 동률 — 갱신 없음) | - | remote/sweep_aggregation.py |
| 37 | 2026-08-10 | **쌍대 비교 토너먼트 (H21, 검증자 대안) — 성공 기준(+1.5%p) 미달, 토너먼트 실질 미실행(0/65)** — `remote/eval_pairwise_tournament.py --margin 3 --gen-n 8 --votes 5` (베이스 모델). val_samples.jsonl(n=32) 기준 접전(1~2위 표차≤3) 65문항 중 재생성 8샘플로 양쪽 후보 답을 모두 확보한 문항 **0건** — 전부 기준(가중 다수결)값 유지, tournament 75.8%(366/483)=baseline과 완전 동일(delta +0) | 75.8%(=baseline, 변화 없음) | - | remote/eval_pairwise_tournament.py |
| 41-A | 2026-08-11 | 분산 축소 스택① — 기존 덤프(seed42) n32 + seed43 n32 + seed44 n32 결합 96표 가중 SC (`remote/merge_lb_dumps.py` 신규) — s3x32 | - | **0.77617** | remote/merge_lb_dumps.py |
| 41-B | 2026-08-11 | 분산 축소 스택② 대조군 — seed45+46+47 각 n32 결합 96표 가중 SC (①과 완전 독립) — s3x32v | - | **0.78459**(=n32w와 동일 정답수) | remote/merge_lb_dumps.py |
| 38 | 2026-08-11 | 제출 다양성 유지 — exp34에서 생성해둔 미제출 후보 4종(agg/w48/dt32/tb32) 중 기존 제출과 15문항 이상 다른 것만 제출. agg는 tr32(이미 제출)와 완전 동일(diff=0), tb32는 w32b(이미 제출)와 5문항만 달라 판별 불가 → 둘 다 제출 생략. w48(weighted n48)·dt32(drop_trunc n32)만 제출 | - | **w48 0.78219** / **dt32 0.77978** | remote/make_submission_from_dump.py |
| 39 | 2026-08-11 | **최종 파이프라인 동결 문서화 (실행 없음, 문서 작업)** — LB 점수가 확인된 제출 10건(exp01/06b/26/29-1/29-2/34b/41a/41b/38-w48/38-dt32)을 표로 정리해 `docs/final-pipeline.md`에 반영. 최고점(exp26/41b, 0.78459)이 아니라 **exp34b가 밝힌 동일설정 재실행 변동(0.48%p)을 감안해, 4회 관측(0.78459/0.77978×2/0.78219)으로 가장 안정적으로 수렴한 "SC n=32 가중, temp0.7/top_p0.8, seed=42"를 최종 스택으로 재확인**. 명령어에 `--seed 42` 명시, 2,000문항 환산 시간(약 2h30m)·중단 재개 절차·8/31 당일 체크리스트 보강 | - | - | docs/final-pipeline.md |
| 42 | 2026-08-11 | **Budget Forcing + SC n=8 결합 (H20 후속) — 목표 미달, H20 최종 기각** — `remote/eval_budget_forcing.py --rounds 1 --n 8` (베이스, temp0.7). round0(이 스크립트 기준) 73.9%(357/483) → round1(WAIT 재검토+SC) 74.5%(360/483, +0.6%p, exp32 greedy와 방향 일치). 비교 기준 exp05 SC n8(74.7%, 361/483) 대비로는 −0.2%p로 판정 기준(+1%p 이상) 미달 — SC와 결합해도 유의미한 개선 없음 | 74.5%(round1) | - | remote/eval_budget_forcing.py |
| 43 | 2026-08-11 | **발표 자산화 (문서 작업 — 실행 없음)** — `docs/presentation.md`를 exp29~exp42까지 전면 반영해 갱신. 슬라이드6.5를 "리더보드 노이즈 측정"으로 재작성(exp34b·exp38·exp41 통합 서술), 슬라이드9.5를 9개 축(SC스케일/온도/가중/프롬프트/자기수정/BudgetForcing/검증자/SFT/GRPO) 표로 재구성해 각 행에 성공기준·기각근거·결론 명시. `report.html` ALL_RUNS는 사전에 이미 exp32(70.0%) 포함 최신 상태였음을 확인(추가 변경 없음) | - | - | docs/presentation.md |
| 46 | 2026-08-11 | **vLLM 스택 스모크 테스트 — 통과, DPO(H15) 준비를 위해 추가한 패키지가 생성 경로를 깨지 않음 확인** — `remote/eval_vllm.py --mode greedy --val-n 60 --tag smoke` (베이스, 오류 필터 후 58문항). greedy 69.0%(40/58), 잘림 4/58(6.9%) — 판정 기준(65~75% 대역, 60문항이라 오차 큼) 통과. GPU 종료 후 유휴 확인, 에러 없음 | 69.0%(58문항, 참고용) | - | results/eval_smoke.json |
| 37b | 2026-08-11 | **쌍대 비교 토너먼트 정정판 (H21, logprobs=0 수정 후) — 성공 기준(+5문항) 미달, H21 최종 기각** — `remote/eval_pairwise_tournament.py --margin 3 --gen-n 32 --votes 5` (베이스). 접전 65문항 중 대표 풀이 양쪽 확보 **57문항**(수정 전 0건 → 버그 수정 확인), 확보 실패 8문항은 기준값 유지. tournament 76.0%(367/483) vs baseline(가중 SC n=32) 75.8%(366/483) — delta **+1문항(+0.21%p)**, 접전 부분집합만 보면 10/57→11/57 | 76.0%(367/483) | - | remote/eval_pairwise_tournament.py |
| 47 | 2026-08-11 | **DPO (H15) — 성공 기준(+1.5%p) 미달. 단, 학습 6전 중 유일하게 하락하지 않은 실행** — `remote/prep_dpo_data.py`(verifier.jsonl 24k 재활용 → 선호쌍 1,832개, 정답·오답 혼재 문제 1,464개) + `remote/train_dpo.py`(LoRA r=8 / beta=0.3 / lr=5e-6 / 115스텝). train_loss 0.693→0.67(거의 미변화), rewards/accuracies **0.60**. 평가 실행: greedy 70.6%(+1.2%p) / SC n32 다수결 74.9% / 가중 75.2%. **그러나 동일 시드(42)로 표본을 다시 덤프해 재측정하니 가중 76.6%(370/483, +0.8%p)** — 같은 어댑터·같은 설정의 두 측정이 1.4%p 벌어져 **효과가 노이즈에 묻힘**. exp48로 시드 대응 비교 진행 | 75.2% / 76.6% (측정 2회) | - | remote/train_dpo.py, remote/prep_dpo_data.py |
| 47-B | 2026-08-11 | **표본 다양성 직접 측정 — "학습이 SC를 죽인다"는 통설 반박** — `remote/dump_samples.py --adapter` 신설 + `remote/compare_diversity.py`로 베이스 vs DPO(동일 시드 42, n=32) 비교. pass@32 87.8%→**88.4%**(+0.62%p), 문제당 고유 답 5.06→**5.12**, 1위 득표율 77.8%→**77.5%**(오히려 평평해짐). **다양성 붕괴 없음** — exp09b 이후 유지해온 진단은 최소한 DPO에는 적용되지 않음 | - | - | remote/compare_diversity.py |

| 48 | 2026-08-11 | **DPO 효과 판정 — 시드 대응 비교로 노이즈 확정, H15 최종 기각** — 베이스·DPO를 **같은 시드(42/43/44)에서 짝지어** n=32 덤프하고 동일 가중 투표 규칙으로 채점. 시드42 +4문항, 시드43 +1문항, **시드44 −7문항** → 부호가 섞임, 평균 −0.67문항(−0.14%p). **부산물: 로컬 시드 노이즈 실측** — 베이스만으로도 364~369(75.4~76.4%, 1.0%p 폭) | 베이스 366/364/369 vs DPO 370/365/362 | - | run_paired_seeds.sh |

## 실험 48: DPO 효과 판정 — 시드 대응 비교, H15 최종 기각 (2026-08-11, AWS)

- **배경**: exp47에서 같은 어댑터·같은 설정의 SC n=32 가중 값이 평가 실행 75.2% / 덤프 재측정 76.6%로 **1.4%p** 어긋났다. 단일 측정으로는 −0.6%p인지 +0.8%p인지 판별할 수 없어, 이 프로젝트가 exp34b에서 세운 원칙("단일 관측으로 판정하지 않는다")을 그대로 적용
- **설계**: 표본 변동을 상쇄하려면 **같은 시드에서 베이스와 DPO를 짝지어** 뽑아야 한다. 시드 42/43/44 각각에 대해 베이스·DPO n=32 덤프(총 6회) 후 동일한 확신도 가중 투표로 채점
- **결과**:

  | 시드 | 베이스 | DPO | 차이 |
  |---|---|---|---|
  | 42 | 366/483 (75.8%) | 370/483 (76.6%) | **+4문항** (+0.83%p) |
  | 43 | 364/483 (75.4%) | 365/483 (75.6%) | **+1문항** (+0.21%p) |
  | 44 | 369/483 (76.4%) | 362/483 (74.9%) | **−7문항** (−1.45%p) |
  | | | **평균** | **−0.67문항 (−0.14%p)** |

- **판정**: 차이의 부호가 섞임(+4 / +1 / −7) → **효과 없음 확정. H15(DPO) 최종 기각.** 학습 축 6전(SFT×3, GRPO×2, DPO×1) 전부 성공 기준 미달로 종료
- **부산물 — 로컬 노이즈의 직접 측정 (방법론 자산)**: 이 실험은 의도치 않게 **같은 베이스 모델의 시드별 변동**을 재줬다. 베이스만으로 364/364→369문항, 즉 **75.4~76.4%(1.0%p 폭)**. DPO는 74.9~76.6%(1.7%p 폭). 프로젝트 초기에 채택한 **±1%p 노이즈 원칙이 실측으로 검증**된 셈이며, exp34b가 잰 LB 재실행 변동(0.48%p)과도 정합한다
- **교훈 (기록해둘 것)**: 시드 42 하나만 봤을 때 +0.83%p는 "학습 6전 만의 첫 양의 신호"처럼 보였고, 실제로 그렇게 해석할 뻔했다. **단일 시드로 채택 판단을 했다면 최종 스택에 무의미한 어댑터를 넣었을 것이다.** 이 프로젝트가 반복해서 배운 것과 같은 교훈이 또 한 번 확인됐다
- **최종 스택 영향**: **없음.** 어댑터 미사용(무학습 베이스 + 가중 SC n=32) 유지 — 이번엔 그 선택에 대한 근거가 하나 더 늘었다
- **결과 파일**: `results/val_samples_s4{3,4}.jsonl`, `results/val_samples_dpo_s4{3,4}.jsonl`, `paired_seeds.log`

## 실험 47: DPO (H15) — 성공 기준 미달, 그러나 다양성 붕괴 가설은 반박됨 (2026-08-11, AWS)

- **배경**: 학습 축 5전 전패(SFT×3, GRPO×2). exp09b에서 원인을 "정확도보다 SC 다양성이 먼저 죽는 것"으로 진단(greedy −1.9%p < SC −2.9%p)했으나, 그 진단은 **정확도 차이에서 역추론한 것**이지 다양성을 직접 잰 것이 아니었다. DPO는 정답을 끌어올리는 대신 오답을 상대적으로 누르는 학습이라 그 경로를 우회할 여지가 있다는 것이 유일한 근거
- **데이터**: `data/verifier.jsonl`(6,000문제×4샘플=24,000줄) 재활용 — **추가 생성 0**. 정답·오답이 모두 존재하는 문제 1,464개(24.4%)에서 문제당 최대 2쌍 → **선호쌍 1,832개**. 나머지 75.6%는 4샘플이 전부 정답이거나 전부 오답이라 쌍을 만들 수 없음
- **설정**: LoRA r=8 / alpha=16 / beta=0.3 / lr=5e-6 / 1epoch(115스텝). SFT(r=16, lr=1e-4)보다 의도적으로 보수적 — 다양성 붕괴를 피하려는 설계
- **학습 지표**: train_loss 0.693(=ln2, DPO 시작점) → **0.67**로 거의 움직이지 않음. **rewards/accuracies 0.60**(우연이 0.50) — 학습에 쓴 쌍에서조차 정답 풀이를 오답보다 높게 평가하는 비율이 60%에 그침
- **결과 (측정 2회, 서로 어긋남)**:

  | 측정 | greedy | SC n32 다수결 | SC n32 가중 |
  |---|---|---|---|
  | 베이스 기준 | 69.4% | 75.4% | 75.8% (366/483) |
  | DPO — 평가 실행(`eval_vllm.py`) | 70.6% | 74.9% | 75.2% |
  | DPO — 덤프 재측정(동일 시드 42) | - | 76.0% | **76.6% (370/483)** |

  같은 어댑터·같은 n·같은 집계 규칙인데 가중 값이 75.2% vs 76.6%로 **1.4%p** 벌어진다. vLLM 연속배치 비결정성(문서화된 ±1%p)과 exp34b가 측정한 재실행 변동(LB 0.48%p)을 감안하면, **DPO의 효과는 현재 측정 정밀도로 −0.6%p인지 +0.8%p인지 판별 불가**
- **판정**: 성공 기준(SC n=32에서 +1.5%p 이상) **미달** — 어느 측정을 쓰든 기준에 닿지 않음. 다만 **학습 6전 중 유일하게 SC가 확실히 하락하지 않은 실행**이므로 "학습은 무조건 손해"라는 단정은 불가. exp48(시드 대응 비교)로 노이즈 여부 판정 중
- **파생 발견 (더 중요)**: 다양성 붕괴 가설 반박 — 아래 별도 절 참고
- **결과 파일**: `results/eval_dpo.json`, `results/val_samples_dpo.jsonl`, `train_dpo.log`, `eval47_dpo.log`
- **사고 기록 2건**:
  1. TRL 0.24의 `DPOTrainer`가 `mergekit`을 강제 임포트하는데 mergekit이 transformers 5.x에서 삭제된 `TRANSFORMERS_CACHE`를 참조한다. **`uv pip install mergekit`을 그냥 실행하면 transformers가 5.14.1→5.12.1로 다운그레이드되어 vLLM(=8/31 최종 파이프라인)이 위험해진다** — 실제로 한 번 발생시켰고 즉시 원복. 최종 조치는 `--no-deps` 설치 + `train_dpo.py` 내부 shim
  2. `DPOTrainer.__init__`이 `model.warnings_issued`를 쓰는데 이 속성도 transformers 5.x에서 삭제됨 → 모델에 빈 dict 주입으로 우회
- **판정 정정 이력**: 최초에 "greedy↑/SC↓ = exp09b와 동일한 다양성 붕괴 패턴"으로 판정했으나, 시드를 맞춰 재측정하고 다양성을 직접 재보니 **틀린 판정**이었다. 아래 절이 그 근거

### 실험 47-B: 표본 다양성 직접 측정 — "학습이 SC 다양성을 죽인다"는 진단 반박

exp09b 이후 이 프로젝트는 학습 실패의 기전을 "다양성 붕괴"로 설명해왔지만, 근거는 greedy·SC 하락폭 차이에서 온 **역추론**이었다. 도구를 만들어 직접 쟀다
(`remote/dump_samples.py --adapter` 신설, `remote/compare_diversity.py` 신규 — 동일 시드 42, n=32).

| 지표 | 베이스 | DPO | 차이 |
|---|---|---|---|
| **pass@32** (정답이 한 번이라도 등장) | 87.8% | **88.4%** | **+0.62%p** |
| 단순 다수결 1위 정확도 | 75.4% | 76.0% | +0.62%p |
| 문제당 고유 답 개수 (평균) | 5.06 | **5.12** | +0.05 |
| 1위 답 득표 비율 (평균) | 77.8% | **77.5%** | −0.27%p |
| 답 추출 실패 문항 | 0 | 0 | 0 |

**해석**: 다양성이 줄었다면 pass@32가 떨어지고(정답 도달 경로 상실) 1위 득표율이 올라야(분포 첨예화) 하는데, **둘 다 반대 방향**이다. 고유 답 개수도 늘었다. 즉 이 DPO 설정은 다양성을 해치지 않았고, SC 하락처럼 보였던 것은 표본 변동이었다.

**따라서 "학습 트랙 6전 전패"의 기전 설명을 수정해야 한다.** 최소한 DPO에 대해서는 다양성 붕괴가 원인이 아니다(SFT에 대해서는 여전히 미측정 — 같은 방법으로 재검증 가능하나 SFT 어댑터 재평가는 별도 비용). 더 정확한 설명은 **"이 모델은 풀이의 정오를 인식하지 못한다"** 쪽이다 — exp18c(절대 채점 +0.5%p), exp37b(상대 비교 +0.21%p), exp47(DPO 보상 정확도 0.60)이 모두 같은 결론을 가리킨다.

## 실험 46: vLLM 스택 스모크 테스트 — 통과 (2026-08-11, AWS)

- **배경**: 2026-08-11에 DPO(H15, exp47) 학습을 위해 venv에 mergekit·llm-blender 등을 `--no-deps`로 추가 설치했다. transformers 5.14.1·vLLM 0.26.0 버전은 원복·유지됨을 확인했으나, 실제 생성 경로(vLLM)가 여전히 정상 동작하는지는 미확인 상태였다. vLLM은 8/31 최종 파이프라인의 핵심이므로 DPO 학습 전에 먼저 무결성을 검증
- **설정**: `remote/eval_vllm.py --mode greedy --val-n 60 --tag smoke` (베이스, 어댑터 없음, val_n=60 중 오류 필터 후 58문항)
- **결과**: greedy 69.0%(40/58), 잘림 비율 4/58(6.9%). 정상 종료, 에러 없음, 완료 후 GPU 유휴(0 MiB) 확인. 판정 기준(65~75% 대역, 60문항 표본이라 오차가 크므로 스택 무결성 확인이 목적이지 성능 측정이 아님) **통과**
- **판정**: **통과** — DPO용 패키지 추가가 vLLM 생성 경로에 지장을 주지 않음 확인. exp47(DPO) 진행 가능
- **결과 파일**: `results/eval_smoke.json`, `eval46_smoke.log`
- **다음**: 큐 다음 항목 exp37b(쌍대 비교 정정판 재시도, margin=3/gen-n=32 + logprobs=0 수정)

## 실험 37b (정정판): 쌍대 비교 토너먼트 재시도 — 성공 기준 미달, H21 최종 기각 (2026-08-11, AWS)

- **배경**: exp37b 1차 시도(margin=2, gen-n=32)가 `logprobs=0` 누락 버그가 있는 채로 실행돼 대표 풀이 확보 0건(무효)이었다. 버그 수정(재생성 `SamplingParams`에 `logprobs=0` 추가, 커밋 e6da3f9) 후 큐 명세대로 정정판을 재실행
- **설정**: `uv run python remote/eval_pairwise_tournament.py --margin 3 --gen-n 32 --votes 5` (베이스 모델, `results/val_samples.jsonl` n=32 기준 접전 문항 선별)
- **결과**: 접전 65문항 중 **대표 풀이(양쪽 답 모두) 확보 57문항**, 확보 실패 8문항(기준값 유지) — 버그 수정이 실제로 작동함을 확인(1차의 0건과 대비). 57문항에 대해 토너먼트(5회 A/B 질의 다수결) 실행: **baseline(가중 SC n=32) 75.8%(366/483) → tournament 76.0%(367/483), delta +1문항(+0.21%p)**. 접전 부분집합만 보면 10/57 → 11/57
- **판정**: 성공 기준(+5문항 이상) 대비 크게 미달 — **H21(쌍대 비교 토너먼트) 최종 기각**. 이번엔 가설이 실제로(0건이 아닌 57문항 규모로) 검증됐으므로, "LLM이 절대 채점보다 상대 비교를 잘한다"는 통념이 이 태스크·이 스케일에서는 확인되지 않는다는 결론이 유효함(1차와 달리 미검증이 아니라 확정 기각)
- **결과 파일**: `results/eval_pairwise_tournament.json`, `eval37b_pairwise.log`
- **사고 기록**: 세션 진입 시 이전(자율 러너) 세션이 이미 정정판을 실행·완료해둔 상태(04:29 UTC 종료, GPU 유휴 확인, JSON의 margin/gen_n/votes가 큐 명세와 정확히 일치)를 발견해 재실행 없이 로그·결과 파일 검증 후 기록만 수행
- **다음**: 큐 다음 항목 exp47-dpo (H15, DPO 파이프라인)

### 부록 — 선택 여지(selection gap) 분해: "심판을 더 잘 만들면 얼마나 벌 수 있는가"

exp37b가 +1문항에 그친 뒤, 이 트랙에 더 투자할 가치가 있는지 판단하려면 **상한**을 알아야 한다.
`remote/analyze_selection_gap.py`로 계산(GPU 불필요, `results/val_samples.jsonl` n=32 사용).

| 선택 방식 | 정확도 | 현재 대비 |
|---|---|---|
| **현재 스택 (확신도 가중 투표)** | **75.8% (366/483)** | — |
| 단순 다수결 1위 | 75.4% (364/483) | −0.41%p |
| 정답이 상위 2후보 안 | 79.9% (386/483) | **+4.14%p** |
| 정답이 상위 3후보 안 | 83.0% (401/483) | +7.25%p |
| 정답이 32개 중 어디든 (pass@32) | 87.8% (424/483) | +12.01%p |

접전(1위−2위 득표차 ≤ 3) 부분집합 65문항: 현재 스택 11문항 정답, 정답이 상위 2후보 안 20문항
→ **완벽한 심판이라도 토너먼트의 상한은 +9문항(+1.86%p)**. exp37b 실측은 +1문항.

**해석 (발표용 핵심)**
1. 선택 여지는 실재한다 — 0.80 LB에 필요한 +7문항은 상위 2후보 헤드룸(+20문항)의 35%만 건져도 된다.
2. 그런데 **이 모델은 그 선택을 스스로 개선하지 못한다.** 절대 채점(exp18c 검증자, +0.5%p)과 상대 비교(exp37b 토너먼트, +0.21%p) 두 방식 모두 실패했다 — 자기 자신을 심판으로 쓰는 경로는 두 방향 모두 소진.
3. 토너먼트가 건드리는 접전 범위 안의 상한은 +1.86%p뿐이다. 나머지 헤드룸(+20 중 11문항)은 **1위가 4표 이상 앞서면서 틀린** 경우에 있어, 확신에 찬 다수결을 뒤집어야 도달 가능하다 — 위험이 크고 이 모델의 심판 능력으로는 접근 불가.
4. 논리적 귀결: 남은 유일한 경로는 **더 나은 심판을 외부에서 들여오는 것**(규칙 5.3이 허용하는 상용 API로 검증자 학습 데이터 구축). 2026-08-11 시점에는 사용자 판단으로 보류.

## 실험 43: 발표 자산화 — docs/presentation.md 전면 갱신 (2026-08-11, 문서 작업 — 실행 없음)

- **배경**: 큐 명세 — 발표 평가(배점 50%) 대비 자산화. `docs/presentation.md`가 c1c765b 커밋(exp30/31/37 완료 직후) 이후로 갱신되지 않아, 그 뒤에 나온 exp32(Budget Forcing)·exp36(top_p)·exp38(제출 다양성)·exp39(최종 파이프라인 동결)·exp41(분산 축소, H22 기각)·exp42(Budget Forcing+SC, H20 최종 기각)·exp44/45(trim 정밀 탐색)가 문서에 전혀 반영되지 않은 상태였음
- **작업 내용** (실행 없이 문서만 갱신, 전부 EXPERIMENTS.md·queue.md 기존 기록 근거):
  1. 헤더의 실험 수 표기를 29개→45개로 갱신, 2026-08-11 최신화 메모 추가
  2. 슬라이드5(검증자)에 exp37 쌍대 비교 토너먼트(H21, 표본 확보 병목으로 미검증 기각) 행 추가, 결론 문단에 "검증 축(절대·상대 모두) 소진" 서술 보강
  3. **슬라이드6.5를 "리더보드 노이즈 측정"으로 재작성** — exp34b(동일 설정 재실행 0.48%p 변동)에 exp41(H22, 독립된 두 96표 병합 세트의 LB 차이가 오히려 0.84%p로 확대되고 로컬 예측답 차이는 대리 지표로 부적절함이 드러난 사례)과 exp38(w48/dt32 추가 제출도 0.776~0.785 노이즈 띠 안)을 통합해 "왜 ±1%p 이하 개선을 채택하지 않았는가"의 방법론 서술을 완성
  4. 슬라이드7에 exp36(top_p)·exp37(쌍대비교)·exp41(분산축소)·exp42(BF+SC)·exp44(trim 정밀탐색) 기각 사례를 표로 추가
  5. 슬라이드8에 exp39(최종 파이프라인 동결)의 선정 근거인 "4회 반복 관측이 0.780 근방으로 수렴" 표 추가
  6. **슬라이드9.5를 "소진된 탐색 지도"로 재작성** — 큐 명세가 요구한 9개 축(SC스케일/온도/가중/프롬프트/자기수정/검증자/SFT/GRPO/BudgetForcing)을 정확히 표로 매핑(기존 문서는 "자기수정"과 "BudgetForcing"을 한 행에 합쳐뒀던 것을 분리), 각 행에 성공 기준·기각 근거·결론을 명시. exp42로 Budget Forcing(9개 축 중 유일한 지속 양의 신호)까지 최종 기각되며 9개 축이 전부 소진됐음을 결론에 반영
  7. 슬라이드10 결론과 "검증 제출물 대응" 표에 `docs/final-pipeline.md`(exp39 산출물) 항목과 wandb run 개수 오기(6개→7개) 수정 반영
- **report.html 확인**: 큐 명세는 "ALL_RUNS 배열에도 exp32(budget forcing 70.0%) 추가"를 요구했으나, 실제 확인 결과 `report.html`은 exp42 커밋(d450e02) 시점에 이미 exp32·exp37·exp38·exp42·exp44 항목이 전부 반영된 최신 상태였음(별도 세션이 코드 커밋 시 함께 갱신해둔 것으로 추정, queue.md 체크 표시만 누락) — 추가 변경 없이 검증만 수행
- **결과 파일**: `docs/presentation.md` (수정), `report.html` (변경 없음, 검증만)
- **다음**: 큐 다음 항목 exp37b-pairwise-fix

## 실험 37b (1차 시도, 무효): 쌍대 비교 토너먼트 재시도 — **오진단, 결과 무효** (2026-08-11, AWS)

> ⚠️ **정정 (2026-08-11)**: 아래 기록은 `remote/eval_pairwise_tournament.py`가 **`logprobs=0` 누락 버그**를 가진 상태(03:00 UTC 종료, 버그 수정 커밋 e6da3f9는 03:01:58 UTC)로 실행된 결과다. 이 버그는 재생성 SamplingParams에 `logprobs=0`이 없으면 vLLM이 `cumulative_logprob`을 전부 `None`으로 반환해, 대표 풀이 선정 루프(`if a is None or c.cumulative_logprob is None: continue`)가 **모든 후보를 무조건 걸러내는** 것이었다 — 즉 gen-n을 얼마로 잡든 확보 건수는 항상 0이 나올 수밖에 없는 코드 결함이었다. 아래의 "구조적 병목" 결론과 **H21 최종 기각 판정은 무효**. 로컬 Claude(계획자)가 버그를 수정하고 큐에 올바른 재시도(exp37b, margin=3/gen-n=32 + logprobs=0)를 등록했으며, 그 결과는 아래 "실험 37b(2차, 정정판)" 절 참고
- **배경**: exp37이 접전 65문항 전부에서 대표 풀이(양쪽 후보 답) 확보에 실패(당시 gen-n=8이 원인으로 **오추정**)해 토너먼트 판정 로직이 한 번도 실행되지 못했다. (오진단에 따라) 병목을 수정해 재시도: ① 접전 기준을 margin≤3→margin≤2로 좁혀 진짜 접전만 선별 ② 대표 풀이 확보용 재생성을 gen-n=8→gen-n=32(4배)로 늘려 소수 득표 답의 재현 확률을 높임 ③ 나머지(votes=5, 쌍대 비교 로직)는 exp37과 동일
- **설정**: `remote/eval_pairwise_tournament.py --margin 2 --gen-n 32 --votes 5` (버그 수정 전 스크립트, CLI 인자만 변경)
- **결과(무효)**: margin≤2로 좁히자 접전 문항이 65→50개로 감소. gen-n을 4배로 늘렸음에도 대표 풀이 양쪽 확보 0건 → 토너먼트 0문항 실행, baseline과 동일(366/483, 75.8%, delta +0). **원인은 표본 부족이 아니라 logprobs 누락 버그였으므로 이 결과로는 아무것도 판정할 수 없다**
- **결과 파일**: `results/eval_pairwise_tournament.json`(정정판 실행 시 덮어써짐), `eval37b_pairwise.log`(1차)
- **사고 기록**: 세션 진입 시 이전 세션이 이미 (버그가 있는 채로) 실행·결과 커밋(03:00 UTC 종료)까지 완료해둔 상태를 발견해 검증 후 기록했으나, 기록 직후 로컬 Claude가 실제 원인(logprobs 누락)을 규명·수정하고 큐를 갱신한 것을 뒤늦게 확인 — 본 절은 정정 기록으로 남기고 실제 판정은 정정판 실행 결과로 대체
- **다음**: 큐의 올바른 exp37b(정정판, margin=3/gen-n=32 + logprobs=0 수정) 실행 필요 — 실행 순서상 exp46(스택 스모크)이 먼저

## 실험 42: Budget Forcing + SC n=8 결합 (H20 후속) — 목표 미달, H20 최종 기각 (2026-08-11, AWS)

- **배경**: 큐 명세 — exp32에서 Budget Forcing(greedy, "Wait" 재검토 이어붙이기)이 +0.6%p로 지금까지 시도한 모든 추론 기법 중 유일한 양의 신호였으나 성공 기준(+2%p)에는 못 미쳤다. SC(n=8)와 결합했을 때 유의미해지는지는 미검증이었으므로 낮은 우선순위로 확인
- **실행**: `uv run python remote/eval_budget_forcing.py --rounds 1 --n 8` (베이스 모델, temp=0.7/top_p=0.8, 검증 483문항). round0=평소 SC n=8 생성 후 다수결, round1=각 표본 뒤에 "Wait, let me double-check my reasoning and the arithmetic." 문구를 이어붙여 재생성 후 다수결
- **결과**: round0 73.9%(357/483) → round1 74.5%(360/483, **+0.6%p**, round0 대비) — exp32의 greedy 결과(+0.6%p)와 방향·크기 모두 일치. 다만 이 실험의 진짜 비교 기준은 round0(같은 스크립트의 재현이라 시드·설정이 다를 수 있음)이 아니라 **exp05의 SC n=8 74.7%(361/483)** — round1(74.5%)은 이 기준 대비 **−0.2%p**로 판정 기준(+1%p 이상) 미달
- **결론**: SC와 결합해도 Budget Forcing이 유의미한 개선을 만들지 못함 — **H20(Budget Forcing) 최종 기각** (greedy round0→round1 exp32에서 +0.6%p로 성공기준 미달·SC 결합도 exp05 기준 대비 미달). "표 하나의 질을 올리는" 유일한 미탐색 축이었으나 이것도 소진 — 지금까지 시도한 모든 축(SC스케일링/온도/가중투표/프롬프트앙상블/자기수정/검증자/QLoRA/GRPO/BudgetForcing)이 전부 정체 확인된 것으로 최종 마무리
- **운영 메모**: 세션 진입 시 이전 세션(자율 러너, run_experiments.sh)이 이미 백그라운드로 프로세스(PID 45059)를 실행·완료해뒀으나(01:37 시작→01:55 완료, `eval42_bf_sc.log` 정상 종료, `results/eval_bf_r1_n8.json` 저장) 결과 기록(EXPERIMENTS.md·log.csv·queue.md 체크)까지는 마치지 못한 채 세션이 종료돼 있었음 — 이번 세션에서 GPU 유휴(0MiB/0%) 확인 후 재실행 없이 로그·결과 파일 검증만으로 기록 완료
- **결과 파일**: `results/eval_bf_r1_n8.json`, `eval42_bf_sc.log`
- **다음**: 큐 다음 항목 exp43-presentation-assets

## 실험 39: 최종 파이프라인 동결 문서화 (2026-08-11, 문서 작업 — 실행 없음)

- **배경**: 큐 명세 — 지금까지 LB 점수가 확인된 모든 제출을 표로 정리하고, 로컬+LB 양쪽에서 일관되게 좋은 설정을 최종 스택으로 선정해 `docs/final-pipeline.md`에 기록. **LB 최고점이 아니라 원리적으로 설명 가능한 것을 고를 것**(과적합 방지)이 조건. `docs/final-pipeline.md` 자체는 exp30 리허설 직후(08-11 새벽) 이미 생성돼 있었으나, 전체 제출 이력 표와 선정 근거 서술이 빠져 있었음
- **집계**: `experiments/log.csv`에서 leaderboard 열이 채워진 행 10건을 추출(exp01 baseline은 구채점기준이라 참고용, 실질 비교 대상은 신기준 9건):
  - exp06b(SC n=8) 0.77015 — 최초 신기준 제출
  - **exp26(SC n=32 가중, seed42) 0.78459** — 역대 최고점
  - exp29-1(SC n=64 가중) 0.77978, exp29-2(SC n=32 단순다수결) 0.77737 — 둘 다 n32w 대비 하락, H11·H16 LB 재확인
  - **exp34b(exp26과 완전 동일 설정 재실행) 0.77978** — 같은 설정인데 0.48%p 하락, 노이즈 하한 측정치
  - exp41a(3시드 병합 96표) 0.77617, exp41b(3시드 병합 96표, 대조군) 0.78459 — H22(분산 축소) 기각의 근거
  - exp38-w48(SC n=48 가중) 0.78219, exp38-dt32(SC n=32+trunc제외) 0.77978
- **선정 근거**: 단순 최고점(0.78459)만 보면 exp26·exp41b이지만, exp34b가 "완전히 동일한 설정"의 재실행만으로 0.48%p 하락함을 실측했으므로 단일 최고 관측치를 최종 스택 근거로 삼는 것은 과적합이다. 대신 **"가중 SC n=32, temp0.7/top_p0.8, 어댑터 없음" 설정이 반복 관측된 값들의 평균이 0.780 근방으로 수렴**하고, 이 값이 다른 모든 시도한 축(n=64 확대·가중 제거·다중시드 병합·n=48)보다 항상 같거나 높다 — **탐색한 축 중 유일하게 반복 검증된 안정적 선택지**. 로컬 검증(75.8%)과의 오프셋(+2.7%p)도 exp06b(74.7%→0.77015)와 exp26(75.8%→0.78459) 두 지점에서 일관됨. 기존에 이미 이 설정을 확정 스택으로 서술해뒀던 `docs/final-pipeline.md`의 선택이 이번 전수 비교로도 그대로 확인됨(변경 없음)
- **문서 갱신 내용**: ① "LB 점수가 확인된 전체 제출 이력" 표 신설(10행) ② "선정 근거" 절 신설(위 내용) ③ 표본 생성 명령에 `--seed 42` 명시 ④ "8/31 당일 체크리스트" 절 신설(기존 "사전 점검(전날)"과 구분 — 착수 시각·진행 확인·검증·제출 전 사용자 확인·제출 후 확인까지 당일 실행 순서를 체크리스트화)
- **결과 파일**: `docs/final-pipeline.md` (커밋 대상)
- **다음**: 큐 다음 항목 exp42-bf-sc로 진행

## 실험 38: 제출 다양성 유지 — 미제출 후보 선별 제출 (2026-08-11, AWS)

- **배경**: exp34에서 집계 규칙 5종(w48/tr32/w32b/dt32/tb32) CSV를 생성해뒀으나, 그중 tr32·w32b는 이후(exp34b) 이미 제출됐고 나머지(agg=exp31c의 trim25 결과·w48·dt32·tb32)는 미제출 상태로 남아 있었다. 큐 명세: 미제출 후보 중 **기존 제출과 15문항 이상 다른 것만** 제출, 답이 같으면 생성만 하고 제출 생략, 하루 3건 이내
- **중복 확인**: 커밋된 CSV 전수 비교 결과, `submission_agg.csv`(exp31c, trim25/n32)는 이미 제출된 `submission_tr32.csv`(exp34-trim, 동일 규칙 trim25/n32, 0.77737)와 **완전 동일**(831문항 중 0문항 차이 — 같은 덤프·같은 규칙이므로 당연한 결과). `submission_tb32.csv`(tiebreak_conf/n32)는 이미 제출된 `submission_w32b.csv`(weighted/n32, 0.77978)와 **5문항만 차이**(15문항 기준 미달, "판별 불가") — tiebreak_conf가 동률에서만 weighted와 갈리므로 대부분 표가 같음. 두 후보 모두 **제출 생략**(파일은 이미 존재, 재생성 불필요)
- **제출 대상**: `submission_w48.csv`(weighted, n=48 — n32w 대비 63문항 차이) · `submission_dt32.csv`(drop_trunc, n=32 — n32w 대비 70문항 차이) — 둘 다 모든 기존 제출 대비 15문항 이상 차이 확인(최소 diff: w48 vs s3x32 39문항, dt32 vs w32b 19문항)
- **제출 이력 확인**: `kaggle competitions submissions`로 사전 확인 — w48/dt32 모두 이전에 제출된 적 없음(중복 제출 방지, exp29 교훈 반영)
- **결과**: w48 → **LB 0.78219**(n32w 0.78459 대비 -0.0024, 지금까지 제출한 것 중 n32w 다음으로 높은 점수). dt32 → **LB 0.77978**(n32w 대비 -0.00481, w32b·n64w와 동점)
- **판정**: 둘 다 기존 최고(n32w) 대비 소폭 하락 — 새 최고 기록은 아니지만 exp34b의 노이즈 범위(±0.48%p) 안에 있어 "집계 규칙이 전부 동등하다"는 기존 결론과 일치. w48이 가장 근접(-0.24%p)한 것은 표본 수를 늘린(n=48) 가중 방식이 n=32보다 약간 더 안정적일 가능성을 시사하나 n=1 비교라 확증 불가
- **제출 자제**: 큐의 "하루 3건 이내" 자체 제한에 따라 2건만 제출(agg·tb32는 위 중복 확인에서 이미 제외). 오늘(2026-08-11) 실제 Kaggle 일일 한도는 exp41(2건: s3x32/s3x32v) + 본 실험(2건: w48/dt32) = 4건 사용, 제출 후 CLI 출력 기준 1건 남음
- **결과 파일**: `results/submission_w48.csv`, `results/submission_dt32.csv`(이미 exp34에서 생성·커밋됨, 재생성 없이 그대로 제출). `results/submission_agg.csv`, `results/submission_tb32.csv`는 제출 생략(파일은 기존 그대로 유지)
- **다음**: 큐 다음 항목 exp39-final-pipeline-freeze로 진행

## 실험 41: 분산 축소 스택 (exp34b 후속) — LB 골드스탠다드 기준 가설 기각 (2026-08-11, AWS)

- **배경**: exp34b에서 동일 설정(가중 SC n=32) 재실행만으로 LB 점수가 0.48%p(0.78459→0.77978, 67문항 예측 변경) 흔들린다는 사실이 드러났다. 최종전은 새로운 2,000문항으로 채점되므로, **평균 정확도가 아니라 "운에 덜 흔들리는" 분산 축소**가 갭 방어에 더 중요할 수 있다는 가설(H22, 신규)을 세웠다. 방법: 서로 다른 시드로 뽑은 n=32 표본 3세트를 96표로 합쳐 집계하면 표본 평균이 안정돼 재실행 변동이 줄어드는가?
- **사전 준비 확인**: `remote/dump_lb_samples.py`는 이미 `--seed` 인자를 보유(38220bf에서 이전 세션이 커밋 완료, 이번 세션에서 추가 구현 불필요). 세션 진입 시 GPU가 유휴 상태였고 필요한 5개 덤프(seed42 기존 n=96, seed43·44·45·46·47 각 n=32)가 이미 831/831문항 완료돼 있음을 확인 — 이전 세션(자율 러너)이 생성 단계까지 마쳐두고 집계·분석·기록을 남기지 못한 상태였음. 재생성 없이 기존 덤프로 진행
- **설정**: `remote/merge_lb_dumps.py`(신규 작성·커밋, 여러 덤프 파일을 문항별로 병합) 사용
  - **s3x32** (검증 대상): `lb_samples.jsonl`(seed42, 앞 32표) + `lb_samples_seed43.jsonl`(32표) + `lb_samples_seed44.jsonl`(32표) = 문항당 96표 → `make_submission_from_dump.py --rule weighted --tag s3x32`
  - **s3x32v** (독립 대조군, 검증용): `lb_samples_seed45.jsonl` + `lb_samples_seed46.jsonl` + `lb_samples_seed47.jsonl`(각 32표) = 문항당 96표 → 동일 규칙으로 `--tag s3x32v`
  - 두 스택은 서로 완전히 독립된 시드 조합이므로, 이 둘의 LB 점수 차이가 곧 "3-시드 결합 방식의 재실행 변동폭"이 된다 — exp34b의 "단일 n=32 재실행 변동(0.48%p)"과 같은 종류의 측정을 96표 결합 방식에 적용한 것
- **로컬 진단(1차)**: s3x32 vs s3x32v 예측 답 비교 — **35문항(4.2%) 다름**. exp34(w32b vs n32w, 단일 n=32 재실행) 기준 67문항(8.06%) 대비 **47.8% 감소**. 큐에 명시된 성공 기준("단일 시드 n=32 재실행의 차이(67문항)보다 뚜렷히 적으면 성공")을 문자 그대로 적용하면 **통과**로 보임
- **LB 실측(2차, 결정적)**: 자율 운영 정책(CSV 생성 시 즉시 제출)에 따라 두 CSV 모두 제출
  - s3x32: **0.77617** (645/831 정답)
  - s3x32v: **0.78459** (652/831 정답, 기존 n32w와 정답 수 완전 일치)
  - **두 독립 96표 스택 간 LB 점수 차이 = 0.842%p(7문항)** — exp34b의 단일 n=32 재실행 변동(0.481%p, 4문항)보다 **오히려 더 큼**
- **핵심 발견 — 로컬 지표와 LB 실측이 반대 방향**: 예측 답이 갈리는 문항 "개수"는 절반으로 줄었는데(67→35), 실제 정답 수 변동은 거의 두 배로 늘었다(4문항→7문항). 이는 "몇 문항이 갈리는가"와 "그 갈린 문항들이 실제로 정답/오답을 얼마나 뒤집는가"가 별개라는 뜻 — 35문항 중 우연히 정답을 뒤집는 비율이 67문항 때보다 높았던 것으로 추정(n=1 비교쌍이라 확증은 어려움). **로컬 예측-차이 문항 수는 정확도 분산의 신뢰할 수 있는 대리 지표가 아니다**라는 방법론적 교훈
- **판정**: LB가 이 프로젝트의 골드스탠다드 판별 기준(CONTEXT.md 해석 원칙)이므로, 로컬 지표상 "성공"이었더라도 **LB 실측 기준으로는 가설(3-시드 결합이 분산을 줄인다) 기각**. n=1 비교쌍의 한계로 통계적으로 약한 증거이지만 방향성이 명백히 가설과 반대 — **3-시드 결합 스택은 채택하지 않음**, 최종 스택은 여전히 n32w(단일 seed42, n=32 가중, 0.78459) 유지
- **결과 파일**: `results/submission_s3x32.csv`, `results/submission_s3x32v.csv`(둘 다 831행/정수/결측·중복 없음 확인, n32w 대비 각각 55·59문항 달라 구조적 차이 기준 충족). 병합 중간 파일(`results/lb_samples_s3x32{,v}.jsonl`)은 용량 문제로 `.gitignore` 추가(재생성 수 초 이내 가능이라 커밋 불필요)
- **제출 현황**: 오늘(2026-08-11) 일일 한도 5건 중 2건 사용, 3건 남음
- **다음**: 큐 다음 항목 exp38(제출 다양성 유지)로 진행

## 실험 37: 쌍대 비교 토너먼트 (H21) — 대표 풀이 재확보 실패로 토너먼트 실질 미실행, 가설 미검증 기각 (2026-08-10, AWS)

- **배경**: H9(검증자, 독립 채점)가 v1·v2 모든 변형에서 기각됐다. 대안 가설: LLM은 절대 채점(이 답이 맞나?)보다 **상대 비교**(A와 B 중 어느 쪽이 맞나?)를 더 잘한다 — 검증자 대신 접전 문제만 쌍대 비교로 재판정
- **설정**: `remote/eval_pairwise_tournament.py`(신규 작성·커밋) — results/val_samples.jsonl(exp31a, n=32 표본)에서 1·2위 후보 답의 득표차가 margin(3) 이하인 문제를 "접전"으로 분류(65문항). 각 접전 문제를 gen-n=8로 재생성해 후보 답별 최고 확신도 대표 풀이(전체 텍스트)를 확보 → 둘 다 확보되면 나란히 제시해 베이스 모델에게 "A/B 중 정답은?"을 5회(votes=5) 질의, 다수결로 승자 답 채택. 비접전·확보 실패 문제는 기존 가중 다수결(n=32, 75.8%) 값 유지
- **결과**: 접전 65문항 전부 재생성 8샘플 안에서 **두 후보 답을 동시에 재현하지 못함**(확보 성공 0건) → 토너먼트가 단 한 건도 실행되지 못한 채 전량 baseline 값으로 폴백. `[baseline weighted n=32] 75.8% (366/483)` → `[pairwise tournament] 75.8% (366/483), delta=+0문제 (+0.00%p)`. 성공 기준(+1.5%p) 명백히 미달
- **원인 분석**: margin≤3인 접전은 정의상 2위 답이 32표 중 소수 득표(예: 상위 14표 vs 11표처럼 흔치 않고, 실제로는 min 득표 근처의 조합도 포함) — gen-n=8 재생성으로 그 특정 소수 답을 다시 뽑을 확률이 낮아, 대표 풀이 확보 자체가 병목이 됨. **가설(상대 비교 우위) 자체는 이 실행에서 검증되지 못함** — 토너먼트 판정 로직은 한 번도 호출되지 않았으므로 "쌍대 비교가 약하다"가 아니라 "재생성으로 특정 소수 답을 다시 뽑기 어렵다"는 표본 확보 단계의 실패
- **판정**: 성공 기준(+1.5%p) 미달로 **기각**. 큐 명세(gen-n=8, 일회용 스크립트)를 그대로 따른 결과이며, 재시도(gen-n 상향 등)는 큐에 없어 임의 추가하지 않음 — 로컬 Claude(계획자)가 후속 여부 판단 필요(예: 대표 풀이를 재생성 대신 원본 n=32/64 표본에서 텍스트까지 저장해두는 방식으로 재설계하면 확보율이 오를 가능성)
- **결과 파일**: `results/eval_pairwise_tournament.json`, `eval37_pairwise.log`
- **사고 기록**: 세션 진입 시 이전 세션(자율 러너)이 스크립트 작성·실행·결과 커밋까지 이미 완료해둔 상태(로그 19:32~19:35 정상 종료, GPU 유휴 확인)를 발견 — 재실행 없이 로그·결과 파일 일치 확인 후 기록만 대행
- **다음**: 큐 다음 항목 exp41(분산 축소 스택)로 진행

## 실험 44: trim_lowconf 정밀 탐색 — 성공 기준(+8문제) 미달, n=32 trim25%대(76.4%)가 최종 상한으로 확정 (2026-08-10, AWS)

- **배경**: exp31b에서 유일한 양의 신호였던 trim_lowconf가 n=32에서만 majority 대비 +5문제(76.4%)로 로컬 최고를 기록했고, pass@32=87.8%로 선택 여지가 12%p 남아 있어 정밀 탐색 대상으로 선정
- **설정**: `remote/sweep_aggregation.py`를 확장(커밋) — results/val_samples.jsonl(exp31a, 483문항×64샘플 덤프) 기반, GPU 불필요. 5개 축의 변형 추가: ① trim 비율 스윕(10/20/25/30/40/50%) ② trim 후 가중 결합(softmax scale=2.0) ③ 잘린 풀이(trunc) 제외 후 추가로 trim(10/25/40%) ④ 문항 내 풀이 길이 퍼센타일 기반 trim(5-95/10-90/20-80pct) ⑤ trim 후 최다 득표가 최소 득표수(2/3/4) 미만(합의 실패)이면 원본 전체 표로 폴백. 총 27개 전략 × n={16,32,64}로 `--sweep-n 16,32,64` 1회 실행(수 초). **성공 기준: majority 대비 +8문제 이상**(약 +1.7%p, 노이즈 초과)
- **결과 (n별 상위 전략)**:

| n | 최고 전략 | 정확도 | 맞힌 수 | 기준 대비 | 성공기준(+8) |
|---|---|---|---|---|---|
| 16 | trunc제외+trim10% | 76.0% | 367/483 | +3 | 미달 |
| 32 | trim_lowconf25% / trim10~30%+weighted / trunc제외+trim25% / trim25%+fallback(min2·3) 등 7개 동률 | **76.4%** | 369/483 | **+5** | 미달 |
| 64 | trim_lowconf30% / trim30%+weighted | 76.0% | 367/483 | +2 | 미달 |

  n=32에서 상한(76.4%, +5)에 도달하는 경로가 7가지(단순 trim25%, 가중 결합 3종, trunc제외 조합, 폴백 2종)로 늘었으나 **수치 자체는 exp31b와 완전히 동일** — 서로 다른 정교화가 전부 같은 87.8%(pass@32) 안의 같은 부분집합에 수렴한다는 뜻. n=16·64는 exp31b(각 +0~1)보다 소폭 나아졌지만(+3, +2) 여전히 n=32에 못 미침
- **판정**: **27개 변형 전부 성공 기준(+8문제) 미달** — 정밀 탐색으로도 n=32 trim25%대(+5, 76.4%)의 로컬 상한을 넘지 못함. trim_lowconf 축은 정밀화 여지가 사실상 소진된 것으로 판단, **집계 규칙 탐색 트랙 최종 종료**(exp31b의 잠정 상한이 확정 상한이 됨)
- **결과 파일**: `results/sweep44.log`(gitignore된 *.log 패턴이라 커밋 대상 아님, 서버 보관), 스크립트 자체는 커밋
- **다음**: exp45는 exp44가 새 최고 전략을 냈을 때만 실행하는 조건부 항목인데 갱신이 없어 **skip**(기존 submission_agg.csv, trim25%/n32 그대로 유효) — exp37(쌍대 비교 토너먼트)로 진행

## 실험 36: 샘플링 파라미터 top_p 탐색 — 목표 미달, top_p=0.8 유지 (2026-08-10, AWS)

- **배경**: 지금까지 모든 SC 실험은 top_p=0.8 고정이었다. top_p(누적확률 컷오프)를 높이면 저확률 토큰까지 허용돼 SC 다수결의 재료(풀이 경로) 다양성이 늘어날 수 있다는 가설 — 아직 탐색하지 않은 마지막 샘플링 하이퍼파라미터
- **설정**: `remote/eval_vllm.py --mode sc --n 16 --top-p 0.95 --tag tp95`, 이어서 `--top-p 1.0 --tag tp100` (베이스 모델, temp=0.7 고정, 검증 483문항, seed=123 val — 학습 미사용). 기준: top_p=0.8 n16 = 75.6%(365/483, exp07/exp27)
- **결과**:

| top_p | 다수결 | 가중 | 기준(75.6%) 대비 | 잘림 비율 |
|---|---|---|---|---|
| 0.8 (기준) | 75.6% (365/483) | - | +0 | - |
| 0.95 | 74.3% (359/483) | 74.3% (359/483) | -1.3%p | 1.3% |
| 1.0 | 74.7% (361/483) | 74.5% (360/483) | -0.9%p | 1.2% |

- **판정**: 둘 다 기준보다 낮음(개선 없음) — top_p=1.0은 ±1%p 노이즈 범위 내 하락, top_p=0.95는 노이즈 범위를 살짝 벗어나지만 방향이 악화라 "유의미한 개선"에 해당하지 않음. 큐 명세("유의미하면 그 설정으로 덤프·제출까지")의 실행 조건 불성립 — 후속 덤프·제출 없이 종료. top_p=0.8이 여전히 국소 최적
- **결과 파일**: `results/eval_tp95.json`, `results/eval_tp100.json`, `eval36_tp95.log`, `eval36_tp100.log`
- **사고 기록**: 세션 진입 시 이미 이전 세션(자율 러너)이 두 실행을 완주해둔 상태(GPU 유휴 확인)를 발견해 재실행 없이 로그·결과 파일 검증 후 기록만 수행
- **다음**: exp37-pairwise-tournament (쌍대 비교 토너먼트)

## 실험 31: 집계 전략 대량 탐색 — 생성/집계 분리, n=32에서 trim_lowconf25%가 유일한 양의 신호 (2026-08-10, AWS)

- **배경**: 지금까지 모든 실험은 "표를 어떻게 세는가"를 다수결/가중 정도로만 시도했다. `dump_samples.py`(exp31a)로 검증셋 표본(답·평균 로그확률·잘림여부·길이)을 한 번 저장해두면, `sweep_aggregation.py`(exp31b)로 **GPU 없이 수 초 만에** 12가지 집계 규칙을 표본 수(8/16/32/64)별로 비교할 수 있다
- **exp31a 설정**: `nohup uv run python remote/dump_samples.py --n 64 > dump_samples.log 2>&1 &` — 검증 483문항(500 중 오류 문항 제외, seed=123, 학습에 미사용) × 64샘플(temp=0.7, top_p=0.8, max_tokens=2048). 세션 진입 시 이미 이전 세션이 완주해둔 결과(로그 정상 종료, `results/val_samples.jsonl` 483줄×64샘플, GPU 유휴)를 확인 후 재실행 없이 채택
- **exp31b 결과 (n=32, 최고 구간)**:

| 전략 | 정확도 | 맞힌 수 | 기준 대비 |
|---|---|---|---|
| trim_lowconf 25% | **76.4%** | 369/483 | **+5** |
| trim+weighted (trim25+가중s2.0) | **76.4%** | 369/483 | **+5** |
| weighted s=1.0/2.0/4.0 | 75.8% | 366/483 | +2 |
| tiebreak_by_conf | 75.8% | 366/483 | +2 |
| plausible+weighted | 75.8% | 366/483 | +2 |
| majority (기준) | 75.4% | 364/483 | +0 |
| drop_truncated / prefer_short / plausible_only | 75.4% | 364/483 | +0 |
| trim_lowconf 50% | 74.7% | 361/483 | -3 |

  다른 표본 수에서는 개선폭이 작았다: n=8 최고 74.5%(가중, +1), n=16 최고 75.4%(majority=drop_trunc=plausible, +0), n=64 최고 75.8%(trim_lowconf25%/trim+weighted, +1) — **n=32에서만 +5로 뚜렷하게 튀는 비단조 패턴**. pass@n 상한(표본 중 정답이 하나라도 있으면 명중 처리): n8 82.0%(396)/n16 84.9%(410)/n32 87.8%(424)/n64 89.9%(434) — 집계 개선의 이론적 여지는 아직 10%p 이상 남아있음
- **trim_lowconf25% vs trim+weighted 동점 분석**: 483문항 중 **2문항만 답이 다름** — 사실상 같은 전략(trim이 주효과, 가중은 부차적). 제출에는 더 단순한 trim_lowconf25%(=trim 후 단순 다수결) 채택
- **exp31c 판정**: 큐 기준 "+5문제 이상"을 정확히 충족(경계값)하여 제출 파일 생성 진행. 이미 리더보드용 덤프(exp33, 831문항×96샘플)와 `make_submission_from_dump.py`의 `trim25` 규칙이 존재해 별도 코드 이식 없이 즉시 생성 가능 — `uv run python remote/make_submission_from_dump.py --dump results/lb_samples.jsonl --rule trim25 --n 32 --tag agg` (수 초, GPU 불필요) → `results/submission_agg.csv` (831행, id/answer 2열, 정수만, 결측·중복 없음 확인). 기존 최고 제출(n32w) 대비 76문항 달라 구조적으로 다른 후보
- **제출 보류 사유**: `python -m kaggle competitions submissions` 확인 결과 오늘(2026-08-10) 이미 5건 제출(tr32/w32b/n32m/n64w×2) — exp34b에서 "일일 한도 5회, 오늘 소진" 기록과 일치. 한도 낭비를 피하기 위해 이번 세션에서는 제출을 시도하지 않고 CSV 생성·검증·기록까지만 완료. **다음 가용일에 `submission_agg.csv` 제출 필요**
- **주의**: n=32 한 지점에서만 나타난 개선이라 exp34b의 교훈(동일 설정 재실행 LB 변동 0.48%p)을 고려하면 로컬 +5문제(약 +1.0%p)가 노이즈일 가능성을 배제할 수 없음 — **LB 실측 전까지는 잠정 신호**로 취급할 것
- **결과 파일**: `results/val_samples.jsonl`(커밋), `dump_samples.log`, `results/submission_agg.csv`(커밋)
- **다음**: `submission_agg.csv` 제출(다음 가용일) 후 LB 점수로 최종 판정. 큐 다음 항목은 exp35(budget forcing 분기, skip 예정)~exp43(자율 운영 프로그램)

## 실험 32: Budget Forcing (s1 방식, H20) — 목표 미달, 유일 남은 "표 하나의 질" 축도 소진 (2026-08-10, AWS)

- **배경**: 지금까지 모든 시도(SC 표본 수/온도/가중치/프롬프트 앙상블/자기수정/검증자/QLoRA/GRPO)는 "표를 세는 방식"이었고 개별 풀이의 질은 고정이었다. s1 논문(arXiv:2501.19393)의 Budget Forcing은 모델이 답을 내려 할 때 EOS 대신 "Wait, let me double-check my reasoning and the arithmetic."를 이어붙여 자기 재검토를 강제한다 — 모델 출력만 사용(도구·코드 실행 없음)이라 규칙 적합, 표 하나하나의 질을 올리는 유일하게 남은 축
- **설정**: `uv run python remote/eval_budget_forcing.py --rounds 2 --n 1` (베이스 모델, greedy(temp=0), 검증 483문항(500 중 오류 문항 제외, seed=123 — 학습에 미사용)). round0 = 통상 greedy 생성, round1/round2 = 이전 라운드 텍스트 뒤에 WAIT 문구를 붙여 이어서 생성(각 라운드 max_tokens=1200)
- **결과**:

| 라운드 | 정확도 | 비고 |
|---|---|---|
| round0(기준) | 69.4% (335/483) | exp05 greedy와 일치 |
| round1 | 70.0% (338/483) | 기준 대비 +0.6%p |
| round2 | 70.0% (338/483) | round1과 동일 — 추가 재검토 무효과(수렴) |

- **판정**: 성공 기준(greedy에서 +2%p 이상)에 크게 못 미침(+0.6%p, 지금까지의 ±1%p 노이즈 판정선 안쪽) — **목표 미달**. round1→round2에서 정확도가 전혀 움직이지 않아(338→338, 답이 바뀐 문항이 있어도 순변화 0) 추가 재검토 라운드가 새로운 신호를 주지 못함을 확인. 큐 명세대로 SC 결합 단계(②, `--rounds 1 --n 8`)는 **미실행**(스킵)
- **의미**: "표 하나의 질을 올리는" 유일하게 남은 미탐색 축도 무의미로 판정됨 — 지금까지 시도한 모든 축(SC 스케일링, 온도, 가중투표, 프롬프트 앙상블, 자기수정, 검증자 Best-of-N, QLoRA SFT, GRPO, Budget Forcing)이 전부 ±1%p 안팎에서 정체. 로컬 최선 스택은 여전히 exp25/26(확신도 가중 SC n=32, 로컬 75.8% / LB 0.78459)
- **결과 파일**: `results/eval_bf_r2_n1.json`, `eval32_bf_r2n1.log`
- **다음**: exp30-final-rehearsal (최종 test 실전 리허설)

## 실험 30: 최종 test 실전 리허설 (운영 리스크 점검) (2026-08-10, AWS)

- **배경**: 8/31 최종 test는 2,000문항을 하루 안에 무사고로 처리해야 하는 이번 대회 최대 실행 리스크. 성능이 아니라 **운영**을 검증하기 위해 리더보드 831문항을 대역 삼아 확정 스택(가중 SC n=32, temp 0.7)으로 `make_submission.py`를 1회 완주하며 시간·GPU/CPU 메모리를 실측
- **설정**: `uv run python remote/make_submission.py --n 32 --weighted --tag rehearsal --lb-csv deep-learning-challenge-2026/deep_chal_math_leaderboard_filtered.csv` (`/usr/bin/time -v`로 감싸 CPU/시간 측정, 별도 프로세스로 `nvidia-smi`를 10초 간격 폴링해 GPU 메모리 시계열 기록)
- **발견 경위**: 세션 진입 시 이미 이전 세션이 15:49~16:50에 백그라운드로 완주해둔 결과(`make_submission_rehearsal.log`, `gpu_mem_rehearsal.csv`, `results/submission_rehearsal.csv`)가 커밋·기록 없이 남아 있는 상태를 발견. 로그 무결성(에러 0건, 831/831 완료, 답 결측 0건) 확인 후 재실행 없이 채택
- **결과**:

| 항목 | 값 |
|---|---|
| 처리 문항 | 831/831 |
| 총 소요 | 1:01:30 (3,690초) |
| 문항당 처리 시간 | **4.44초/문항** |
| 2,000문항 환산 | **약 2시간 28분** (8,881초) |
| GPU 메모리 최대 | 21,891 MiB / 23,028 MiB (**95.1%**) |
| CPU RSS 최대 | 5.07 GiB |
| CPU 사용률 | 37% (GPU 바운드 확인) |
| 오류 | 없음 |

- **핵심 발견 — 재개 불가능**: `make_submission.py`는 100문항 청크로 메모리만 관리할 뿐 **디스크 체크포인트가 없다**. 831문항 중 830번째에서 죽어도 결과 0건 — 재시작 시 전량(약 2.5시간, 2,000문항 기준) 재실행 필요. 반면 `dump_lb_samples.py`는 청크마다 `.jsonl`+`.progress`로 저장해 실제 재개가 동작함(exp33에서 검증됨)이 확인됨 — **8/31 당일은 `dump_lb_samples.py`(재개 가능) + `make_submission_from_dump.py`(GPU 불필요, 수 초) 조합으로 대체 권장**
- **의미**: 2.5시간이라는 소요 시간 자체는 하루 안에 충분히 여유 있으나(중단 1회 가정해도 최대 5시간), 재개 불가라는 운영 리스크가 이번 리허설의 가장 중요한 산출물. 성공/실패 판정 대상 아님(운영 점검)
- **결과 파일**: `results/rehearsal_report.md`(전체 체크리스트 포함), `results/submission_rehearsal.csv`, `gpu_mem_rehearsal.csv`, `make_submission_rehearsal.log`
- **다음**: exp31a-dump-samples (집계 전략 대량 탐색 사이클)

## 실험 34: 집계 전략 5종 제출 후보 생성 — 덤프 기반, GPU 불필요 (2026-08-10, AWS)

- **배경**: exp33 덤프(831문항×96샘플) 완료로 이후 제출 후보는 GPU 없이 수 초 만에 생성 가능. 리더보드 과적합을 피하려 구조적으로 다른 축만 생성 — 표본 최대화+가중(①), 가중 실효성 판별용 단순다수결(②), 기존 제출(n32w)과 동일 설정의 재현 대조군(③, 우리 실행 간 변동폭 측정용), 잘린 풀이 제외(④), 동률 시 확신도 우선(⑤)
- **설정**: `remote/make_submission_from_dump.py --dump results/lb_samples.jsonl` 5회 실행
  ① `--rule weighted --n 48 --tag w48`
  ② `--rule trim25 --n 32 --tag tr32`
  ③ `--rule weighted --n 32 --tag w32b` (n32w와 동일 설정 — 표본만 다른 재현 대조군)
  ④ `--rule drop_trunc --n 32 --tag dt32`
  ⑤ `--rule tiebreak_conf --n 32 --tag tb32`
- **결과**: 5개 CSV 모두 831행 / id·answer 2열 / answer 정수 / 결측·중복 없음 확인. 기존 제출(n32w) 대비 차이 문항 수:

| 태그 | 규칙 | n | n32w 대비 차이 | 판별 가능? (15문항 기준) |
|---|---|---|---|---|
| w48 | weighted | 48 | 63문항 | 가능 |
| tr32 | trim25 | 32 | 76문항 | 가능 |
| w32b | weighted(대조군) | 32 | 67문항 | 가능 |
| dt32 | drop_trunc | 32 | 70문항 | 가능 |
| tb32 | tiebreak_conf | 32 | 67문항 | 가능 |

- **의미**: 5개 후보 모두 LB 표준오차(±1.4%p) 이상으로 판별 가능한 구조적 차이를 가짐. w32b는 n32w와 완전히 동일한 방법(가중 SC n=32)이지만 다른 표본 실행이므로, 이 둘의 LB 점수 차이가 그대로 "같은 설정의 실행 간 변동폭"이 됨 — exp29의 중복 제출 사고 교훈을 반영해 제출 전 이력 확인 필수
- **제출 여부**: 큐 명세대로 서버는 생성·기록까지만 수행, **제출 판단은 로컬 Claude(계획자) 담당**
- **결과 파일**: `results/submission_w48.csv`, `results/submission_tr32.csv`, `results/submission_w32b.csv`, `results/submission_dt32.csv`, `results/submission_tb32.csv` (5개 모두 커밋 대상)
- **다음**: exp32-budget-forcing

## 실험 33: 리더보드 표본 대량 덤프 (2026-08-10, AWS)

- **배경**: exp29까지 제출 후보 1개당 GPU 1~2시간이 병목이었음. 표본을 한 번 만들어 저장해두면 이후 어떤 n·어떤 집계 규칙의 제출 파일도 GPU 없이 수 초 만에 생성 가능해짐 — 하루 최대 건수 제출 체제의 기반
- **설정**: `nohup uv run python remote/dump_lb_samples.py > dump_lb.log 2>&1 &` — 베이스 모델(어댑터 없음), 리더보드 831문항 전체, temperature=0.7, top_p=0.8, max_tokens=2048, 청크(100문항)마다 저장·재개 지원(exp29 OOM 사고 이후 도입된 로직)
- **큐 명세와의 차이**: 큐(exp33)는 최신 커밋에서 `--n 48`로 축소 지정돼 있었으나, **세션 진입 시 이미 이전 세션이 (큐가 --n 48로 수정되기 전 시점에) `--n 96`(스크립트 기본값)으로 시작해둔 프로세스가 12:32부터 실행 중**이었고, 이번 세션 진입 직후인 15:30에 정상 완료된 상태로 발견됨. n=96 덤프는 n=48이 필요로 하는 모든 것을 포함하는 상위집합(`make_submission_from_dump.py --n <원하는 수>`로 앞에서부터 잘라 쓰는 구조)이라 **재실행하지 않고 이 결과를 그대로 채택** — 재실행 시 GPU 약 3시간 낭비만 발생하고 결과물은 오히려 더 제한적이었을 것
- **검증**: 831/831문항 완료(로그 "진행 831/831문항 저장됨"). 파일 무결성 직접 확인 — 831줄, 리더보드 CSV의 id 집합과 완전 일치, 전 줄 samples 배열 길이 정확히 96(부족/과다 없음). `results/lb_samples.jsonl` 5.5MB, `.progress` 파일과 함께 서버에만 보관(용량 사유로 커밋 대상 아님, 큐 명세대로)
- **소요 시간**: 약 3시간(12:32 모델 로드 시작 → 15:30 완료) — n=96 기준, n=48이었다면 더 짧았겠으나 이미 완료된 작업을 되돌릴 이유 없음
- **의미**: exp34(제출 배치) 착수 준비 완료 — `remote/make_submission_from_dump.py --rule <규칙> --n <48 이하 원하는 값> --tag <태그>`로 GPU 없이 수 초 내 제출 파일 생성 가능
- **다음**: exp34-submission-batch (집계 규칙 5종 후보 생성 + 기존 제출과의 차이 문항 수 기록)

## 실험 29: 제출 후보 2종 — n=64 가중 vs n=32 단순 다수결 (LB 판별, H11 최종 결론 강화) — 목표 미달, 둘 다 기존 최고 대비 하락 (2026-08-10, AWS)

- **배경**: exp27·28(온도 축)이 모두 미달이라 온도는 0.7 고정. 로컬 노이즈로는 못 가리는 두 축(표본 수 32→64, 가중 유무)을 이미 제출된 n32-가중(LB 0.78459)과 비교해 **깨끗한 리더보드 831문항**으로 최종 판별
- **설정**: ① `remote/make_submission.py --n 64 --weighted --tag n64w` (표본 수 축: 32→64가 깨끗한 데이터에서도 무의미한지) ② `remote/make_submission.py --n 32 --tag n32m` (가중 없이 단순 다수결 — 가중이 실제로 도움인지)
- **검증**: 두 CSV 모두 831행, `answer` 정수형, 결측·중복 id 없음 확인(스크립트 자체 출력 "빈 답 없음 확인: True"로도 재확인). 기존 제출 n32w 대비 답이 다른 문항 수: n64w **69개**, n32m **67개** — 둘 다 "구조적으로 다른 후보"(15문항 이상) 기준 충족, LB 판별력(±1.4%p) 이상의 유효한 비교 대상
- **결과 (리더보드 831문항 실측)**:

  | 후보 | 설정 | LB 점수 | 기존(n32w 0.78459) 대비 |
  |---|---|---|---|
  | n64w | n=64, 가중 | 0.77978 | −0.00481 (약 4문항) |
  | n32m | n=32, 단순 다수결(무가중) | 0.77737 | −0.00722 (약 6문항) |

- **판정**: 둘 다 기존 최고 스택(n=32 가중)보다 하락 — **성공 기준 미충족**. (a) n=32→64는 깨끗한 LB에서도 이득 없음(오히려 하락) — 로컬에서 확인된 SC 표본 스케일링 정체(H11)가 노이즈가 아니라 실제 상한이었음을 LB가 재확인. (b) 가중 제거(n32m)가 가중 유지(n32w)보다 낮아 **확신도 가중 투표(H16)가 LB에서도 실질적으로 도움**임을 재확인(-0.00722, 로컬 exp10의 +0.8%p 방향과 일치)
- **의미**: 최종 스택은 여전히 exp25/26(v1 검증자 없이, 확신도 가중 SC n=32, 로컬 75.8%/LB 0.78459)이 최선 — 표본 수·가중 두 축 모두 LB로 재확인 완료. 로컬 판정과 LB 판정이 일치해 향후 유사 튜닝(표본 수↑, 가중 제거)은 추가 검증 없이도 기각 가능
- **결과 파일**: `results/submission_n64w.csv`, `results/submission_n32m.csv` (둘 다 커밋)
- **운영 사고 기록**:
  1. n=64 생성 1차 시도(`make_sub_n64w.log`, 04:32 시작)가 결과 저장 없이 종료(OOM 추정 — 이후 `make_submission.py`에 청크 처리 로직 추가돼 이번 세션 진입 전 커밋 38220bf로 이미 수정됨). 재시도(`make_sub_n64w_v2.log`, 09:18~11:18)로 정상 완료
  2. **중복 제출 실수**: 세션 진입 시 이미 이전 세션이 11:24에 n64w를 제출(55404933, LB 0.77978)해둔 상태였는데, 제출 이력을 먼저 확인하지 않고 12:27에 동일 파일을 재제출(55406328, 동일 점수)함 — 일일 제출 한도 1건 낭비. **교훈**: 제출 전 반드시 `kaggle competitions submissions`로 기존 제출 이력 확인
  3. **일일 제출 한도 정정**: CONTEXT.md에 "일일 한도 없음 확인(2026-08-10)"으로 기록돼 있었으나, 실제로는 Kaggle CLI가 제출마다 "N submissions remaining today"를 출력함(이번 세션 3건 남음→2건 남음 확인) — **한도가 존재함**. CONTEXT.md 기록 정정 필요
- **다음**: exp33-lb-dump (리더보드 표본 대량 덤프 — 하루 최대 제출 체제의 기반)

## 실험 28: 온도 혼합 앙상블 — 목표 미달, 온도 차원 탐색 사이클 종료 (2026-08-09, AWS)

- **배경**: exp27(단일 온도 축 탐색)이 t=0.7 국소 최적을 확인했지만, "서로 다른 온도의 표본을 한 표결에 섞으면 표본 분포 자체가 달라져 SC 정체를 돌파할 수 있다"는 마지막 미검증 가설이 남아 있었음
- **설정**: `remote/eval_temp_mixture.py --spec 0.4:8,0.7:8,1.0:8` (베이스 모델, 검증 483문항, top_p=0.8 seed=42, temp 0.4/0.7/1.0 각 8샘플=총 24표 합동 다수결/가중투표). 비교 기준: 단일온도 n=32 가중 75.8%(exp25). 성공 기준: 혼합 가중 ≥76.3%
- **결과**:

  | 구성 | 정확도 |
  |---|---|
  | t=0.4 단독 (n8) | 74.1%(358/483) |
  | t=0.7 단독 (n8) | 73.3%(354/483) |
  | t=1.0 단독 (n8) | 72.9%(352/483) |
  | 혼합 24표 다수결 | **74.9%**(362/483) |
  | 혼합 24표 가중 | **74.9%**(362/483, 다수결과 동률) |

- **판정**: 혼합 24표(74.9%)가 단일온도 n=32 가중(75.8%, exp25) 대비 −0.9%p로 오히려 낮고, 성공 기준(76.3%+)에도 크게 못 미침. 온도를 섞어도 표본 24개 자체가 n=32보다 적어 표본 수 효과에 못 미쳤을 가능성과, 서로 다른 온도의 표를 합쳐도 "다른 종류의 정보"가 아니라 그냥 잡음이 늘어난 것으로 추정(가중투표가 다수결과 완전히 동률인 점이 확신도 신호가 약함을 시사)
- **의미**: 온도 차원(단일 축 exp27 + 혼합 exp28) 전체가 목표 미달로 종료 — SC 다수결 계열은 표본 수·온도·온도 혼합 세 가지 하이퍼파라미터 조정 전부 소진, 추가 이득 없음이 확정적
- **결과 파일**: `results/eval_temp_mixture.json`, `eval28_temp_mixture.log`
- **사고 기록**: 세션 진입 시 이미 이전 세션이 백그라운드로 실행·완료해둔 상태(04:55~05:24, GPU 유휴 확인)를 발견 — 재실행 없이 로그와 결과 파일 검증 후 기록만 수행
- **다음**: exp29-submission-candidates (exp27·28 중 로컬 상위 2개 설정으로 제출 파일 생성, 리더보드로 최종 판별)

## 실험 27: 온도 탐색 (H11 온도 축, 마지막 미검증 차원) — 목표 미달 (2026-08-08→09, AWS)

- **배경**: 지금까지 SC 샘플링은 temperature=0.7 고정으로만 진행(exp05~26 전부). H11(SC 스케일링) 가설에는 "온도 탐색"도 포함돼 있었으나 표본 수 축(n=4~64)만 검증되고 온도 축은 미검증 상태로 남아 있었음 — 저비용 미검증 카드로 등록
- **설정**: `remote/eval_vllm.py --mode sc --n 16 --temp 0.4 --tag t04_n16`, 이어서 `--temp 1.0 --tag t10_n16` (베이스 모델, 검증 483문항, top_p=0.8 seed=42 고정, n=16으로 exp07의 t=0.7 n16=75.6%와 직접 비교)
- **결과**:

  | temp | 다수결 | 가중투표 | 잘림 비율 |
  |---|---|---|---|
  | 0.4 | 74.1%(358/483) | 74.1%(358/483) | 2.2%(168/7728) |
  | 0.7 (exp07) | 75.6%(365/483) | - | - |
  | 1.0 | 74.5%(360/483) | 75.4%(364/483) | 1.5%(116/7728) |

- **판정**: 성공 기준 없이 탐색 목적이었으나, 기준(t=0.7 n16=75.6%) 대비 t=0.4(-1.5%p)·t=1.0 가중(-0.2%p) 모두 낮음 — **온도를 낮추거나(0.4) 높여도(1.0) 이득 없음, 기존 t=0.7이 국소 최적**. t=0.4는 다양성이 줄어 SC 효과가 감소(가중투표도 다수결과 동률로 확신도 신호가 약함), t=1.0은 다양성은 늘지만 노이즈도 늘어 가중투표로 일부 상쇄(+0.9%p)해도 기준 못 미침. 잘림 비율은 t=0.4 2.2%, t=1.0 1.5%로 **둘 다 5% 미만** — max_tokens(2048) 상향은 불필요
- **의미**: H11(SC 샘플 수 + 온도) 두 축 모두 소진 — 표본 확대(exp17)도 온도 조정(exp27)도 75.6~75.8% 부근 상한을 못 넘김. SC 다수결 계열의 하이퍼파라미터 튜닝으로는 추가 이득이 없음이 확정적. 다음은 exp28(온도 혼합 앙상블 — 여러 온도를 섞어 다양성을 인위적으로 늘리는 방향)로 마지막 시도
- **결과 파일**: `results/eval_t04_n16.json`, `results/eval_t10_n16.json`, `eval27_t04_n16.log`, `eval27_t10_n16.log`
- **사고 기록**: 세션 진입 시 이미 이전 세션이 백그라운드로 두 온도 모두 실행·완료해둔 상태(04:09~04:51, GPU 유휴·관련 프로세스 없음 확인)를 발견 — 재실행 없이 로그와 결과 파일(잘림 비율 포함) 검증 후 기록만 수행
- **다음**: exp28-temp-mixture (온도 혼합 앙상블, 큐 다음 항목)

## 실험 26: 최종 제출 파일 생성 (SC n=32 가중, 확정 최고 스택) (2026-08-07, AWS)

- **배경**: exp17·exp25 두 방법(균일 SC와 검증자 Best-of-N)이 독립적으로 일치한 로컬 최고 수치 — 확신도 가중 SC n=32(75.8%)를 최종 제출 스택으로 확정, 리더보드 831문항에 적용
- **설정**: `uv run python remote/make_submission.py --n 32 --weighted --tag n32w` (베이스 모델, 어댑터 없음, 확신도 가중 다수결). 리더보드 대상: `deep-learning-challenge-2026/deep_chal_math_leaderboard_filtered.csv`(831문항)
- **결과**: `results/submission_n32w.csv` 생성 완료 — 831행, id/answer 컬럼, 정수 답만, 결측·중복 id 없음(검증 완료). 로그 마지막 줄: "저장: results/submission_n32w.csv (831행). 빈 답 없음 확인: True"
- **소요**: 14:27 모델 로드 시작 → 15:28 완료, 약 61분 (예상 2.5시간보다 빠름)
- **다음**: 사용자가 Kaggle/대회 플랫폼에 제출 후 실측 리더보드 점수 확인 (기대 0.775~0.785). 큐 재소진 — 로컬 Claude(계획자)가 다음 항목 등록 필요
- **사고 기록**: 세션 진입 시 이미 이전 세션이 백그라운드로 실행·완료해둔 결과(로그 `make_submission_n32w.log`, GPU 유휴·관련 프로세스 없음 확인)를 발견 — 재실행 없이 로그와 CSV 파일 검증(행 수·컬럼·정수 여부·중복 id) 후 기록만 수행

## 실험 25: v1 검증자 × n=32 잔여 조합 — 목표 미달, 큐 소진 (2026-08-07, AWS)

- **배경**: H9(v1 검증자, exp18c)에서 유일하게 방향이 맞았던 신호(verifier_weighted 75.4%, 다수결 74.9% 대비 +0.5%p)를 표본 수를 n=8→32로 늘리면 pass@n 상승과 맞물려 성공 기준을 넘을 수 있는지 확인 — 큐의 마지막 잔여 조합 카드
- **설정**: `uv run python remote/eval_bestofn.py --verifier outputs/verifier/verifier_final --n 32` (검증 483문항, 베이스 모델로 n=32 풀이 생성 후 v1 검증자 어댑터로 채점, majority/verifier_weighted/best_of_1 3전략)
- **결과**: **majority 75.4%(364/483), verifier_weighted 75.8%(366/483), best_of_1 72.3%(349/483)**. exp18c(n8: 74.9/75.4/72.5) 대비 verifier_weighted +0.4%p, best_of_1은 -0.2%p로 거의 그대로. **성공 기준(verifier_weighted≥76.3%) 미달**
- **의미**: 표본을 4배 늘려도 verifier_weighted가 +0.4%p만 오름 — SC 자체가 n16 이후 정체(exp17)하는 것과 같은 패턴으로, 검증자 신호도 n 확대만으로는 목표치까지 못 미침. H9는 v1(n8, n32)·v2 세 변형 모두 성공 기준 미달로 최종 기각 확정
- **결과 파일**: `results/eval_bestofn.json`, `eval25_v1_n32.log`
- **다음**: 큐의 마지막 항목이 완료됨 — 로컬 Claude(계획자)가 다음 근본 가설(H15 DPO 등)을 문서화해 큐에 등록 필요
- **사고 기록**: 세션 진입 시 이미 이전 세션이 백그라운드로 실행·완료해둔 결과(13:22 모델 로드 시작 → 14:14 완료, GPU 유휴·관련 프로세스 없음 확인)를 발견 — 로그(`eval25_v1_n32.log`) 마지막 줄(`검증 483문항, n=32`, majority/verifier_weighted/best_of_1 수치)과 `results/eval_bestofn.json` 수치가 정확히 일치함을 확인한 뒤 재실행 없이 기록만 수행

## 실험 24: GRPO 스케일업 (최종 대형 스윙 카드②) — 목표 미달 (2026-08-07, AWS)

- **배경**: exp12(GRPO 파일럿, 3000문제/400스텝)가 베이스와 사실상 동일(중립, 하락 아님)했던 것이 "덜 돌려서"였는지 "GRPO 자체가 이 태스크에 한계가 있는 것"인지 판정 — 최종 대형 스윙 카드②
- **설정**: `remote/train_grpo.py` CONFIG 수정 — `n_problems=6000`(파일럿의 2배), `max_steps=1500`(파일럿의 3.75배), `output_dir=outputs/grpo_scale`, 나머지(lr5e-6, num_generations=4)는 파일럿과 동일. 학습 후 `remote/eval_vllm.py --mode both --adapter outputs/grpo_scale/grpo_scale_final`로 검증 483문항 평가
- **학습 결과**: 1500스텝 완료(epoch 0.5, 6000문제의 절반만 소진), train_runtime 27,680초(약 7.7시간), train_loss **0.0177**([wandb c637q5at](https://wandb.ai/loonaticvibe2-11-jin-jason/huggingface/runs/c637q5at)) — 파일럿(0.02028)과 비슷한 수준으로 낮음. reward_correct/mean이 스텝 전반에 걸쳐 0.4~0.65 사이를 오가며 뚜렷한 상승 추세 없음(400스텝 이후에도 개선 신호 부재) — "파일럿이 덜 돌린 것"이라는 가설과 배치되는 관찰
- **평가 결과**: greedy **69.8%**(337/483, 베이스 69.4% 대비 +0.4%p), SC n8 **73.1%**(353/483, 베이스 74.7% 대비 **-1.6%p**), 가중투표 74.1%(358/483, -0.6%p) — 성공 기준(SC n8≥75.7%) 미달. exp12 파일럿(SC 74.5%)보다도 **낮아** 스텝을 늘릴수록 오히려 SC 다양성이 줄어드는 방향(자기증류와 유사한 패턴)으로 작용한 것으로 추정
- **의미**: H17(GRPO) 최종 기각 확정 — 파일럿의 중립적 결과는 "학습 부족"이 아니라 "GRPO가 이 3B 모델·태스크 조합에서 SC 다수결에 필요한 다양성을 깎아먹는 방향으로 수렴"하는 것에 가까움. 학습 트랙(트랙 A)은 QLoRA SFT·GRPO 파일럿·GRPO 스케일업까지 모든 파라다임이 베이스 대비 중립 이하로 마무리
- **결과 파일**: `results/eval_outputs_grpo_scale_grpo_scale_final.json`, 로그 `train_grpo_scale.log`(학습), `eval24_grpo_scale.log`(평가)
- **사고 기록**: 세션 진입 시 이미 이전 세션이 백그라운드로 학습(05:16 시작 → 12:58 완료, 약 7.7시간)과 평가(13:06 시작 → 13:18 완료)를 모두 실행·완료해둔 상태를 발견 — GPU 유휴·관련 프로세스 없음 확인, 로그 마지막 줄과 결과 JSON 수치(greedy 0.6977/sc_n8 0.7308/weighted 0.7412)가 정확히 일치함을 확인한 뒤 재실행 없이 기록만 수행
- **다음**: exp25(잔여 조합 — v1 검증자 × n=32, 큐의 마지막 항목) — 완료, 결과는 "실험 25" 절 참고

## 실험 23a: 검증자 v2 데이터 생성 (H9 재도전) (2026-08-07, AWS)

- **배경**: H9(exp18a-c)는 즉답 Yes/No 검증자로 verifier_weighted 75.4%(성공 기준 76.5%+ 미달)에 그쳤다. 최종 대형 스윙 카드①은 v1 실패 원인 두 가지를 겨냥한 재도전: ① v1 학습 데이터의 음성 표본이 무작위 오답 위주라 "다수결을 속이는 어려운 오답"을 충분히 못 배웠을 가능성 ② 즉답 Yes/No보다 "재검산 근거 생성 후 판정"이 더 신뢰도 높은 판정을 낼 가능성(판정 근거 자체를 rejection sampling)
- **설정**: `remote/gen_verifier_v2_data.py` — exp18a의 `data/verifier.jsonl`(24,000쌍)에서 문제별 4표본의 다수답을 계산해 난이도 태그 부여: 어려운음성=오답인데 다수답이 된 풀이(다수결을 속인 것), 어려운양성=정답인데 다수답이 아니었던 풀이(표결에서 밀린 것), 쉬운양성/쉬운음성=그 반대. 어려운음성·어려운양성은 전량 포함, 쉬운양성 4,000/쉬운음성 2,000으로 다운샘플. 선별된 각 쌍에 대해 베이스 모델(Qwen2.5-3B-Instruct, vLLM)이 풀이를 재검산하는 근거+"Verdict: Yes/No" 판정을 2회 시도(temp=0.7, max_tokens=600) 생성, 판정이 실제 라벨과 일치하는 첫 근거만 채택
- **결과**: 선별 9,745쌍(어려운음성 3,239 + 어려운양성 506 + 쉬운양성 4,000 + 쉬운음성 2,000). 생성 결과 **채택 6,425개(65.9%) / 근거실패 3,320개(34.1%)** → `data/verifier_v2.jsonl`. 처리 시간은 예상(~2-3시간)보다 짧게 소요(약 20분, 03:59 시작 → 04:16 완료로 추정)
- **다음**: exp23b(검증자 v2 학습 — assistant 응답을 Yes/No 대신 근거+Verdict 전문으로, 클래스 균형은 label 기준 유지) → exp23c(Best-of-N v2 평가, 성공 기준 76.5%+) → exp23d(성공 시 제출)
- **결과 파일**: `data/verifier_v2.jsonl`(커밋 제외, 용량), `data/verifier_v2_progress.json`, `gen_verifier_v2.log`
- **사고 기록**: 세션 진입 시 이미 이전 세션(재부팅 후 러너가 재트리거)이 백그라운드로 실행·완료해둔 결과(03:59경 시작 → 04:16 완료, GPU 유휴 확인)를 발견 — 로그(`gen_verifier_v2.log`)의 진행률 로그(9,745/9,745, 채택 6,425, 실패 3,320)와 `data/verifier_v2.jsonl` 실제 라인 수(6,425줄)가 정확히 일치함을 확인한 뒤 재실행 없이 기록만 수행

## 실험 23b: 검증자 v2 어댑터 학습 (H9 재도전) (2026-08-07, AWS)

- **배경**: exp23a에서 생성한 재검산 근거+Verdict 데이터(data/verifier_v2.jsonl, 6,425쌍)로 `remote/train_verifier.py`를 학습. v1(exp18b)과 달리 assistant 응답이 즉답 Yes/No가 아니라 `r['judge']`(근거 몇 줄 + "Verdict: Yes/No") 전문 — 큐 명세대로 스크립트가 이미 이렇게 구성돼 있어 추가 수정 없이 그대로 실행
- **설정**: `nohup uv run python remote/train_verifier.py > train_verifier_v2.log 2>&1 &` — data='data/verifier_v2.jsonl', output_dir='outputs/verifier_v2', r16/alpha32/lr1e-4/ep1, 클래스 균형은 label 기준 1:1(max_per_class=12000, 실제로는 음성 2,198개가 제한 클래스라 양성도 2,198개로 다운샘플 → 총 4,396개 학습)
- **결과**: 275스텝(1 epoch, 배치4×누적4), train_loss 0.7129(step10) → 0.2266(마지막 step) → 평균 0.2523, train_runtime 1,629초(약 27분). [wandb yacah786](https://wandb.ai/loonaticvibe2-11-jin-jason/huggingface/runs/yacah786). 어댑터 저장: `outputs/verifier_v2/verifier_final`
- **다음**: exp23c(Best-of-N v2 평가 — 베이스 n=8 생성 후 이 어댑터로 채점, majority/verdict-vote/hybrid 3전략 비교, 성공 기준 최고 전략 ≥76.5%)
- **결과 파일**: `outputs/verifier_v2/verifier_final`(어댑터, 커밋 제외), `train_verifier_v2.log`
- **사고 기록**: 세션 진입 시 이미 이전 세션이 백그라운드로 학습을 실행·완료해둔 상태(로그 마지막 줄 `저장: outputs/verifier_v2/verifier_final`, GPU 유휴, 프로세스 종료 확인, 완료 시각 04:51~04:53경으로 이번 세션 진입 직전)를 발견 — 로그의 최종 손실·스텝수·어댑터 파일 존재를 확인한 뒤 재실행 없이 기록만 수행

## 실험 23c: 검증자 v2 Best-of-N 평가 (H9 재도전, 최종) — 목표 미달, H9 최종 기각 (2026-08-07, AWS)

- **배경**: exp23a·23b에서 만든 v2 검증자(재검산 근거+Verdict 생성)가 v1(즉답 Yes/No, exp18c verifier_weighted 75.4%)보다 신뢰도 높은 판정을 낼 수 있는지 확인 — H9 최종 판정
- **설정**: `remote/eval_bestofn_v2.py --verifier outputs/verifier_v2/verifier_final --n 8` (신규 작성, 커밋). 검증 483문항에서 베이스 모델로 n=8 풀이 생성(temp0.7 top_p0.8 seed42) → 각 풀이를 v2 어댑터로 채점(greedy, max_tokens 600, `Verdict: Yes/No` 파싱) → 3전략 비교: ①majority(일반 다수결) ②verdict_vote(답별 Yes표 최다) ③hybrid(득표수+Yes표 합산 최대)
- **결과**: **majority 73.7%(356/483), verdict_vote 73.3%(354/483), hybrid 73.7%(356/483)**. 성공 기준(최고 전략 ≥76.5%) 미달. 셋 다 v1의 verifier_weighted(75.4%)보다도 낮고, 심지어 이 표본의 순수 다수결(majority 73.7%)조차 exp18c 표본의 다수결(74.9%)보다 낮음 — vLLM 생성 비결정성(±1%p) 범위 내로 보이나, v2 검증자가 v1보다 개선됐다는 증거는 없음. 근거 생성 기반 판정이 즉답 Yes/No보다 낫다는 가설은 기각
- **판정**: H9 재도전 실패 — v1·v2 두 번 모두 성공 기준 미달로 **H9(검증자 Best-of-N) 최종 기각**. 최종 대형 스윙 카드①(검증자 v2) 종료
- **결과 파일**: `remote/eval_bestofn_v2.py`(커밋), `results/eval_bestofn_v2.json`, `eval23c_verifier_v2.log`
- **다음**: exp23d는 성공 조건 미충족으로 skip. 큐 다음은 exp24(GRPO 스케일업, 최종 대형 스윙 카드②, 성공 기준 SC n8≥75.7%)
- **사고 기록**: 세션 진입 시 이미 이전 세션이 백그라운드로 실행·완료해둔 결과(04:56 모델 로드 시작 → 05:11 완료, GPU 유휴·관련 프로세스 없음 확인)를 발견 — 로그(`eval23c_verifier_v2.log`) 마지막 줄과 `results/eval_bestofn_v2.json` 수치가 정확히 일치함을 확인한 뒤 재실행 없이 기록만 수행. runner.log상 이번 세션 진입 직후 exp23c가 재트리거된 흔적이 있으나(05:12 기준 이미 완료된 결과였음), 이전 세션 패턴(exp19·exp23a·exp23b)과 마찬가지로 중복 실행은 없었음

## 실험 17: SC 표본 수 확대 n=32/64 마무리 (H11) (2026-08-06, AWS)

- **배경**: exp07(n=4/8/16)의 후속 — n=32, n=64까지 SC 표본을 늘려 다수결 정확도가 계속 오르는지, 76.3%+(2등 추격 목표)에 도달하는지 확인
- **설정**: `remote/eval_vllm.py --mode sc --n 32`, 이어서 `--n 64` (베이스 모델, 검증 483문항, temp=0.7 top_p=0.8 seed=42) — 같은 표본에서 다수결/가중투표 동시 측정
- **결과**:

  | n | 다수결 | 가중투표 |
  |---|---|---|
  | 4 (exp07) | 72.7%(351/483) | - |
  | 8 (exp05) | 74.7%(361/483) | - |
  | 16 (exp07) | 75.6%(365/483) | - |
  | 32 | 75.4%(364/483) | 75.8%(366/483) |
  | 64 | 75.6%(365/483) | 75.4%(364/483) |

- **판정**: 성공 기준(n32 또는 n64 다수결/가중 ≥ 76.3%) **미달** — 최고치는 n32 가중투표 75.8%로 기준에 -0.5%p 못 미침. n=16 이후로는 표본을 4배(16→64)까지 늘려도 정확도가 74.7~75.8% 사이에서 **정체·비단조**(n32 다수결 75.4% < n16 75.6%) — SC 스케일링만으로는 추가 이득이 없음을 확정. exp11(n16 가중투표가 다수결보다 낮음)에 이어 이번에도 가중투표 우위가 n에 따라 뒤집혀(n32는 가중>다수결, n64는 반대) **가중투표 효과 자체가 불안정**함이 재확인됨
- **참고(운영 메모)**: 이 실험은 세션 중단이 반복되며 n=32가 이전 세션들에서 여러 차례 재실행됐다(결과가 두 값 사이를 오갔음: 75.4%/75.8% ↔ 76.0%/76.0%). 이번 세션은 재실행하지 않고 가장 마지막에 완료된 클린 실행 결과(`eval17_n32.log`, `results/eval_base_sc_n32.json`과 일치하는 75.4%/75.8%)를 채택해 기록. n64는 1회만 실행됨(`eval17_n64.log`)
- **결과 파일**: `results/eval_base_sc_n32.json`, `results/eval_base_sc_n64.json`(이번 세션에 eval_base.json·로그로부터 재구성), `eval17_n32.log`, `eval17_n64.log`
- **다음**: exp17b(제출 파일 생성)는 성공 기준 미달로 skip. SC 스케일링·프롬프트 앙상블·자기수정·가중투표·QLoRA·GRPO 모두 시도된 상태 — 남은 유효 후보는 H9(검증자 Best-of-N, 큐 exp18a~d, 운영진 허용 확정됨)와 H13b(검증셋 라벨 재검수)뿐. 로컬 Claude가 exp18 착수 여부 및 우선순위를 판단해야 함

## 실험 18a: 검증자 학습 데이터 생성 (H9) (2026-08-06, AWS)

- **배경**: 트랙 A·B 모두 목표 미달로 종료된 뒤 남은 유효 가설 H9(검증자 Best-of-N) 착수. pass@6 79.9% vs SC 74.7% 간극(exp04b/exp05)이 검증자가 노릴 여지를 시사
- **설정**: `remote/gen_verifier_data.py` — 베이스 모델(Qwen2.5-3B-Instruct, vLLM)로 학습·검증 제외 문제 중 6,000문제를 문제당 4개씩 샘플링(temperature=0.8, top_p=0.95, max_tokens=2048, seed=7). 각 (문제, 풀이) 쌍을 최종 답이 정답과 일치하는지로 자동 라벨링(label=True/False). 검증 세트(seed=123, 500문제)와 오류 문항(train_filtered_ids.csv)은 후보 풀에서 사전 제외
- **결과**: 6,000/6,000문제 처리 완료, 총 24,000개 (문제,풀이,라벨) 쌍 → `data/verifier.jsonl`. **양성(정답) 16,240개 (67.7%) / 음성(오답) 7,760개 (32.3%)**
- **의미**: 양성:음성 비율이 약 2.1:1로 불균형 — exp18b 학습 시 큐 명세대로 클래스 균형 1:1 샘플링 필요(음성 7,760개가 병목이 되어 최대 약 15,520개 균형 데이터 확보 가능)
- **결과 파일**: `data/verifier.jsonl`(커밋 대상 아님, gitignore), `data/verifier_progress.json`(중단-재개용)
- **다음**: exp18b-verifier-train (검증자 어댑터 학습) → exp18c(Best-of-N 평가, 성공 기준 verifier_weighted≥76.5%)

## 실험 18b: 검증자 어댑터 학습 (H9) (2026-08-07, AWS)

- **배경**: exp18a에서 생성한 24,000개 (문제,풀이,라벨) 데이터로 "풀이가 맞는지 Yes/No로 답하는" 검증자 LoRA를 학습. 추론 시 생성은 베이스(무어댑터)로, 채점만 이 어댑터로 수행할 예정(exp18c)
- **설정**: `remote/train_verifier.py` — Qwen2.5-3B-Instruct, r16/alpha32/lr1e-4/ep1, per_device_batch=4×grad_accum=4(유효배치16), max_seq_len=2048, seed=42. 큐 명세대로 클래스 균형 1:1 샘플링(양성 7,760개가 상한이라 양성 7,760+음성 7,760=15,520개, 970 스텝)
- **결과**: 정상 완료(약 81분 소요, 970/970 스텝). train_loss 시작 0.7613 → 종료 0.2116, 전체 평균 **train_loss 0.2288**. [wandb ufarew6n](https://wandb.ai/loonaticvibe2-11-jin-jason/huggingface/runs/ufarew6n). 어댑터 저장: `outputs/verifier/verifier_final`
- **결과 파일**: `train_verifier.log`, `outputs/verifier/verifier_final/`(어댑터, 커밋 대상 아님 — 용량)
- **다음**: exp18c-bestofn-eval — `remote/eval_bestofn.py --verifier outputs/verifier/verifier_final --n 8`로 majority/verifier_weighted/best_of_1 측정, 성공 기준 verifier_weighted≥76.5%

## 실험 18c: 검증자 Best-of-N 평가 (H9) — 목표 미달, H9 종료 (2026-08-07, AWS)

- **배경**: exp18b에서 학습한 검증자 어댑터로 SC 표본을 채점해 다수결보다 나은 선별이 가능한지 확인. H9의 최종 검증 단계
- **설정**: `remote/eval_bestofn.py --verifier outputs/verifier/verifier_final --n 8` — 베이스(무어댑터)로 검증 483문항에 n=8 풀이 생성(temp=0.7, top_p=0.8, seed=42) → 각 풀이를 검증자 어댑터로 채점(첫 토큰 "Yes" 확률, temp=0). 세 가지 선택 전략 비교: majority(일반 다수결), verifier_weighted(답별 P(Yes) 합산 최대), best_of_1(검증자 최고점 단일 풀이의 답)
- **결과**: majority 74.9%(362/483), **verifier_weighted 75.4%(364/483)**, best_of_1 72.5%(350/483)
- **판정**: 성공 기준(verifier_weighted ≥ 76.5%) **미달** (-1.1%p). 같은 표본 내에서 verifier_weighted가 majority보다 +0.5%p 높아 방향은 맞지만(검증자가 약하게나마 유효 신호를 가짐), 다수결 대비 상승폭이 목표(+1.8%p)에 크게 못 미침. best_of_1(검증자 최고점 풀이 1개만 채택)은 오히려 majority보다 -2.4%p 낮아, 검증자 단독 판단은 다수결의 "여러 풀이 합의" 효과를 대체하지 못함 — 검증자가 완벽하지 않을 때는 다수결과의 결합(가중치)이 단독 최고점 선택보다 안전함을 시사
- **의미**: H9(검증자 Best-of-N) 기각. 트랙 A(학습)·트랙 B(추론 재설계)에 이어 H9까지 목표 미달로 종료되며, 시도 가능한 안전한 가설 풀이 사실상 소진됨(SC 스케일링·프롬프트 앙상블·자기수정·가중투표·QLoRA SFT·GRPO·검증자 모두 시도 완료). 남은 후보는 H13b(검증셋 라벨 재검수, 미등록)와 H15(DPO, 미시도) 정도
- **결과 파일**: `results/eval_bestofn.json`, `eval18c_bestofn.log`
- **다음**: exp18d(제출 파일 생성)는 성공 기준 미달로 skip. 로컬 Claude(계획자)가 다음 근본 가설(H15 DPO 또는 H13b 검증셋 재검수)을 큐에 등록 필요 — CONTEXT.md 정체 정책상 안전한 반복만 지속하지 말 것

## 실험 19: 정책 앙상블 (H8) — 목표 미달 (2026-08-07, AWS)

- **배경**: 총력전 사이클 카드 ①. H9(검증자)까지 종료된 뒤 남은 저비용 미검증 가설 중 하나 — 서로 다른 정책(베이스/QLoRA/GRPO)이 생성한 풀이를 합쳐 다수결하면, 단일 정책 SC를 표본만 늘린 것(exp17 균일 n16)보다 다양성 자체의 효과로 더 오를 것이라는 가설(운영진 허용 확정)
- **설정**: `remote/eval_policy_ensemble.py` — 베이스 모델(Qwen2.5-3B-Instruct)로 n=8 생성 + QLoRA 어댑터(`outputs/qlora/qlora_r16_lr0.0002_ep2_final`, exp06)로 n=8 생성 → 16표 합동 다수결. 같은 방식으로 GRPO 어댑터(`outputs/grpo_pilot/grpo_pilot_final`, exp12)와도 측정. temp=0.7, top_p=0.8, 검증 483문항
- **결과**: base8+qlora8 앙상블 **74.9%(362/483)**, base8+grpo8 앙상블 **74.7%(361/483)**
- **판정**: 성공 기준(≥76.6%) **미달**. 비교 기준인 균일 n16 다수결(exp07/17, 75.6%)보다도 두 조합 모두 낮음(-0.7%p, -0.9%p) — 정책 다양성이 표본 다양성보다 나은 신호를 준다는 가설과 반대 방향. QLoRA·GRPO 어댑터 모두 베이스와 성능이 비슷하거나 낮은 상태(exp06/exp12)라 앙상블에 섞여도 오답 쪽 표가 늘어나는 효과가 더 큰 것으로 추정 — 애초에 어댑터 자체가 베이스보다 나은 정책이 아니었으므로(트랙 A 동결 사유) 다양성 이득이 발휘될 전제가 없었음
- **의미**: H8 기각. 총력전 사이클 4장 중 1장 소진, 나머지 exp20(퓨샷)·exp21(문제 정화)·exp22(적응형 예산) 진행
- **결과 파일**: `results/eval_policy_ensemble.json`, `eval19_policy_ensemble.log`
- **다음**: exp20-fewshot

## 실험 20: 4-shot 프롬프트 (총력전 카드②) — 목표 미달 (2026-08-07, AWS)

- **배경**: 총력전 사이클 카드 ②. 문제 유형별 예시 풀이를 프롬프트에 붙이면(in-context 학습) 모델이 풀이 형식·접근을 모방해 정확도가 오를 것이라는 가설. 학습(파라미터 갱신) 없이 프롬프트만으로 시도하는 저비용 카드
- **설정**: `remote/eval_fewshot.py` — `data/sft_short.jsonl`(exp06c에서 만든 문제당 최단 정답 풀이 1개 모음, train 세트, 검증셋과 무관)에서 유형이 다른 짧은 풀이 4개를 선정(기하 train-005807, 대수 train-005399, 정수론 train-002653, 응용 train-013800). 모든 문제 앞에 예시 대화(user 문제→assistant 풀이) 4쌍을 고정으로 붙여 greedy와 SC n=8(temp=0.7, top_p=0.8, seed=42) 평가. 검증 483문항(exp05와 동일 세트, seed=123)
- **결과**: fewshot greedy **70.2%(339/483)**, fewshot SC n8 **73.1%(353/483)**
- **판정**: 성공 기준(SC n8≥75.7%) **미달**. zero-shot 기준(exp05: greedy 69.4%/SC 74.7%)과 비교하면 greedy는 +0.8%p로 노이즈 범위 내 소폭 상승이나, **SC n8은 -1.6%p로 오히려 하락** — 노이즈 범위(±1%p)를 벗어난 명확한 악화
- **의미**: 4-shot 예시가 오히려 SC 다수결에 역효과. 예시 풀이 형식에 모든 샘플이 수렴하면서 temp=0.7 샘플링에도 불구하고 풀이 경로의 다양성이 줄어든 것으로 추정(exp06 QLoRA 실패의 "자기증류로 다양성 감소" 가설과 유사한 메커니즘) — SC는 다양한 풀이 경로의 합의에서 이득을 얻는데, few-shot이 그 다양성을 프롬프트 레벨에서 억누른 것으로 해석. 총력전 사이클 4장 중 2장 소진(H8·퓨샷 모두 기각), 나머지 exp21(문제 정화)·exp22(적응형 예산) 진행
- **결과 파일**: `results/eval_fewshot.json`, `eval20_fewshot.log`
- **사고 기록**: 실행 자체는 이전 서버 세션에서 이미 완료돼 있었음(02:16 모델 로드 → 02:30 완료, 스크립트 `remote/eval_fewshot.py` 커밋 b15d974까지 돼 있었으나 결과 파일은 미커밋 상태로 남아 있었음). 이번 세션은 로그(`eval20_fewshot.log`)와 결과 파일(`results/eval_fewshot.json`)로 완료를 확인한 뒤 재실행 없이 기록만 수행
- **다음**: exp21-question-clean

## 실험 21: 문제 텍스트 정화 (총력전 카드③) — 목표 미달, 노이즈 범위 (2026-08-07, AWS)

- **배경**: 총력전 사이클 카드 ③. 크롤링 과정에서 문제 본문에 번역 파이프라인 잔재 지시문("Translate the above text...")·마크다운 이미지 링크·Asymptote 도형 코드·문두 번호 매김 같은 잡음이 섞여 들어갔을 가능성 — 이런 잡음이 낀 문항에서 모델이 불필요한 토큰에 이끌려 오답을 낼 것이라는 가설. 순수 전처리라 학습 불필요, 저비용 카드
- **설정**: `remote/clean_question.py` — 정규식 4종으로 잡음 패턴 제거(번역지시문·이미지링크 `![](...)`·`[asy]...[/asy]`·문두 `N. ` 번호). `remote/eval_question_clean.py` — 검증 483문항에 대해 원문/정화 두 버전으로 greedy(temp=0) 평가, 전체 정확도와 "정화로 실제 바뀐 문항(영향받은 문항)" 부분집합 정확도를 각각 비교
- **결과**: 정화로 영향받은 문항 **25개**(483 중 5.2%). 전체 483문항 — 원문 greedy **70.0%**(338/483) / 정화 greedy **69.4%**(335/483, -0.6%p). 영향받은 25문항만 — 원문 greedy **52.0%**(13/25) / 정화 greedy **56.0%**(14/25, +4.0%p이나 실제로는 1문항 차이)
- **판정**: 성공 기준("영향받은 문항 부분집합에서 뚜렷한 개선") **미달**. 영향받은 부분집합의 +4.0%p는 표본이 25문항뿐이라 문항 1개가 뒤집힌 결과에 불과 — vLLM 생성 자체의 비결정성(±1문항 수준 흔들림, exp11·17에서 반복 관측)과 구분되지 않는 노이즈. 전체 483문항 기준으로는 오히려 -0.6%p로 소폭 하락. "뚜렷한 개선"이라 부를 근거가 없음
- **의미**: 잡음 패턴 자체가 영향받는 문항 수(5.2%)가 적어 설령 진짜 효과가 있어도 전체 정확도에 미치는 영향이 구조적으로 작다. 25문항이라는 표본 크기로는 뚜렷한 신호와 노이즈를 구분할 수 없음 — 정화를 제출 파이프라인(`make_submission.py`)에 이식할 근거 없음, 카드③ 기각. 총력전 사이클 4장 중 3장 소진(H8·퓨샷·문제정화 모두 기각), 남은 카드는 exp22(적응형 예산)뿐
- **결과 파일**: `results/eval_question_clean.json`, `eval21.log`
- **사고 기록**: 실행 자체는 이전 서버 세션에서 이미 완료돼 있었음(스크립트 `remote/clean_question.py`·`remote/eval_question_clean.py` 및 결과 `results/eval_question_clean.json`이 미커밋 상태로 존재, `eval21.log` 02:37 모델 로드 → 02:40 완료). 이번 세션은 로그와 결과 파일이 정확히 일치함을 확인한 뒤 재실행 없이 기록만 수행
- **다음**: exp22-adaptive-budget

## 실험 22: 적응형 예산 (총력전 카드④) — 목표 미달, 총력전 사이클 종료 (2026-08-07, AWS)

- **배경**: 총력전 사이클 마지막 카드 ④. exp17(SC 스케일업)에서 균일 n=32/64가 정체됐던 이유가 "이미 합의된 쉬운 문제에도 표를 낭비"하기 때문이라는 가설 — 합의 실패(최다 득표 ≤3표) 문제에만 표를 몰아주면 같은/더 적은 예산으로 더 큰 이득을 볼 수 있는지 검증
- **설정**: `remote/eval_adaptive_budget.py` — 1단계: 베이스 모델(Qwen2.5-3B-Instruct, vLLM)로 검증 483문항에 n=8 생성(temp=0.7, top_p=0.8, seed=42), 각 문제의 최다 득표수 확인. 2단계: 최다 득표 ≤3표(합의 실패)인 문제만 추가 n=24 생성(seed=43, 합계 32표)해 전체 표로 재다수결. 합의 성공 문제는 n=8 결과 그대로 유지
- **결과**: n8 baseline **74.5%**(360/483), 합의 실패 문제 **90개**(18.6%), adaptive **75.2%**(363/483), 문제당 평균 투표수 **12.47**(균일 n16 대비 22% 적은 예산)
- **판정**: 성공 기준(≥76.3%) **미달** (-1.1%p). 비교 기준인 균일 n16(exp07/17, 75.6%)보다도 -0.4%p 낮음 — 더 적은 평균 표본(12.47 vs 16)을 썼으니 예산 대비는 합리적이지만, 절대 정확도 자체가 목표에 못 미쳐 "성공 시 새 제출 스택" 조건 불충족
- **의미**: 합의 실패 문제에 표를 집중해도 절대적인 상한(75~76% 부근)을 못 넘음 — SC 계열 전체(균일/가중/정책앙상블/적응형)가 결국 비슷한 상한에 수렴하는 패턴 재확인. **총력전 사이클(exp19~22) 4장 전부 기각**(H8 정책앙상블·퓨샷·문제정화·적응형예산 모두 목표 미달) — 저비용 미검증 카드 소진, 다음은 근본적으로 다른 가설(H15 DPO, H13b 검증셋 라벨 재검수 등) 문서화·큐 등록 필요
- **결과 파일**: `results/eval_adaptive_budget.json`, `eval22_adaptive_budget.log`
- **사고 기록**: 실행 자체는 이전 서버 세션에서 이미 완료돼 있었음(02:45 스크립트 작성 → 03:05 완료, 스크립트·결과 파일 모두 미커밋 상태로 존재). 이번 세션은 로그(`eval22_adaptive_budget.log`)와 결과 파일이 정확히 일치함을 확인한 뒤 재실행 없이 기록만 수행
- **다음**: 큐 비어있음 — 로컬 Claude(계획자)가 새 근본 가설을 큐에 등록 필요

## 실험 16: 오답 정밀 분석 (H13 사전 단계) (2026-08-06, AWS)

- **배경**: docs/pivot-plan.md 사이클(트랙 A 동결·트랙 B 전 항목 기각) 종료 후, 남은 가설 H13(오답 유형 분석 → 취약 유형 표적 증강)의 사전 조사 단계. 후처리로 얻을 "공짜 점수"(①답 추출 실패)가 있는지, 검증 라벨 자체를 의심해야 하는지(④) 먼저 확인
- **설정**: `remote/eval_error_analysis.py` (베이스 모델, 어댑터 없음) — `remote/eval_vllm.py --mode sc --n 8`과 동일 설정(temp=0.7, top_p=0.8, seed=42)으로 검증 483문항 재실행하되, 오답 문제마다 `{id, question 앞 200자, 정답, 8개 샘플 추출 답 목록}`을 `results/wrong_analysis.jsonl`에 저장
- **1차 결과**: SC n=8 정확도 **73.7% (356/483)**, 오답 **127건** — exp05 기준(74.7%)과 -1.0%p 차이는 기존에 반복 관측된 vLLM 연속배칭 비결정성(±1%p) 범위 내
- **2차: 30건 정밀 분류** — 오답 127건 중 앞 30건에 대해 `remote/eval_error_sample_read.py`(보조 스크립트, greedy 1샘플 전체 텍스트 저장)로 전체 풀이 텍스트를 확보(`results/wrong_sample_texts.jsonl`), 직접 읽고 4유형으로 분류:

  | 유형 | 개수(/30) | 설명 |
  |---|---|---|
  | ① 답 추출 실패 | 0 | 없음 — boxed 안 값이 항상 모델의 실제 (틀린) 추론 결과를 반영 |
  | ② 계산 실수 | 3 | 접근법은 맞으나 산술·부분 누락 등으로 최종 수치가 틀림 (예: train-013850 popcount 최댓값을 n=1991에서만 확인, train-001333 구간 누락) |
  | ③ 접근 자체 오류 | 20 | 문제 조건 오독, 핵심 제약 누락, 근본적으로 다른(틀린) 풀이 전략 사용 — 압도적 다수 |
  | ④ 정답 라벨 의심 | 7 | 직접 재계산·검증 결과 모델 풀이가 오히려 타당하거나 문제 자체가 훼손(TNYWR 참조값 누락, 재귀식이 홀수에 도달 불가 등)돼 정답 라벨을 신뢰하기 어려움 (예: train-007093, train-013511, train-010120) |

- **판정**: **①이 0건** — 후처리/추출 로직 개선으로 얻을 공짜 점수는 이 표본 기준 사실상 없음. **③(접근 오류)이 67%로 압도적** — 이는 SFT/GRPO 모두 실패한 것과 일관됨(얕은 후처리가 아니라 추론 능력 자체의 문제). **④가 23%로 무시 못할 비중** — 검증 세트(483문항) 신뢰도에 상한이 있을 수 있음을 시사(추정 라벨 오류율 ~5%p 안팎이면 SC n=8 실측 74.7%의 진짜 상한/하한 해석에 유의 필요)
- **결과 파일**: `results/wrong_analysis.jsonl`(127건), `results/wrong_sample_texts.jsonl`(30건 전체 텍스트), `results/eval_error_analysis.json`, 로그 `eval16.log`, `eval16_sample.log`
- **다음**: H13 본체(표적 데이터 증강)는 트랙 A가 동결된 상태라 즉시 착수하지 않음. ④의 비중을 볼 때 검증 세트 라벨 재검수(가칭 H13b)가 SC 정확도 측정 자체의 신뢰도를 높일 수 있는 저비용 후속 조치로 유력 — 로컬 Claude가 다음 가설로 검토 필요. 큐 비어있음

## 실험 14: 트랙 B2 — 프롬프트 앙상블 SC (H19) (2026-08-06, AWS)

- **배경**: docs/pivot-plan.md 트랙 B2. SC의 힘은 풀이 다양성에서 온다는 전제 아래, 단일 시스템 프롬프트 대신 서로 다른 4종의 시스템 프롬프트(일반/검산 강조/대수적 접근/최단 경로)로 다양성을 인위적으로 확대하면 SC n8(74.7%)보다 나아지는지 확인
- **설정**: `remote/eval_prompt_ensemble.py` (베이스 모델, 어댑터 없음) — 검증 483문항(exp05와 동일 세트, seed=123), 프롬프트 4종 × 문항당 2샘플(temp=0.7, top_p=0.8, seed=42) = 8표 다수결, 총 3,864개 생성 요청, 소요 약 9분
- **결과**: **74.3% (359/483)** — `results/eval_prompt_ensemble.json`
- **판정**: 단일 프롬프트 SC n8(exp05, 74.7%) 대비 **-0.4%p**. 큐 명세의 판정 기준("±1%p는 노이즈로 간주")에 따라 유의미한 차이 없음 — **H19 기각**. exp10(가중투표 같은 표본 내 +0.8%p)·exp11(n16 가중투표 -0.4%p) 사례처럼 이 규모(483문항)에서의 ±0.5%p 내외 변동은 vLLM 비결정성·표본 노이즈로 해석하는 것이 일관됨
- **결과 파일**: `results/eval_prompt_ensemble.json`, `eval14_prompt_ensemble.log`
- **다음**: 트랙 B 남은 후보는 H18(exp15, 2단계 자기수정) — 큐에 이미 등록돼 있어 이어서 실행 대상

## 실험 15: 트랙 B3 — 2단계 자기수정 (H18) (2026-08-06, AWS)

- **배경**: docs/pivot-plan.md 트랙 B3. 1차 풀이를 같은 모델에게 "엄격한 채점자" 역할로 재검토·수정시키면(Self-Refine 계열) 산술·논리 오류를 스스로 잡아 greedy 단일 패스보다 나아지는지 확인. 모델 출력만 사용(코드 실행·외부 도구 없음)해 규칙에 적합
- **설정**: `remote/eval_two_pass.py` (베이스 모델, 어댑터 없음) — 검증 483문항(exp05와 동일 세트, seed=123). 1차: greedy(temp=0, max_tokens=2048)로 풀이 생성. 2차: "Problem + Proposed solution(앞 3000자)"을 채점자 프롬프트에 넣어 같은 모델이 greedy로 검토·수정, 최종 답을 boxed에서 추출(2차 추출 실패 시 1차 답 유지)
- **결과**: 1차 greedy **68.9%** (333/483), 2차 자기수정 **69.6%** (336/483), 답 변경 37건 — `results/eval_two_pass.json`
- **판정**: 비교 기준 exp05 greedy 69.4% 대비 2차 결과 **+0.2%p** — 판정 기준(±1%p는 노이즈)에 따라 유의미한 개선 없음, **H18 기각**. 1차 값(68.9%)이 exp05 기준(69.4%)과도 -0.5%p 차이가 나는데, 이는 기존에 관측된 vLLM 연속배칭 비결정성(±1%p) 범위 내. pivot-plan.md 트랙 B의 두 후보(B2 프롬프트 앙상블 exp14, B3 자기수정 exp15) 모두 채택 기준(+0.5%p) 미달로 트랙 B 전체가 유의미한 추가 이득을 내지 못함 확인
- **결과 파일**: `results/eval_two_pass.json`, `eval_two_pass.log`
- **다음**: pivot-plan.md의 트랙 A(동결)·트랙 B(B1·B2·B3 모두 목표 미달) 사이클이 모두 종료됨. 큐 비어있음 — 로컬 Claude(계획자)가 다음 방향(H8·H9 운영진 답변 여부 확인, 또는 새로운 근본 가설)을 문서화해 큐에 등록 필요

## 실험 12: 트랙 A — GRPO 강화학습 파일럿 (H17) (2026-08-06, AWS)

- **배경**: docs/pivot-plan.md 트랙 A. QLoRA SFT 3연속 실패(exp06/06c/09b) 이후 근본 전환 — KL 제약으로 베이스 정책을 크게 벗어나지 않으면서 정답 도달 여부만 보상해 SFT가 해치던 출력 다양성을 보존하는 것이 목표
- **설정**: `remote/train_grpo.py` (수정 없이 그대로 실행, API 불일치 없었음) — unsloth `FastLanguageModel` + `fast_inference=True`(내장 vLLM 생성), r=16/alpha=32 LoRA, 중간 난이도 위주 3,000문제(RFT 6회 시도 중 1~2회만 정답이었던 문제 우선), `num_generations=4`(그룹 상대 보상), `per_device_batch=4`, `lr=5e-6`, `max_steps=400`, 보상 함수: 정답 일치 +1.0, boxed 존재 +0.1. 검증 세트(seed=123, 500문항)는 `build_dataset()`에서 명시적으로 제외
- **학습 결과**: 400스텝 완료(약 2h18m 소요, runtime 8304s), `train_loss` **0.02028**, [wandb run ctiym4ny](https://wandb.ai/loonaticvibe2-11-jin-jason/huggingface/runs/ctiym4ny) — 학습 중 `reward/reward_correct/mean`이 스텝 전반에 걸쳐 0.3~0.65 사이를 오르내리며 뚜렷한 상승 추세 없이 노이즈 수준으로 진동(KL 0.003~0.005로 베이스 정책 근처에 안정적으로 유지된 것은 의도대로 확인됨). 어댑터: `outputs/grpo_pilot/grpo_pilot_final`
- **평가 결과** (`remote/eval_vllm.py --mode both --adapter outputs/grpo_pilot/grpo_pilot_final`, 검증 483문항): greedy **69.2%** (335/483 상당, -0.2%p vs 베이스 69.4%), SC n=8 **74.5%** (-0.2%p vs 베이스 74.7%), 가중투표 **74.9%** (+0.2%p vs 베이스 SC)
- **판정**: **성공 기준(SC n8 ≥ 75.7%, 베이스 +1%p) 미달** — 다만 QLoRA SFT 계열(-1.1~-2.9%p로 뚜렷한 하락)과 달리 GRPO는 베이스와 통계적으로 구분 안 되는 수준(±0.2%p, 기존 관측된 vLLM 비결정성 노이즈 ±1%p 이내)으로 "해롭지 않지만 이롭지도 않음". 파일럿 스케일(3,000문제·400스텝·num_generations=4)에서는 유의미한 개선 신호를 얻지 못함 — 보상이 학습 내내 정체된 것으로 보아 스텝 수·데이터 난이도 구성·num_generations 규모가 부족했거나, 이 태스크에서 GRPO 자체의 개선 여지가 크지 않을 가능성 모두 열려 있음
- **결과 파일**: `results/eval_outputs_grpo_pilot_grpo_pilot_final.json`, 로그 `train_grpo.log`(학습), `eval12.log`(평가)
- **다음**: pivot-plan.md 철수 기준("+1%p 미만이면 학습 트랙 동결")에 해당 — exp13에서 트랙 A 지속 여부 최종 판정 필요 (로컬 Claude 판단 대기)

## 실험 13: 트랙 A 지속/철수 최종 판정 (2026-08-06, 판정만 — 실행 없음)

- **배경**: docs/pivot-plan.md 성공/철수 기준 — "트랙 A 파일럿이 베이스 +1%p 미만이면: 학습 트랙 완전 동결, 남은 기간 추론 최적화 + 검증자/앙상블(H8·H9, 운영진 답변 대기)에 집중"
- **판정 근거**: exp12 SC n=8 74.5% vs 베이스(exp05) 74.7% = **-0.2%p** (성공 기준 +1%p·75.7%에 크게 미달, 오히려 소폭 하락)
- **판정**: **트랙 A(학습 트랙) 완전 동결**. QLoRA SFT(exp06/06c/09b, 3연속 실패)에 이어 GRPO(exp12)도 목표 미달로 실패 — 현재까지 시도한 모든 학습 파라다임(SFT 계열 3종 + GRPO 파일럿 1종)이 베이스 대비 개선을 만들지 못함. 파일럿 규모 확장(스텝·데이터 증량)으로 회복될지는 불확실하고, pivot-plan.md에 명시된 규칙(파일럿 미달 시 즉시 동결)을 그대로 적용
- **향후 방향**: 남은 자원은 트랙 B(추론 파이프라인 재설계)에 집중 — H18(2단계 자기수정)·H19(프롬프트 앙상블 SC)를 다음 가설로 큐에 등록 대기. H8(멀티 LoRA 앙상블)·H9(검증자 어댑터)는 여전히 운영진 답변 대기 중이므로 답변 도착 시 우선순위 재평가
- **결과 파일**: 없음(신규 실행 없이 exp12 데이터로 판정만 기록)
- **다음**: 로컬 Claude(계획자)가 트랙 B 다음 실험(H18 또는 H19)을 문서화해 큐에 등록 필요. 현재 큐 비어있음

## 실험 11: 트랙 B1 — 최적 추론 스택 확정 (SC n=16 + 가중투표) (2026-08-06, AWS)

- **배경**: docs/pivot-plan.md 트랙 B1. exp07(n16 다수결 75.6%)과 exp10(n8 가중투표 +0.8%p)의 이득이 합산되는지 확인 — n=16 + 가중투표 조합이 76%+에 도달하는지가 목표
- **설정**: `remote/eval_vllm.py --mode sc --n 16` (베이스 모델, 어댑터 없음), 검증 483문항(exp05·07·10과 동일 세트, seed=123), SC temp=0.7/top_p=0.8/seed=42, 같은 표본에서 일반 다수결과 확신도 가중 투표(H16) 동시 측정
- **결과**: n16 일반 다수결 **74.7% (361/483)**, n16 가중투표 **74.3% (359/483)** — 가중투표가 오히려 다수결보다 **-0.4%p 낮음**. 목표(76%+) 미달
- **의미**: exp10(n8)에서는 가중투표가 다수결보다 +0.8%p 높았는데, 이번 n16에서는 반대로 가중투표가 더 낮게 나와 **가중투표의 이득이 n에 비례해 누적되지 않고, 오히려 표본·실행마다 부호가 바뀔 정도로 불안정**함을 시사. 또한 이번 n16 다수결(74.7%)은 exp07에서 측정한 n16(75.6%, 365/483)보다 낮아 — exp10에서 이미 관찰된 vLLM 연속배칭 비결정성(±1%p 내외)이 n16에서도 재현됨. 결론적으로 "n16 + 가중투표" 스택이 "n8 단순 다수결"(74.7%)보다 뚜렷하게 낫다고 보기 어려움 — 트랙 B1의 개별 이득 합산 기대(~76.5%)는 확인되지 않음
- **결과 파일**: `results/eval_base.json` (`sc_n16`, `sc_n16_weighted` 필드 — exp10의 `sc_n8` 필드는 이번 실행으로 덮어써짐, 과거 수치는 EXPERIMENTS.md·log.csv에 보존), 로그 `eval11.log`
- **사고 기록**: 이번 실행도 이전 세션에서 이미 완료돼 있었음(eval11.log 완료 14:09, 학습·평가 중 git pull 충돌로 세션이 한 차례 중단·자동커밋 후 재개된 흔적 있음) — 재실행하지 않고 로그·결과 파일 검증 후 기록만 수행
- **다음**: 트랙 B1 결론은 "복합 스택이 확실히 낫다"고 하기엔 근거 부족 — 제출 스택은 비용 대비 SC n=8(±가중) 유지 권장. 큐의 다음 항목은 exp12(트랙 A, GRPO 파일럿)

## 실험 9a: NuminaMath 외부 데이터 준비 (2026-08-06, AWS)

- **설정**: `remote/prep_numina.py --take 30000` — AI-MO/NuminaMath-CoT(Apache 2.0, 원본 859,494개)에서 셔플 후 순회하며 채택: 풀이 길이 50~2500자, `boxed` 포함, `extract_answer`로 정수 답 추출 가능, 검증 세트(seed=123, 500문항) 질문 텍스트와 겹치지 않음. 채택된 외부 데이터를 자체 RFT 최단 풀이 데이터(`data/sft_short.jsonl`, 문제당 1개)와 병합·셔플
- **결과**: 외부 데이터 **30,000개** 채택 → `data/numina.jsonl`. 자체 12,923개와 합쳐 총 **42,923개** → `data/sft_mix.jsonl` (H12 학습용 입력, exp09b에서 사용)
- **결과 파일**: `data/numina.jsonl`(30,000줄), `data/sft_mix.jsonl`(42,923줄), 로그 `exp09a.log`
- **사고 기록**: 이 실행도 이전 서버 세션에서 이미 완료돼 있었으나(파일·로그 08-06 08:07 생성) 기록·커밋 전에 세션이 중단된 것으로 추정. 이번 세션에서 산출물을 검증(파일 라인 수 일치 확인) 후 기록만 대행, 재실행하지 않음. 같은 세션에서 이어서 `remote/train_qlora.py`로 exp09b 학습(`outputs/qlora_mix/qlora_r16_lr0.0001_ep1_final`, train loss 0.3994, [wandb fvh36wox](https://wandb.ai/loonaticvibe2-11-jin-jason/huggingface/runs/fvh36wox))까지도 이미 완료돼 있었음 — 평가만 이번 세션에서 실행
- **다음**: exp09b 평가(`remote/eval_vllm.py --mode both --adapter outputs/qlora_mix/qlora_r16_lr0.0001_ep1_final`)

## 실험 9b: QLoRA SFT 외부 데이터 혼합 (H12) — 3연속 실패, 정체 3/3 (2026-08-06, AWS)

- **설정**: `remote/train_qlora.py` CONFIG 수정 — `data_path=data/sft_mix.jsonl`(자체 RFT 최단풀이 12,923개 + NuminaMath-CoT 외부 30,000개 = 42,923개), `output_dir=outputs/qlora_mix`, `lr=1e-4`, `epochs=1`, r16/alpha32/seq2048, unsloth 4bit. 학습 후 `remote/eval_vllm.py --mode both --adapter outputs/qlora_mix/qlora_r16_lr0.0001_ep1_final`로 검증 483문항 평가(greedy + SC n=8 + 가중투표)
- **학습 결과**: train loss 0.3994 ([wandb fvh36wox](https://wandb.ai/loonaticvibe2-11-jin-jason/huggingface/runs/fvh36wox)), 어댑터 `outputs/qlora_mix/qlora_r16_lr0.0001_ep1_final`
- **평가 결과**: greedy **67.5%** (326/483), SC n=8 **71.8%** (347/483), SC n=8 가중투표 **72.0%** (348/483) — 베이스(exp05: greedy 69.4%/SC 74.7%) 대비 greedy **-1.9%p**, SC **-2.9%p**, 가중투표도 베이스 SC보다 **-2.7%p**로 모두 하락
- **의미**: H12(외부 데이터 혼합으로 자기증류 한계 탈피) 가설 기각. exp06(자체데이터만, lr2e-4)·exp06c(자체데이터만, lr5e-5)에 이어 **QLoRA SFT 계열 3연속 실패** — lr을 2e-4→5e-5→1e-4로 바꾸고, 데이터를 자체전용→외부혼합으로 바꿔도 회복되지 않음. 학습률/데이터 구성보다 더 근본적인 요인(학습 포맷, LoRA target modules, 프롬프트 템플릿 불일치, 혹은 SFT 자체가 이 태스크·모델 크기에 안 맞을 가능성)을 의심할 필요 — **정체 카운트 3/3 도달**, CONTEXT.md 운영 정책에 따라 근본 전환 검토 단계
- **결과 파일**: `results/eval_outputs_qlora_mix_qlora_r16_lr0.0001_ep1_final.json`, 로그 `train_mix.log`(학습), `eval09b.log`(평가)
- **사고 기록**: 학습·평가 모두 이전 서버 세션에서 이미 완료돼 있었음(train_mix.log 완료 10:57, eval09b.log 완료 11:12) — 기록·커밋·푸시 전에 세션이 사용량 한도 등으로 중단된 것으로 추정(exp09a·exp10과 동일 패턴). 이번 세션은 로그·결과 파일을 검증한 뒤 기록만 대행, 재실행하지 않음
- **다음**: 큐 비어있음. QLoRA SFT류는 3연속 실패로 보류 — 로컬 Claude(계획자)가 근본 전환 방향(GRPO / 학습 데이터 전면 재구성 / 추론 파이프라인 재설계 / 커리큘럼 학습 중 택1, 문서화 후 큐 등록) 결정 필요

## 실험 10: 확신도 가중 투표 (2026-08-06, AWS)

- **설정**: `remote/eval_vllm.py --mode sc --n 8` (베이스 모델, 어댑터 없음), 검증 483문항(exp05·07과 동일 세트, seed=123). SC 샘플(temperature=0.7, top_p=0.8, seed=42)에 대해 일반 다수결과 함께, 같은 생성 결과에서 확신도(`cumulative_logprob / 토큰수`)로 softmax 가중(scale=2.0) 후 투표하는 가중 다수결(H16)을 동시에 측정
- **결과**: 일반 다수결 **73.7% (356/483)**, 확신도 가중 투표 **74.5% (360/483)** — 같은 표본 내 비교로 가중 투표가 **+0.8%p** 높음
- **의미**: H16(기대 +1~3%p) 방향은 맞지만 기대 하단에 근접하는 수준의 개선. 다만 이번 실행의 일반 다수결 값(73.7%)은 exp05에서 측정한 SC n=8(74.7%, 361/483)보다 낮게 나왔는데, 두 실행 모두 동일한 시드(temp=0.7/top_p=0.8/seed=42)·동일 모델·동일 검증셋을 사용했음에도 차이가 남 → vLLM 연속 배칭(continuous batching)의 실행 간 비결정성으로 추정(±1%p 내외 노이즈). 가중 투표 자체의 효과는 "같은 실행 내" 비교(73.7%→74.5%)로 판단하는 것이 안전
- **결과 파일**: `results/eval_base.json` (`sc_n8`, `sc_n8_weighted` 필드), 로그 `eval10.log`
- **사고 기록**: 이번 실행은 이전 서버 세션에서 이미 완료되어 있었으나(로그·결과 파일 존재, 08-06 08:02 커밋) EXPERIMENTS.md·log.csv·queue.md 기록과 푸시가 되지 않은 채 남아 있었음(추정: 서버 Claude 사용량 한도로 세션 중단). 결과를 로그로 검증 후 이번 세션에서 기록만 대행, 재실행하지 않음
- **다음**: exp09a(NuminaMath 외부 데이터 준비, H12) → exp09b(외부 혼합 QLoRA)

## 실험 6c: QLoRA SFT 재시도 — 완만한 학습 + 최단 풀이 (2026-08-06, AWS)

- **설정**: exp06 실패 대응 1단계. `data/sft.jsonl`에서 문제당 가장 짧은 정답 풀이 1개만 남긴 `data/sft_short.jsonl` 생성 → `remote/train_qlora.py`를 lr=5e-5(exp06의 1/4), epochs=1(exp06의 절반)로 학습(train loss 0.2429, [wandb sv8lkhho](https://wandb.ai/loonaticvibe2-11-jin-jason/huggingface/runs/sv8lkhho)) → 어댑터 `outputs/qlora_gentle/qlora_r16_lr5e-05_ep1_final` → `remote/eval_vllm.py --mode both --adapter ...` (검증 483문항, exp05·06·07과 동일 세트)
- **결과**: greedy **67.9% (328/483)**, SC n=8 **73.3% (354/483)** — 베이스(69.4%/74.7%) 대비 greedy −1.5%p, SC −1.4%p. exp06(68.3%/73.5%)과 비교해도 **거의 동일하거나 소폭 더 낮음**
- **의미**: 학습률을 1/4로, epoch을 절반으로, 학습 데이터도 문제당 최단 풀이 1개로 줄였음에도 실패 폭이 줄지 않음 → **원인 가설 1(학습률 과함)은 주 원인이 아니었을 가능성이 높음**. 남은 원인 가설(2. 자기증류로 출력 다양성 감소, 3. 이미 푸는 문제만 학습해 실력 확장 없음) 쪽에 무게가 실림
- **정체 카운트 갱신**: QLoRA SFT 계열 시도 2연속 실패 (exp06, exp06c) → **정체 카운트 2/3**. 다음 QLoRA류 시도가 또 실패하면 근본 전환(GRPO/외부데이터 중심 재구성/DPO 등) 착수
- **후속 결정 (exp08)**: 반복 RFT(H10)는 "exp06c 어댑터가 베이스보다 좋을 때만 실행" 조건이었으나 조건 미충족 → **스킵**. 대신 백로그의 H12(외부 데이터 혼합)·H15(DPO)가 다음 유력 후보
- **결과 파일**: `results/eval_outputs_qlora_gentle_qlora_r16_lr5e-05_ep1_final.json`, 어댑터는 삭제하지 않고 보존
- **다음**: 큐 비어 있음 — 로컬 Claude가 H12/H15 중 다음 실험을 큐에 등록해야 함

## 실험 6: QLoRA SFT 1차 — 실패 분석 (2026-08-05→06)

- **결과**: 베이스 대비 greedy −1.1%p, SC −1.2%p. 학습 자체는 정상 종료(loss 0.166, 2 epoch)했으나 **낮아진 loss가 오히려 과적합 신호**
- **원인 가설 3가지**:
  1. **학습률 과함**: 2e-4 × 2 epoch은 3B 모델에 공격적 — 자기 풀이를 암기하는 수준까지 loss가 내려감
  2. **자기증류의 한계**: 자기가 만든 정답 풀이만 다시 배우면 새 정보가 없고, 출력 다양성이 줄어 **SC 다수결의 재료(다양한 풀이 경로)가 오히려 감소**
  3. **데이터 편향**: 이미 푸는 80% 문제의 풀이만 학습 — 못 푸는 20%는 데이터에 없어서 실력 확장이 안 됨
- **대응 (연결 체인)**: exp06c = 완만한 학습(lr 5e-5, 1 epoch) + 문제당 최단 풀이 1개(H14, 3B에 유리) → 그래도 안 되면 가설 2·3을 정면 공략: 외부 데이터 혼합(H12)·DPO(H15)로 전환. 정체 카운트 1/3
- **사고 기록**: 러너가 git pull 충돌로 사망해 평가 후 기록이 누락됐었음 → 러너에 자동 커밋 후 재시도 로직 추가

## 실험 6b: 리더보드 제출 파일 생성 (2026-08-06, AWS)

- **설정**: `remote/make_submission.py --n 8 --tag base` — exp06 QLoRA 어댑터가 베이스보다 하락했으므로 **어댑터 없이 베이스 모델**로 생성. SC n=8(temperature=0.7, top_p=0.8), 리더보드 831문항 전체(`deep_chal_math_leaderboard_filtered.csv`) 대상
- **결과**: `results/submission_base.csv` 831행 생성 완료, 정수 답 831개 전부 채움(결측 없음 확인). 실제 리더보드 점수는 사용자가 Kaggle에 제출해야 확인 가능
- **다음**: exp07(SC 스케일 실험) → exp06c(완만한 QLoRA 재시도)

## 실험 7: SC 샘플 수 스케일링 (2026-08-06, AWS)

- **설정**: `remote/eval_vllm.py --mode sc --n 16`, 이어서 `--n 4` — 베이스 모델(어댑터 없음), 검증 483문항(exp05와 동일 세트), temperature=0.7, top_p=0.8
- **결과**: n=4 **72.7%** (351/483) / n=8 **74.7%** (361/483, exp05) / n=16 **75.6%** (365/483)
- **의미**: n=4→8 구간(+2.0%p)이 n=8→16 구간(+0.9%p)보다 이득이 커 **수확 체감** 확인(H11 가설과 일치, 다만 32까지는 아직 미탐). 추론 비용(샘플 수 비례)을 고려하면 제출용으로는 n=8이 비용 대비 합리적 선택 — n=16은 소폭 이득(+0.9%p) 대비 2배 비용
- **결과 파일**: `results/eval_base_sc_n4.json`, `results/eval_base_sc_n16.json`
- **사고 기록**: 실행 자체는 이전 서버 러너 세션에서 완료됐으나(로그 `logs_exp07_n16.log`, `logs_exp07_n4.log` 확인) 기록 단계 전에 세션이 종료되어 이번 세션에서 결과를 확인 후 기록만 대행
- **다음**: exp06c(완만한 QLoRA 재시도, H14 결합)

## 실험 4: RFT 데이터 생성 (2026-08-05, AWS)

- **설정**: `remote/generate_rft.py`, 베이스 모델 Qwen2.5-3B-Instruct, vLLM 샘플링(temperature=0.8, top_p=0.95, max_tokens=2048), 문제당 n_samples=6, 문제당 정답 풀이 최대 3개 채택(max_keep), chunk_size=500(중단-재개 지원)
- **대상**: train 17,000문제 중 검증 500문제(seed=123) 제외한 16,500문제. 이 시점엔 아직 오류 문항(627개) 사후 필터 전 — exp04b에서 제거
- **결과**: 16,500/16,500 처리 완료. **정답 도달 문제 13,190개 (79.9%)**, 채택 풀이(assistant CoT) 총 **36,733개** → `data/sft.jsonl`에 저장 (커밋 대상 아님, 통계만 기록)
- **exp04b 필터링**: `deep-learning-challenge-2026/train_filtered_ids.csv`(오류 문항 627개)의 id에 해당하는 라인을 id 필드 기준으로 제거. **36,733줄 → 36,144줄 (589줄 제거)**. 이후 `data/sft.jsonl`은 필터된 버전으로 교체됨
- **다음**: exp05(베이스 모델 AWS 평가) → exp06(QLoRA r16 SFT, 필터된 sft.jsonl 사용)

## 실험 5: 베이스 모델 AWS 평가 (2026-08-05, AWS)

- **설정**: `remote/eval_vllm.py --mode both` (베이스 모델, 어댑터 없음), 검증 500문제(seed=123)에서 오류 문항(train_filtered_ids.csv) 자동 제외 → **유효 검증 문항 483개**
- **결과**: greedy(temperature=0, max_tokens=2048) **69.4% (335/483)**, SC n=8(temperature=0.7, top_p=0.8) **74.7% (361/483)**
- **의미**: exp02·03(Kaggle, 구 500문항 기준)과는 검증 세트 구성이 달라 직접 비교 불가하지만, AWS 환경·필터된 483문항 기준의 새 베이스라인 확보. exp06(QLoRA) 이후 이 수치와 비교해 SFT 효과 측정
- **결과 파일**: `results/eval_base.json`
- **다음**: exp06-qlora-r16

## 실험 1: 베이스라인 (2026-07-31)

- **설정**: 파인튜닝 없음. fp16, T4 x2, greedy 디코딩(do_sample=False), max_new_tokens=1024, batch 16
- **프롬프트**: system에 "step by step + 최종 정수 답을 \boxed{}에" 지시
- **후처리**: \boxed{} 마지막 값 → 없으면 마지막 정수 → 없으면 0
- **결과**: train 50문제 미니 평가 66.0%
- **발견한 문제점** (다음 실험에서 개선할 것):
  1. 긴 풀이가 1024 토큰에서 잘려 \boxed{}를 못 쓰는 경우 발생 → 토큰 한도 2048로 증가 필요
  2. 폴백 추출이 소수의 일부를 정수로 착각 (예: "38.585"에서 585를 뽑음) → 소수점 뒤 숫자 제외 로직 필요
  3. **(사후 발견)** GitHub→Kaggle 복사 과정에서 백슬래시가 한 겹 벗겨져, 실행된 사본에서는 \boxed 추출 정규식과 프롬프트의 \boxed{} 지시가 깨져 있었음. 즉 0.648은 "마지막 정수 폴백"만으로 낸 점수 → 노트북 02부터 모든 패턴을 백슬래시 없이(chr(92) 조립) 작성해 복사 경로와 무관하게 동작. exp02 상승분에는 이 수리 효과가 포함될 것

## 실험 50: 집계 축 최종 폐쇄 — 답값 사전·메타집계 전부 기각 (2026-08-11, 로컬 CPU)

- **배경**: 교사 증류(H23) 착수 전에 "정말 데이터셋만이 답인가"를 확인. 지금까지 집계 규칙 39종은 전부 **확신도·잘림·길이** 기반이었고, **답 값 자체**와 **규칙을 쓰는 방식**은 미탐색이었다. GPU·API 불필요(기존 덤프만 사용)
- **판정 원칙**: exp48에 따라 시드 42/43/44 **대응 비교**. 채택 기준 = 3시드 모두 양수 + 평균 +3문항 이상

### 50-A. 답 값 사전 (`remote/sweep_answer_prior.py`)
분포에는 신호가 분명히 있었다(n=32, 검증 483문항):

| | \|값\| 중앙 | \|값\| 90% | \|값\|>10⁴ 비율 | 답=0 비율 |
|---|---|---|---|---|
| 정답 | 25 | 400 | 1.87% | 0.72% |
| 오답 | 40 | 3,988 | **7.12%** | **2.39%** |

오답이 극단값일 확률이 약 4배(관측 최대 300자리 정수). 그러나 **15개 변형 전부 중립 또는 하락**.
**원인 규명**: 기준 규칙이 실제로 고른 답 중 \|값\|>10⁴는 14/483(2.9%)뿐이고 그중 오답은 7개.
→ **극단값 오답은 애초에 소수표라 투표에서 이기지 못한다. 분포에는 신호가 있으나 결정 경계에는 없다.**

### 50-B. 메타 집계 (`remote/sweep_meta_aggregation.py`)
규칙 자체가 아니라 **쓰는 방식**을 바꾼 3축, 9변형 — 전부 기각(최고가 ±0문항):

| 축 | 결과 |
|---|---|
| 규칙 앙상블(7종 투표) | 시드별 차이 **0 / 0 / 0** |
| 부트스트랩 안정성(B=200/500) | −1.00 / −0.33문항 |
| 조건부 규칙(표차 기준 분기, 4변형) | 0.00 ~ −0.67문항 |

**원인 규명 — 규칙 간 불일치율**: majority·weighted(1/2/4)·trim25·drop_trunc·len_trim 7종이 서로
**1.2~5.6%**밖에 다르지 않다. 서로 다른 실수를 하는 게 아니라 **같은 실수를 한다** → 어떤 조합도 이득이 없다.
39종이 76.4%에서 일제히 포화한 이유가 이것이다.

### 50-C. 선택 축이 닫힌 이유 (핵심)
"다수결 1위가 틀렸다"는 **탐지는 잘 된다**:

| 특징 | 1위 정답(364) | 1위 오답(119) |
|---|---|---|
| 표차 | 27.4 | 7.8 |
| 1위 득표율 | 0.90 | 0.40 |
| 고유 답 개수 | 2.7 | 12.4 |
| 평균 길이 | 411 | 843 |

그런데 **탐지해도 바꿔 넣을 답이 없다**. 1위가 틀린 119문항에서 정답의 위치:
- 정답이 2위 : **22문항(18.5%)** — 2위로 바꾸면 81.5%는 오히려 망친다
- 정답이 3위 : 15문항
- 32표 어디에도 없음 : **59문항(49.6%)**

**→ 집계·선택 축 최종 폐쇄.** 남은 유일한 방향은 **생성 품질 개선(= 학습 데이터)**이며,
이는 exp49(강한 교사 증류)가 겨냥하는 지점과 정확히 일치한다.

## 실험 54: implicit PRM — 폐기한 DPO 체크포인트를 채점기로 재해석 (2026-08-14, AWS)

- **가설(H25)**: DPO 학습은 정책을 바꾸는 동시에 **암묵적으로 보상모델을 만든다**
  (arXiv 2412.01981). 보상은 `r(x,y) = β·log[P_policy(y|x) / P_ref(y|x)]` 로 읽을 수 있고,
  이 값은 토큰 단위로 분해되므로 **과정 보상(PRM)을 추가 학습 없이** 얻을 수 있다.
  exp47에서 성능 개선에 실패해 폐기한 DPO 어댑터가 그대로 재료가 된다 — 학습 비용 0.
- **구현**: `remote/eval_implicit_prm.py`. 어댑터를 켠 상태에서 정책 로그확률을,
  `PeftModel.disable_adapter()` 컨텍스트 안에서 참조 로그확률을 재서, **한 모델로 두 점수**를 뽑는다.
- **표본**: 검증 483문항 × 32후보(`results/rm_gen_seed42_n32.jsonl`, seed 42)

| 규칙 | 정확도 | 기준 대비 |
|---|---|---|
| baseline (확신도 가중 투표 = 최종 스택) | **76.0%** (367/483) | — |
| prm_sum_best (보상 합 1위 채택) | 69.2% (334/483) | **−33문항 (−6.83%p)** |
| prm_mean_best (보상 평균 1위 채택) | 69.2% (334/483) | **−33문항 (−6.83%p)** |
| prm_sum_vote (보상 합 가중 투표) | 74.5% (360/483) | −7문항 (−1.45%p) |
| prm_mean_vote (보상 평균 가중 투표) | 76.0% (367/483) | ±0 |
| prm_mean_plus_conf (보상 + 확신도) | 76.0% (367/483) | ±0 |

- **판정**: **H25 최종 기각.** 양수가 하나도 없어 exp48 원칙(시드 3개 확장)을 적용할 대상 자체가 없다.
- **해석**: `_best` 계열이 −6.83%p로 가장 크게 망가진 것이 핵심이다. 투표를 버리고 보상 1위만
  채택하면 급락한다 = **암묵적 보상은 후보 간 순위를 거의 못 매긴다**. exp47에서 이미 관측된
  rewards/accuracies **0.60**과 정확히 같은 이야기다 — DPO가 학습한 신호가 애초에 약했으므로,
  그것을 어떤 방식으로 다시 읽어도 없는 신호가 생기지는 않는다.
- **자기검증 축의 5번째 기각**: 검증자(+0.5%p) → 쌍대 토너먼트(+0.21%p) → DPO 보상정확도 0.60
  → 보상 헤드 holdout 0.647 → implicit PRM(0 이하). **서로 다른 5가지 방법이 전부 판별력 ~0.6에
  수렴한다.** 이는 구현 문제가 아니라 3B 모델이 자기 풀이의 옳고 그름을 표현에 담고 있지 않다는
  뜻으로 읽어야 한다.
- 산출: `results/eval_implicit_prm_rm_gen_seed42_n32.json`, `implicit_prm.log`

## 실험 51: 스칼라 보상 헤드 + Bradley-Terry — 목적함수를 바꿔도 같은 자리 (2026-08-13~14, AWS)

- **가설(H24)**: exp18c(검증자)의 실패를 **목적함수 오류**로 재해석했다. exp18c는 "이 풀이가 맞나?"를
  Yes/No 토큰으로 **생성**하게 학습시켰는데, 그것은 문제를 넘나드는 **절대 채점** 과제다.
  Best-of-N이 실제로 요구하는 것은 **같은 문제 안에서 후보끼리의 순위**다. 스칼라 보상 헤드에
  Bradley-Terry 쌍대 손실을 걸어 그 과제 자체를 최적화한다. 운영진이 2026-08-11에 파라미터 추가를
  명시 허용해 열린 축이며, 데이터는 `data/verifier.jsonl` 재활용이라 추가 생성 0.
- **겨냥한 헤드룸**: exp50이 측정한 **+4.14%p**(상위 2후보 완벽 선택).

### 51-A. 판별력 자체 (`remote/eval_reward_holdout.py`)
학습에 쓰지 않은 holdout에서 정직하게 잰 **쌍 정확도 0.647** (SE 0.031, 95% CI 0.586~0.708).
exp47 DPO의 rewards/accuracies 0.60과 통계적으로 구분되지 않는다.

### 51-B. 실제 Best-of-N 효과 (`remote/eval_reward_bestofn.py --stage score`)
검증 483문항 × 32후보(seed 42), 같은 표본에 규칙만 다르게:

| 규칙 | 정확도 | 기준 대비 |
|---|---|---|
| baseline (확신도 가중 투표) | **76.0%** (367/483) | — |
| rm_best (보상 1위 채택) | 70.8% (342/483) | **−25문항 (−5.18%p)** |
| rm_vote (보상 가중 투표) | 75.2% (363/483) | −4문항 (−0.83%p) |
| rm_plus_conf (보상 + 확신도) | 74.9% (362/483) | −5문항 (−1.04%p) |

- **판정**: **H24 최종 기각.** 양수가 없어 시드 43·44 확장 대상이 없다.

### 51-C. 시뮬레이션이 틀린 이유 (방법론적 수확)
`remote/simulate_reranker.py`는 쌍 정확도 `a`를 받아 `z = √2·Φ⁻¹(a)`로 신호를 만들고
점수를 `z·[정답] + N(0,1)`로 두어 기대 이득을 계산했다. 예측은 **a=0.65에서 +1.45%p**였다.
실제는 **−0.83%p**. 부호까지 틀렸다.

**원인은 독립성 가정이다.** 시뮬레이션은 보상 모델의 오차를 투표와 무관한 가우시안 잡음으로 뒀지만,
실제 보상 모델은 **투표가 이미 저지르는 실수와 같은 실수를 한다** — 둘 다 같은 3B 표현에서 나오기 때문이다.
이는 exp50이 집계 규칙 7종에서 발견한 구조(상호 불일치율 1.2~5.6%, "서로 다른 실수가 아니라 같은 실수")와
정확히 같은 이야기다. **판별력 수치가 좋아도, 그 오차가 기존 규칙과 상관되어 있으면 이득으로 바뀌지 않는다.**
앞으로 재순위화 계열을 시뮬레이션으로 가늠할 때는 오차 상관을 파라미터로 넣어야 한다.

- 산출: `results/eval_reward_holdout.json`, `results/eval_reward_rm_seed42.json`, `eval51_rm_bestofn.log`

## 실험 53: 강한 교사 증류 — 능력은 그대로인데 고르지를 못하게 됐다 (2026-08-14, AWS)

- **가설(H23)**: 학습 6전(SFT×3, GRPO×2, DPO×1)이 전부 실패했는데, **6전 모두 학습 데이터가
  자기 자신이 만든 풀이(RFT) 아니면 외부 공개 데이터(NuminaMath)였다.** 강한 교사가
  **우리 문제에 대해** 쓴 풀이는 한 번도 써본 적이 없다 — 학습 축에서 유일하게 남은 미탐색 변형.
- **데이터**: 분야별 진단으로 고른 경시형 1,200문제 중 936문항을 채점해 **교사 정답률 59.8%**,
  정답 일치분 **560개**만 채택(라벨 노이즈 0). 생성은 서버의 헤드리스 Claude Code(`claude -p`).
- **학습**: QLoRA r=16, lr 5e-5, **3에폭(105스텝)**, `data/sft_teacher.jsonl`.

### 결과 (검증 483문항, n=32, 시드 대응 비교)

| 시드 | 베이스 가중투표 | 교사 어댑터 | 차이 | pass@32 |
|---|---|---|---|---|
| 42 | 75.8% (366) | 70.0% (338) | **−28문항 (−5.80%p)** | 424 → 423 (−1) |
| 43 | 75.4% (364) | 68.7% (332) | **−32문항 (−6.63%p)** | 422 → 417 (−5) |
| | | | **평균 −6.21%p** | |

시드 42 상세 (순위별 분해):

| 지표 | 베이스 | 교사 어댑터 | 차이 |
|---|---|---|---|
| 가중 투표 (최종 스택) | **75.8%** (366) | **70.0%** (338) | **−28문항 (−5.80%p)** |
| 단순 다수결 | 75.4% (364) | 70.4% (340) | −24문항 |
| 정답이 상위 2후보 안 | 79.9% (386) | 74.7% (361) | −25문항 |
| 정답이 상위 3후보 안 | 83.0% (401) | 77.2% (373) | −28문항 |
| **pass@32** | 87.8% (424) | **87.6%** (423) | **−1문항** |

### 해석 — 이 실패는 이전 실패들과 종류가 다르다
**pass@32가 사실상 그대로다(424→423).** 32번 뽑으면 정답을 만들어내는 능력은 손상되지 않았는데,
그 정답을 골라내는 순위가 전 구간에서 무너졌다. 즉 **능력을 잃은 것이 아니라 오답 쪽으로 확신이
기울었다** — 과적합의 전형적인 모습이다.

이는 이 프로젝트가 오래 믿었던 "다양성 붕괴" 설명과도 다르다(exp47이 DPO에서 이미 그 가설을
반박했고, 여기서도 다양성은 멀쩡하다). 학습이 망가뜨리는 것은 **다양성이 아니라 순위**다.

### ⚠️ 판정 보류 — 설정 변경이 원인일 수 있다
큐 명세는 **1에폭**이었으나 실행 시 **3에폭**으로 올렸다. 근거는 "데이터가 560개라 1에폭은
35스텝뿐이고, 12,923개를 전제한 exp06c의 '완만한 1에폭'과 같은 세기가 아니다"였다.
그런데 나온 결과가 과적합의 모습이므로 **이 변경 자체가 유력한 원인**이다.
가설(H23)의 성패를 말하기 전에 1에폭 대조군(exp53b)으로 두 원인을 분리한다.

시드 44 덤프는 취소했다 — −5.80%p는 시드 노이즈(±1%p)의 5배라 부호 확인에는 시드 43 하나면
충분하고, 같은 GPU 시간이면 1에폭 대조군이 정보량이 훨씬 크다("이미 아는 것"의 재확인 대신
"모르는 것"의 판별).

### 남은 선택 효과 (결과 해석 시 반드시 함께 말할 것)
교사가 틀린 376문항을 버렸으므로, 학습 데이터는 "어려운 문제" 중에서도 **강한 모델이 풀 수 있는
쪽**에 치우쳐 있다. 성공하더라도 가장 어려운 구간을 정복한 것은 아니다.

- 산출: `data/sft_teacher.jsonl`(560), `data/teacher_cc.jsonl`, `data/teacher_cc_rejected.jsonl`(376),
  `outputs/qlora_teacher/qlora_r16_lr5e-05_ep3_final`, `results/val_samples_teacher_s42.jsonl`

## 실험 53b·53c: 에폭이 원인이었나 — 그리고 H23 최종 판정 (2026-08-14, AWS)

exp53(3에폭)의 −6.21%p가 **가설의 실패인지 설정의 실패인지** 가르기 위한 두 단계.

### 53b. 1에폭 대조군 (시드 42)

| 지표 | 베이스 | 교사 3에폭 | 교사 **1에폭** |
|---|---|---|---|
| 가중 투표 | **75.8%** (366) | 70.0% (338) — **−28** | **73.9%** (357) — **−9** |
| 정답이 상위 2후보 안 | 79.9% (386) | 74.7% (361) | **79.7%** (385) — **−1** |
| pass@32 | 87.8% (424) | 87.6% (423) | 87.6% (423) |

**손상의 3분의 2가 에폭 선택 탓이었다.** 큐 명세(1에폭)가 옳았고, "데이터가 560개뿐이라
1에폭은 35스텝에 불과하니 exp06c와 같은 세기가 아니다"라며 3에폭으로 올린 판단이 틀렸다.
작은 데이터에서 스텝을 늘리는 것은 *같은 개입의 반복*이 아니라 **같은 예시를 외우는 것**이다.
1에폭에서는 상위 2후보 정확도가 79.7% vs 79.9%로 후보 풀의 품질이 사실상 손상되지 않았다.

### 53c. 최종 판정 — 전량 데이터(683개) × 1에폭 × 시드 3개

| 시드 | 베이스 | 교사(전량·1에폭) | 차이 | pass@32 |
|---|---|---|---|---|
| 42 | 75.8% (366) | 73.9% (357) | **−9문항 (−1.86%p)** | 424 → **427 (+3)** |
| 43 | 75.4% (364) | 73.9% (357) | **−7문항 (−1.45%p)** | 422 → **426 (+4)** |
| 44 | 76.4% (369) | 74.3% (359) | **−10문항 (−2.07%p)** | 423 → 421 (−2) |
| **평균** | 75.9% | 74.0% | **−8.67문항 (−1.79%p)** | **+1.7문항** |

- 교사 풀이 1,170문제 채점, **교사 정답률 60.0%**, 채택 683개.
- 부호 3시드 일관, 성공 기준(+1.5%p) 미달 → **H23 최종 기각. 학습 축 7전 종료**
  (SFT×3, GRPO×2, DPO×1, 교사증류×1).

### 이 실패는 병목이 "선택"이라는 가장 강한 증거다
**교사 증류는 실제로 모델을 더 유능하게 만들었다** — pass@32가 평균 **+1.7문항** 올랐고
시드 42·43에서는 +3, +4였다. 32번 뽑았을 때 정답이 후보 안에 들어 있는 문제가 늘었다는 뜻이다.
**그런데도 최종 점수는 −8.67문항 떨어졌다.**

지금까지 선택 병목의 근거는 "정답이 후보에 있는데 못 고른다"는 **정적인 관찰**(exp50)이었다.
이번에는 후보를 **늘렸는데 오히려 손해를 봤다** — 선택기가 개선을 흡수하지 못한다는 것을
능동적으로 보여준 것이라 증거의 성격이 다르다. 생성을 잘하게 만드는 것만으로는 점수가 오르지 않고,
그 개선을 받아낼 선택기가 함께 있어야 한다. 그리고 그 선택기는 다섯 갈래로 시도해 전부 ~0.6에서 막혔다.

### 선택 효과 (해석 시 함께 말할 것)
교사가 틀린 468문항을 버렸으므로 학습 데이터는 "어려운 문제" 중에서도 **강한 모델이 풀 수 있는 쪽**에
치우쳐 있다. 반대로 그 468문항은 exp52b(문제 품질 감사)의 재료가 된다.

- 산출: `data/sft_teacher_full.jsonl`(683), `outputs/qlora_teacher_full/qlora_r16_lr5e-05_ep1_final`,
  `results/val_samples_teacher_full_s{42,43,44}.jsonl`, `exp53b_chain.log`, `exp53c_chain.log`

## 실험 52b: 교사 오답 456건은 무엇이었나 — 라벨 감사 (2026-08-15, AWS, GPU 불필요)

- **동기**: 대상은 "베이스가 32번 시도해도 정답을 한 번도 못 만든 문제"인데 **강한 교사까지 40%를
  틀렸다**(exp53c: 교사 정답률 60.0%). exp16이 검증셋 오답 표본의 23%를 라벨 의심으로 분류한 것과
  방향이 같아, 이 비율이 크면 **검증셋 신뢰도의 상한**을 뜻한다.
- **방법**: `api/audit_rejected.py` — 오답 456건에서 무작위 30건(seed 42)을 뽑아, 각 건마다
  문제를 **처음부터 독립적으로 다시 풀게 한 뒤** gold와 비교해 분류. 판정자도 같은 계열 모델이라
  "내가 맞고 gold가 틀렸다"로 기울 수 있으므로, gold를 부정하려면 근거를 요구하고
  애매하면 TEACHER_WRONG으로 기울도록 지시했다.

| 판정 | 건수 | 비율 |
|---|---|---|
| GOLD_WRONG (문제는 명확한데 라벨이 틀림) | 19 | 73.1% |
| AMBIGUOUS (문장이 모호·불완전) | 5 | 19.2% |
| TEACHER_WRONG (문제·라벨 온전, 풀이가 틀림) | 2 | 7.7% |
| **문제·라벨 결함 의심** | **24/26** | **92.3%** |

*(30건 중 4건은 판정 호출 실패로 제외)*

### 검증 — 92.3%를 그대로 믿지 않고 직접 검산했다
운영진 공개 오류 목록(627문항)과의 겹침은 **0건**이라 독립적 뒷받침이 없었다. 그래서 GOLD_WRONG
3건을 손으로 풀어봤다:

| 문항 | 확인 결과 |
|---|---|
| train-003865 (부피 적분) | 상반 타원에서 ∬y dA = (2/3)ab² = 50√3, ×(1/√3) → **V=50**. gold 1250이 틀림 |
| train-014177 (자리 배치) | 여사건 = [x⁶](1+x+x²)⁵ × 6! × 4! = 45×720×24 = 777,600 → 10!−777,600 = **2,851,200**. gold 3,627,792(=10!−1008)가 틀림 |
| train-010964 (원뿔) | 일반해 x = L/(L+R). 원본 AIME(R=3, L=5)는 k=125/387 → 512. 그런데 이 문항은 숫자가 높이 6·반지름 2로 바뀌어 L=2√10이 되고 x=√10/(√10+1)이라 **k가 무리수** — "k=m/n(서로소 정수)"라는 전제 자체가 깨진다 |

3건 모두 결함이 실재했다. **판정자 편향만으로 92.3%가 나온 것은 아니다.**

### 그러나 이 숫자를 확대 해석하면 안 된다
92.3%는 **이중으로 조건화된 표본**의 값이다: ① 베이스가 32번 실패하고 ② 강한 교사까지 gold와
다른 답을 낸 문항. 두 조건을 통과한 집합에서 라벨 결함이 농축되는 것은 베이즈이지 편향이 아니다.
**"데이터셋의 92%가 깨졌다"는 뜻이 아니다.** 정확한 진술은 다음과 같다:

> 교사 오답 456건 중에서는 문제·라벨 결함이 지배적이며, 이는 exp16의 검증셋 오답 표본 23%와
> 같은 방향이되 훨씬 강하게 걸러진 표본이라 비율이 높다.

### 새로 확인된 결함 유형
train-010964는 **알려진 경시 문제의 숫자를 바꿔 만든 변형인데 그 변형이 문제의 전제를 깨뜨린** 사례다.
지금까지 걸러온 "자동생성 역문제"와는 다른 종류이며, 원본을 외운 모델은 바뀐 숫자를 무시하고
원본 답(512)을 그대로 내놓기 때문에 채점에서도 걸러지지 않는다.

### 발표에서의 쓰임
"로컬 85%는 도달 불가"의 근거가 하나 더 늘었다. 완벽한 상위 5후보 선택이 84.9%라는 통계적 상한
(exp50)에 더해, **검증셋 자체에 정답이 틀린 문항이 섞여 있어** 100%가 애초에 정의되지 않는다.

- 산출: `results/audit_rejected.jsonl`(26건 판정+사유), `audit52b.log`

## 실험 56: 정책 혼합 투표 — 정답을 더 놓아줬는데 0을 건졌다 (2026-08-15, GPU 불필요)

- **가설**: exp19(멀티 어댑터 앙상블)는 실패했지만 그때 쓴 어댑터들은 pass@n이 베이스보다
  **낮았다.** 교사 어댑터(exp53c)는 pass@32가 베이스보다 **높다**(+1.7문항). 갈아치우는 대신
  **섞으면** 베이스의 투표 성향은 유지하면서 후보 풀만 넓힐 수 있지 않을까.
- **방법**: `remote/merge_policy_vote.py` — 기존 덤프 재사용, GPU 0.

| 시드 | 베이스만 | 교사만 | A16+B16 혼합 | A32+B32 합집합 |
|---|---|---|---|---|
| 42 | 75.8% (366) | 73.9% (357) | 75.8% (366) **±0** | 75.8% (366) ±0 |
| 43 | 75.4% (364) | 73.9% (357) | 74.7% (361) −3 | 75.6% (365) +1 |
| 44 | 76.4% (369) | 74.3% (359) | 76.6% (370) +1 | 76.2% (368) −1 |
| **평균** | | | **−0.67문항** | **0.00문항** |

부호 혼재 → 노이즈, 기각.

### 그런데 옆에 있는 숫자가 결론이다

| 시드 | pass@32 베이스 | **pass@64 합집합** | 늘어난 몫 |
|---|---|---|---|
| 42 | 87.8% (424) | **90.5% (437)** | +13문항 |
| 43 | 87.4% (422) | **89.2% (431)** | +9문항 |
| 44 | 87.6% (423) | **90.1% (435)** | +12문항 |
| **평균** | 87.6% | **89.9%** | **+11.3문항 (+2.34%p)** |

**후보 풀에 정답이 평균 11.3문항 더 들어 있는데 투표가 건진 것은 0이다.**

이는 exp53c("능력을 올렸는데 점수가 떨어졌다")보다 **더 깨끗한 증명**이다. exp53c는 학습이
후보 풀 말고 다른 것도 바꿨을 여지가 있지만, 여기서는 **베이스 표본을 그대로 둔 채 후보만
추가**했다. 선택기 앞에 정답을 더 놓아주는 것 이외에 바뀐 것이 없는데도 회수는 0이었다.

정답이 32표 중 1~2표로 들어오면 다수결에도, 확신도 가중에도 잡히지 않는다. 후보를 늘리는 것은
**표를 늘리는 것이지 표차를 뒤집는 것이 아니다.**

- 산출: `remote/merge_policy_vote.py`

## 실험 55: max_tokens 2048→3072 — 잘림은 원인이 아니라 증상이었다 (2026-08-15, AWS)

- **가설**: 잘림을 지금까지 **집계 규칙**으로만 다뤘다(`drop_trunc` = 잘린 표 버리기).
  그건 "이미 버려진 표를 어떻게 셀까"의 문제이고, **잘림 자체를 줄이는 축**은 H1(1024→2048)
  이후 재검토한 적이 없다. `max_model_len`이 4096이라 3072까지 가능하다.
- **사전 진단** (`remote/analyze_truncation.py`, GPU 0): 잘린 표는 전체의 1.7%뿐이지만
  **오답 문항에 12배 몰려 있다** — 정답 문항 문항당 0.15표 vs 오답 문항 1.80표.
  "틀렸고 · 잘림 있고 · **안 잘린 표 어디에도 정답이 없는**" 표적 집합이 3시드에서
  28 / 30 / 28문항(**5.8~6.2%p**). 20~25%만 회수해도 +1.2~1.5%p로 2등과의 격차와 같다.

| 시드 | 2048 (기준) | 3072 | 차이 | 오답 문항의 잘림 (2048→3072) |
|---|---|---|---|---|
| 42 | 75.8% (366) | 74.5% (360) | **−6문항** | 1.80 → **1.46** |
| 43 | 75.4% (364) | 75.6% (365) | **+1문항** | 1.89 → **1.31** |

부호 혼재 → 노이즈 (시드 44 진행 중, 부호가 이미 섞여 결론 불변).

### 기제는 작동했는데 점수로 옮겨지지 않았다 — 인과를 거꾸로 읽었다
잘림은 의도대로 **30%가량 줄었다.** 그런데 정확도는 오르지 않았다.
즉 **잘림은 원인이 아니라 증상**이었다. 오답 문항에 잘림이 12배 몰린 것은 잘려서 틀린 것이
아니라, 모델이 그 문제에서 **헤매느라 길게 쓴** 것이다. 방을 더 준다고 헤매던 풀이가 정답에
도달하지는 않는다. **상관을 인과로 읽은 사례**로 기록한다 — 진단이 "표적 집합 5.8%p"라는
구체적 수치까지 냈기 때문에 설득력이 있어 보였다는 점이 특히 남길 만하다.

- 부수 측정: 시드당 41분 / 40분 (2048 대비 유의미한 지연 없음 — 8/31 시간 계획에 영향 없음)
- 산출: `results/val_samples_mt3072_s{42,43,44}.jsonl`, `exp55_chain.log`

## 실험 57: 검색 기반 퓨샷 — 예시의 관련성 문제가 아니었다 (2026-08-15, AWS)

- **가설(H26)**: exp20(퓨샷)은 기하·대수·정수론·응용 예시 **4개를 모든 문제에 똑같이** 붙여
  SC가 74.7%→73.1%로 떨어졌다. 무관한 예시가 방해했다면, **문제별로 비슷한 문제를 검색해**
  붙이면 반대 결과가 나와야 한다. 분야별 진단에서 조합론 30.0%로 유형별 풀이 패턴을 못 잡는
  것이 드러났으므로, 그 유형의 풀린 예시는 직접적인 처방이다.
- **구현**: `remote/eval_retrieval_fewshot.py`. TF-IDF 역색인을 직접 구현(sklearn 미설치 —
  과거 mergekit 설치가 transformers를 다운그레이드해 vLLM을 위협한 전례). 검색 풀 12,598개
  (`data/sft_short.jsonl`에서 검증셋 제외). `max_model_len` 4096→**8192**
  (Qwen2.5는 32k 지원인데 우리가 스스로 8분의 1로 묶어두고 있었다).
- **검색 품질은 좋았다**: 평균 1위 유사도 0.570, 예시를 못 찾은 문항 0개.
  펜/연필 문제엔 펜/연필 문제가, 반지름→지름 문제엔 사실상 동일한 문항(유사도 0.700)이 붙었다.

| 지표 (시드 42) | 베이스(제로샷) | 검색 퓨샷 k=3 |
|---|---|---|
| 가중 투표 | **75.8%** (366) | **73.5%** (355) — **−11문항 (−2.28%p)** |
| 단순 다수결 | 75.4% (364) | 73.5% (355) |
| pass@32 | 87.8% (424) | 87.0% (420) |

- **판정**: 노이즈 폭의 2배 이상 하락. exp20과 **같은 방향, 더 큰 폭**.
  시드 확장 없이 **H26 기각**.

### 즉시 세운 대안 가설도 즉시 반증했다
두 퓨샷 실험 모두 **"최단 정답 풀이"** 를 예시로 썼다. 모델이 예시의 **짧음을 흉내 내** 추론을
덜 하게 된 것이라면 생성 길이에 드러난다. 덤프의 `len`으로 바로 확인했다:

| | 평균 길이 | 중앙값 | 문항당 고유 답 |
|---|---|---|---|
| 베이스 | 518 | 388 | 5.06 |
| 검색 퓨샷 | **518** | 381 | 5.00 |

**차이가 없다.** 길이도 다양성도 그대로인데 정확도만 떨어졌다 → 간결성 모방 가설 기각.

**결론**: 퓨샷이 해로운 것은 예시가 무관해서가 아니다. 예시를 **정확히 겨냥해도** 해롭다.
이미 instruct 조율된 모델에 대화형 예시를 얹는 것 자체가 이 과제에서 손해다.
(학습 7전의 "기존 조율의 파괴"와 같은 이야기가 추론 시점에서 반복된 것으로 읽는다.)

- 산출: `results/val_samples_rfs3_s42.jsonl`, `exp57_chain.log`

## 실험 59: SFT 손실 마스킹 결함 — 실재했지만 원인은 아니었다 (2026-08-15, AWS)

### 발견 경위
"0.8을 넘긴 팀이 있는데 왜 우리는 안 되나"라는 사용자 지적을 따라, **우리 결론 자체가 틀렸을
가능성**을 점검하다 찾았다. TRL 문서:

> `completion_only_loss` 기본값 `None` — "prompt-completion 데이터셋이면 completion에만,
> **language modeling 데이터셋이면 전체 시퀀스에** 손실을 계산한다"

`train_qlora.py`는 `messages`를 직접 텍스트로 렌더링해 `dataset_text_field='text'`로 넘겼다.
정확히 language modeling 경로이며 `assistant_only_loss`도 기본 `False`였다.
→ **시스템 프롬프트와 문제 본문에까지 손실이 걸렸다.** SFT 4전(exp06·06c·09b·53c)이 전부 이 설정.

잘못 걸린 손실의 비중을 토크나이저로 실측:

| 데이터 | 비대상(문제+시스템) 비중 |
|---|---|
| 교사 683개 (exp53c) | **8.4%** |
| RFT 최단 풀이 (exp06/06c/09b) | **19.4%** |

### 수정
TRL의 `assistant_only_loss`는 Unsloth가 감싼 `SFTTrainer`에서 `formatting_func`를 요구하며
실패한다(`RuntimeError`). Unsloth 전용 `train_on_responses_only`로 Qwen2.5 템플릿 경계
(`<|im_start|>user\n` / `<|im_start|>assistant\n`)를 지정해 마스킹했다.
**마스킹이 실제로 걸렸는지 첫 샘플 `labels`의 `-100` 개수를 세어 출력하고 0이면 중단**하게 했다 —
조용히 안 걸리면 실험 자체가 무의미해지기 때문이다.

### 결과 — exp53c와 데이터·에폭·lr·시드 전부 동일, 마스킹만 다름

| 시드 | 베이스 | exp53c (전체 시퀀스) | exp59 (assistant만) |
|---|---|---|---|
| 42 | 366 | 357 (−9) | 363 (**−3**) |
| 43 | 364 | 357 (−7) | 354 (**−10**) |
| 44 | 369 | 359 (−10) | 362 (−7) |
| **평균** | 366.3 | **357.7 (−8.67)** | **359.7 (−6.67)** |

- 마스킹 수정의 효과는 **평균 +2문항(+0.41%p)** — 노이즈 폭 안.
- 여전히 베이스보다 −6.67문항이므로 **H23 기각은 유지**된다.

### ⚠️ 기록해야 할 자기 실수
시드 42만 나왔을 때 "−9 → −3, 손실의 3분의 2 회복"으로 **사용자에게 보고했다.** 3시드로 재보니
시드 43은 오히려 나빠졌고(−7 → −10) 평균 회복은 2문항이었다. **exp48 원칙(시드 하나로 판단하지
말 것)을 계속 인용하면서 정작 그 원칙을 어긴 보고였다.** 중간 결과를 전할 때는 "시드 1개 값이며
유지되지 않을 수 있다"를 반드시 함께 말해야 한다.

- 산출: `outputs/qlora_teacher_masked/...`, `results/val_samples_masked_s{42,43,44}.jsonl`

## 실험 60: 마스킹을 고치고 exp06c 재실행 — 학습 축 결론 확정 (2026-08-15, AWS)

- **왜 이 실험이 중요한가**: 초기 SFT 3전(exp06·06c·09b)이 "우리 규모의 추가 학습은 해롭다"는
  이 프로젝트 결론의 **출발점**이었고, 그 뒤 GRPO 2전·DPO 1전·교사 증류 1전이 전부 그 전제 위에서
  해석됐다. 그 3전이 쓴 RFT 데이터는 **잘못 걸린 손실 비중이 19.4%** 로 실측됐다(exp59).
  여기가 뒤집히면 학습 축 7전의 해석이 통째로 바뀐다.
- **설정**: exp06c와 동일 — `data/sft_short.jsonl`, lr 5e-5, 1에폭, r=16. **마스킹만 다름.**
  (마스킹 확인 로그: 첫 샘플 268토큰 중 72개 무시 = 학습 대상 73%)

| 시드 | 베이스 | RFT 학습 (마스킹 후) |
|---|---|---|
| 42 | 366 | 361 (−5) |
| 43 | 364 | 354 (−10) |
| 44 | 369 | 358 (−11) |
| **평균** | 366.3 | **357.7 (−8.67문항, −1.79%p)** |

- **판정**: 부호 3시드 일관, 성공 기준 미달 → **마스킹 가설 최종 기각.**
  참고로 exp06c(마스킹 전)는 SC n8 기준 −1.4%p였고 지금 −1.79%p로 **같은 대역**이다.

### 이 검증의 값어치 — 반증을 시도해서 결론이 단단해진 사례
"학습 7전 전패는 설정 오류 탓 아니냐"는 우리 결론에 대한 **가장 그럴듯한 반론**이었다.
실제로 결함을 찾아냈고(교사 데이터 8.4% / RFT 19.4%), 고쳤고, **두 데이터셋(exp59·exp60)으로
각각 3시드 재실행**했다. 결과는 바뀌지 않았다.

이제 "우리 규모의 추가 학습은 이 모델에 해롭다"는 **버그 의심이 배제된 관측**이다.
발견 경위도 남길 만하다 — 사용자가 "0.8을 넘긴 팀이 있다"고 지적한 것이 계기였고,
그 지적이 없었으면 결론을 재검토하지 않았을 것이다. **외부 반증 증거가 있을 때는
자기 결론부터 의심하는 것이 순서다.**

- 산출: `outputs/qlora_rft_masked/...`, `results/val_samples_rftmask_s{42,43,44}.jsonl`

## 실험 61: Ranked Voting SC — 순위 투표지는 후보 공간이 작을 때만 정보가 된다 (2026-08-15, AWS)

- **가설(H28, arXiv 2505.10772 ACL 2025)**: 우리가 소진한 집계 규칙 63종은 전부 **투표지는
  그대로 두고 개표 방식만** 바꾼 것이었다. 각 CoT가 **순위 매긴 후보 리스트**를 내게 하면
  투표지 자체가 바뀐다. 논문의 평가 체크포인트가 **Qwen2.5-3B-Instruct로 우리와 동일**하고,
  6개 벤치마크 전부에서 SC가 최하위였다(평균 SC 60.13 → MRR 65.08, +4.95pt).
- **우리 근거**: 다수결 1위가 틀린 문항에서 정답이 2위 18.5% + 3위 12.6% = 31%인데
  단일답 투표지에서는 이 표가 애초에 기록되지 않는다.

### 순응률 — 실험은 유효했다
| 지표 | 값 |
|---|---|
| RANKING 줄 있음 | 15,029/15,456 = **97.2%** |
| 2개 이상 순위 | 14,822/15,456 = **95.9%** |
| 1위 ≠ boxed 답 | 6.6% |

퓨샷 없이 제로샷 지시만으로 형식이 지켜졌다. **기제는 완벽히 작동했다** —
exp37처럼 "실행되지 않은 것을 기각으로 기록"하는 사고가 아니다.

### 결과 (시드 42, 같은 실행 안에서 규칙만 다름)

| 규칙 | 정확도 |
|---|---|
| top1_weighted (같은 실행 대조군) | 74.5% (360) |
| **mrr_weighted** | **74.9% (362)** — **+2** |
| mrr / top1 | 74.7% (361) |
| borda / borda_weighted / irv | 74.5% (360) |

- 같은 실행 내 최대 **+2문항** — 판정 기준(+5) 미달.
- **더 중요한 것**: 이 실행의 대조군이 360인데 기존 스택은 366이다. **순위를 요구하는 프롬프트
  변경 자체가 6문항을 깎았다**(exp58에서 프롬프트 5종이 전부 하락한 것과 같은 패턴).
  실제 스택 대비로는 **−4문항**.

### 왜 논문 결과가 전이되지 않았나 — 정량 진단
1위가 틀린 127문항에서 정답의 위치를 순위표 기준으로 분해했다:

| | 문항 수 |
|---|---|
| 정답이 1위 후보에 이미 등장 | 70 (55.1%) — 기존 투표도 아는 정보 |
| **1위엔 없고 2·3위에만 등장** | **19 (15.0%)** — 순위투표가 새로 얻는 정보 |
| 순위 어디에도 없음 | 38 (29.9%) |

그런데 **2·3위 자리에 문항당 고유 답이 11.7개** 흩어져 있다. 19개의 신호가 11.7개짜리
잡음 더미에 묻힌다.

**결론 — 이 논문 기법의 경계 조건**: 순위 투표지는 **후보 공간이 작을 때만** 정보가 된다.
논문 벤치마크는 AQUA-RAT(5지선다)·ARC-C·CommonsenseQA로 2위가 **남은 4개 중 하나**라
정보량이 크다. 우리는 정수 답 공간이 무한해서 2위가 **흩어진 약 12개 중 하나**다.
같은 체크포인트(Qwen2.5-3B-Instruct)에서도 과제의 답 공간 구조가 다르면 전이되지 않는다.

- 시드 43·44 확장은 하지 않았다: +2는 기준 미달이고, 기제 진단이 그 이유를 설명하며,
  실제 스택 대비로는 음수다.
- 산출: `results/val_samples_rank_s42.jsonl`, `remote/eval_ranked_voting.py`, `exp61_chain.log`

## 실험 62: CCD 계열의 전제 검증 — 구현 전에 전제부터 쟀다 (2026-08-15, GPU 1회 + CPU)

- **배경**: 2차 딥리서치(105 에이전트)에서 **7개 실측 제약을 전부 통과한 후보는 CCD 하나**였다
  (arXiv:2602.18232, 학습 없음·프롬프트 불변·같은 모델 내 두 KV 캐시 대조, +3~5pp).
  나머지는 전부 탈락 — DoLa는 36모델 독립 재현에서 **Qwen 계열이 오히려 악화**,
  CarBoN은 추론 시 외부 PRM-7B 필요(규칙 위반), SPREAD는 pass@N만 보고(우리 제약 #2가 반박),
  cache steering은 자유형식 수학에서 6/6 모델 null~음수, SPARK의 steering 벡터는
  **퓨샷의 활성 서명**(우리가 −1.6%p·−11문항으로 이미 잰 개입).
- **CCD도 유보가 무거웠다**: 하이퍼파라미터를 AIME25 자체에서 튜닝(인접 격자점 이득 0),
  n=30문항 오차막대 없음(+3.76pp ≈ 1.1문항), 논문에 "Qwen2.5"·"3B-Instruct" 0회 등장,
  전부 long-CoT thinking 모델, **"self-consistency 미적용"**.
- **그래서 방법이 아니라 전제를 먼저 쟀다.** 구현은 vLLM 밖 커스텀 디코딩이라 며칠짜리인데,
  전제("오류가 저확신 토큰에 국소화된다")는 긴 숙고 흐름에서 측정된 것이고 우리는 중앙값
  388토큰 short-CoT다. (exp55에서 상관을 인과로 읽고 GPU를 쓴 전례가 있다.)
- 재료: `remote/dump_token_logprobs.py`로 토큰 단위 logprob 저장(483×32, 798만 토큰, 52 MiB,
  토큰 수 불일치 0건).

### 전제 검증 결과 (`remote/test_confidence_premise.py`)

| 항목 | 정답 풀이 | 오답 풀이 |
|---|---|---|
| ① 저확신 토큰 비율 | 0.104 | 0.124 |
| ② **저확신 위치 분산** | **0.289** | **0.276** |
| ③ 최악 10% 평균 | −0.746 | −0.853 |

**②가 결정적이다. 0.29는 균등분포 값이다** — 저확신 토큰이 특정 구간에 몰려 있지 않고
풀이 전체에 흩어져 있다. CCD의 기제 전체가 "저확신 지점에서 개입"인데 **개입할 지점이 없다.**
논문이 전제한 "긴 숙고 흐름 속 국소화된 오류 구간"이 short-CoT 3B에는 존재하지 않는다.

판별력(AUC, 0.5 = 무신호):

| 신호 | AUC |
|---|---|
| 최악 10% 평균 (= DeepConf의 핵심 지표) | **0.610** |
| 현행 평균 logprob | **0.591** |
| 저확신 비율 | 0.578 |

### 실제 투표로 옮겨보면 (`remote/sweep_token_conf_vote.py`, scale까지 최적화)

| 가중치 | 정확도 | 기준 대비 |
|---|---|---|
| 현행 평균 logprob | 75.8% (366) | — |
| 최악 5% / 10% / 20% 평균 | 75.8% (366) | **+0** |
| 마지막 128토큰 평균 | 76.0% (367) | +1 |
| 최저 토큰 1개 / 마지막 32토큰 | 75.6% (365) | −1 |
| 평균 + 최악10% 절반씩 | 75.8% (366) | +0 |

**변형마다 최적 scale을 골라준 낙관적 표인데도 최대 ±1문항.** exp51의 교훈이 반복됐다 —
판별력이 조금 높아도(0.610 vs 0.591) 두 신호가 **같은 logprob에서 나와 오차가 상관되면**
이득으로 바뀌지 않는다.

### 결론 — 여섯 번째 0.6
| 신호 | 판별력 |
|---|---|
| 검증자 어댑터 | +0.5%p |
| 쌍대 토너먼트 | +0.21%p |
| DPO 암묵 보상 | 0.60 |
| 보상 헤드 holdout | 0.647 |
| implicit PRM | 0 이하 |
| **토큰 단위 확신도 (신규)** | **0.610** |

완전히 다른 여섯 번째 각도에서도 같은 자리다. **CCD·DeepConf 계열 최종 배제**,
며칠짜리 구현을 CPU 몇 초로 막았다.

### 연구가 함께 알려준 것 — 우리 모델 계열이 미연구 영역이다
2차 연구는 **우리 제약을 반박하는 근거를 하나도 찾지 못했고**, 더 결정적으로
**Qwen2.5-3B-Instruct나 short-CoT non-thinking instruct 모델을 평가한 논문이 단 한 편도 없었다.**
모든 권고가 모델 계열을 건너뛴 외삽이며, 우리는 그 외삽이 깨지는 것을 두 번 실측했다 —
순위투표는 **답 공간 구조** 때문에(exp61), CCD는 **추론 길이 구조** 때문에(exp62).

- 산출: `results/val_tok_s42.jsonl`(52 MiB, 재사용 가능), `remote/test_confidence_premise.py`,
  `remote/sweep_token_conf_vote.py`

## 실험 65: 소수 지분 혼합 + 우리 판정 절차의 실제 판별력 (2026-08-16, GPU 불필요)

외부 검토에서 나온 두 지적을 우리 데이터로 검증했다.

### ① 소수 지분 혼합 — 기제 논증이 실증으로 무너졌다
지적: "32표가 전부 같은 분포에서 나와 틀릴 때 같이 틀린다. **소수 지분**으로 다른 생성기를
섞으면 베이스가 커버 못 하는 문제에서만 캐스팅보트를 쥐어 오류가 탈상관된다."
(exp56은 50:50과 32+32 합집합만 했으므로 이 예측 자체는 미검증이 맞았다.)

| 비율 (베이스:어댑터) | 시드 42 | 43 | 44 | 평균 | 대조 대비 |
|---|---|---|---|---|---|
| **32:0** (대조) | 366 | 364 | 369 | 366.3 | — |
| 28:4 | 366 | 365 | 367 | 366.0 | −0.33 |
| **24:8** | 367 | 364 | 369 | **366.7** | **+0.33** |
| 20:12 | 367 | 364 | 368 | 366.3 | ±0 |
| 16:16 | 366 | 361 | 370 | 365.7 | −0.67 |
| 0:32 | 357 | 357 | 359 | 357.7 | −8.67 |

**McNemar가 이유를 보여준다.** 24:8에서 불일치쌍이 시드별 b=6/c=5, 2/2, 3/3 —
483문항 중 11개만 뒤집히고 그것도 **양방향**이다.

**기제가 성립하지 않는 이유**: 베이스가 정답을 못 만드는 문제에서 베이스의 **오답이 이미
24표를 쥐고** 있고, 어댑터의 8표는 여러 답으로 흩어진다. 캐스팅보트를 쥐려면 소수표가
한 답에 뭉쳐야 하는데, 어려운 문제일수록 그게 안 일어난다(오답 문항의 고유답 12.4개).
**소수 지분 앙상블은 "소수가 뭉칠 때"만 작동하는데 우리 실패 구간은 정확히 그 반대다.**

### ② 판별력 — 지적이 절반만 맞다, 그러나 그 절반이 중요하다
지적: "483문항 SE ≈ ±1.9%p이므로 +1~2%p는 원리적으로 판별 불가."

| | 임계 (α=0.05) |
|---|---|
| 독립 두 측정 비교 | **±2.76%p** ← 지적의 정확한 값 |
| **우리 대응 비교 (McNemar)** | **±1.49%p** |

우리는 exp48 이후 같은 문항·같은 시드로 **대응 비교**를 해왔으므로 임계가 1.85배 낮다.
"원리적으로 불가능"은 아니었다.

**그러나 우리 성공 기준이 정확히 +1.5%p였다.** 즉 우리는 **탐지 임계선 위에 정확히 걸쳐서**
판정해왔고 여유가 0이었다. 이것이 실제 결과를 낳았을 수 있다 — 부호가 일관되게 양수였는데
기준 미달로 기각한 것들이 있다:

| 실험 | 효과 | 판정 |
|---|---|---|
| Budget Forcing (exp32·42) | **+0.6%p** (두 번 모두 양수) | 기각 |
| 검증자 (exp18·25) | **+0.4~0.5%p** (일관 양수) | 기각 |

**둘 다 개별로는 임계 아래라 기각했고, 합쳐본 적이 없다.** 독립이라면 +1.1%p다.
검증셋을 2,500문항으로 늘리면 임계가 1.49%p → 약 **0.65%p**로 떨어져 이 크기가 판별 가능해진다.

**중요한 부수 확인**: 최종 스택은 어댑터를 쓰지 않으므로, RFT·검증자·교사증류 데이터 생성에
쓴 train 문항이라도 **베이스 스택 평가에는 오염이 없다.** 검증셋 확장에 제약이 거의 없다.

- 산출: `remote/analyze_mix_and_power.py`

## 실험 69: 표차 보상(Certified Self-Consistency) — 목적함수를 달성해도 답이 안 바뀐다 (2026-08-16, GPU 0)

- **배경**: 4차 딥리서치(111 에이전트)가 우리 소진 목록과 **구조적으로 다른** 축을 하나 찾았다.
  arXiv 2510.17472의 **라벨 없는 그룹 보상** — 답 히스토그램만으로 계산한다:
  - SNR: `r = (N₁−N₂)² / [n(N₁+N₂) − (N₁−N₂)²]` (1·2위 표차 최대화)
  - 엔트로피: 답 분포의 Shannon 엔트로피 최소화
- **왜 구조적으로 새로운가**: 집계 규칙 63종은 **주어진 히스토그램을 어떻게 세느냐**를 바꿨고,
  자기검증 6종은 **정답 판별기**를 필요로 해 0.6 벽에 막혔다. 이 보상에는 correctness 항이
  아예 없어서 **0.6 벽이 적용되지 않는다.** 우리 GRPO×2·DPO는 정답 라벨 기반이었다.
- **그래서 구현할 뻔했다**(GRPO 6~8시간). 그 전에 **기제를 따졌다** — exp55·exp62에서 전제
  검증이 며칠을 아낀 전례를 따라.

### 전제 검증 (`remote/test_sharpening_premise.py`)
표 분포를 인위적으로 날카롭게/평평하게 만들며 정확도를 잰다.

| sharpen | 시드 42/43/44 | 평균 | 평균 표차 |
|---|---|---|---|
| 0.25 (평평) | 366 / 364 / 369 | **366.3** | 0.524 |
| 1.00 (현행) | 366 / 364 / 369 | **366.3** | 0.708 |
| 4.00 | 366 / 364 / 369 | **366.3** | 0.906 |
| 16.0 (극단) | 366 / 364 / 369 | **366.3** | 0.970 |

**표차는 0.52 → 0.97로 배 가까이 커지는데 정확도는 3시드 전부에서 완전히 불변이다.**

1위 정답/오답으로 쪼개면 이유가 드러난다:

| sharpen | 1위 정답 유지 | **1위 오답에서 회복** |
|---|---|---|
| 0.5 / 1.0 / 4.0 / 16.0 | 366/366 (전부) | **0/117 (전부)** |

### 결론 — 수학적으로 그럴 수밖에 없다
**날카롭게 만드는 것은 표 수의 단조 변환이므로 argmax를 바꿀 수 없다.**
SNR 보상의 목적함수를 완전히 달성해도 우리가 뽑는 답은 **하나도** 바뀌지 않는다.
얻는 것은 정확도가 아니라 **실행 간 분산 축소**이고, 우리 최종 분산은 이미 문항 표집
(Private SE 1.11%p)이 지배한다.

**유보(정직하게)**: 이것은 *사후 변환*으로서의 sharpening 검증이다. **학습**으로 하면 모델의
추론 자체가 바뀌어 다른 답이 1위가 될 여지는 원리적으로 있다. 다만 그 보상은 **현재 1위를
강화하는 방향**이라 우리 실패 구간(정답이 2위인 15%)에서는 오답 1위를 더 굳힌다.
논문 스스로 보증이 "model-relative"라고 인정하며, "32표 어디에도 없음 30%" 구간은
원리적으로 못 건드린다고 검증자가 지적했다. 또 논문의 헤드라인 이득 상당 부분이
**포맷 정규화**(format score 90.2→98.9)인데 우리 파서는 이미 추출 실패 0건이라 회수 불가다.

- **GPU 6~8시간을 CPU 몇 초로 막았다.**
- 산출: `remote/test_sharpening_premise.py`

### 부수 — 4차 연구가 exp68 설계에 준 경고
AdaSTaR(NeurIPS 2025)의 win statistic `w_i = (1/K)Σ I[y_i=ŷ_i]`는 **문자 그대로 우리 x/32
통과율**이고 진단 서술도 동일하다("the model wastes compute repeatedly re-training on examples
it can already solve"). 그런데 **Qwen2.5-3B GSM8K에서 4개 방법이 정확히 77.0 동률**(이득 0),
SVAMP는 −0.5다. 실이득은 FLOPs 93.7% 절감뿐이다.
더구나 그 논문의 AdaC 컴포넌트는 **"어려운 것만 뽑기"를 막으려고** 존재하며
"combining easy and hard samples yields better outcomes than selecting only hard samples"를
근거로 쉬운 문항을 재혼합한다 — **exp68의 순수 경계구간 필터가 정확히 그 반대 설계다.**

다만 AdaSTaR의 모든 수치가 **zero-shot greedy**이고 self-consistency 결과가 논문에 없다.
우리 병목은 greedy 정확도가 아니라 표차이므로 77.0 동률이 exp68을 죽이지는 못한다.
exp68은 예정대로 진행하되, 미달 시 "쉬운 문항 30% 리믹스" 변형이 문헌이 실제로 지지하는 arm임을 기록해둔다.

## 실험 64: 온도 양방향 + 프롬프트를 빼는 방향 (2026-08-16, AWS)

같은 실행·같은 시드 안에서 대조군을 함께 생성해 재실행 변동(0.48%p)을 제거한 설계(exp58 방식).

| 설정 | 정확도 | 대조 대비 | pass@32 |
|---|---|---|---|
| **base_t07** (대조 = 현행 스택) | **75.8%** (366) | — | 428 |
| base_t11 (확산, temp 1.1) | 74.7% (361) | −5 | 426 |
| **nosys_t07** (시스템 역할 제거) | 74.5% (360) | **−6** | — |
| base_t03 (집중, temp 0.3) | 74.1% (358) | −8 | 416 |
| **minimal_t07** (프롬프트 1/4로 축약) | 73.3% (354) | **−12** | 420 |

**답 추출 실패는 5종 모두 0/15,456** — 프롬프트를 줄여도 형식이 무너지지 않았으므로
이 수치는 형식 붕괴가 아니라 **정확도**를 잰 것이 맞다.

### 두 축이 닫혔다
1. **온도 0.7은 양방향 국소 최적**이다. 낮추면 커버리지까지 잃고(pass@32 428→416),
   높이면 커버리지는 유지되나(426) 표가 흩어져 투표에서 진다.
   exp27의 전역 스윕을 n=32·같은 실행 대조로 재확인했다.
2. **프롬프트를 빼는 것도 해롭다.** 그동안 프롬프트 실험 5건(exp14·20·57·58·61)이 전부
   **더하는** 변형이어서 "빼는 방향은 미검증"이라는 유보를 달아뒀는데, 이제 해소됐다 —
   축약(−12)도 시스템 역할 제거(−6)도 손해다. **exp01에서 비교 없이 정해진 프롬프트가
   실제로 국소 최적이었다.**

## 실험 68: 경계 난이도 구간 SFT — 가설이 자기 전제에서 실패 (2026-08-16, AWS)

- **가설(H31)**: 학습 7전의 데이터는 두 극단이었다. SFT 3전은 "베이스가 푼 문제 전부"였는데
  실측 분포상 **4표본 중 4개 다 맞히는 문제가 54.7%** — 절반 이상이 모델이 이미 완벽히
  아는 문제였다. 교사증류는 반대편 0/32(능력 부재)를 겨냥했다.
  **가운데 1~3/4 구간(1,464문항, 24.4%)** 은 SFT로 한 번도 안 겨냥했고, 그게 병목의
  정확한 좌표다 — 정답이 이미 표본에 나타나지만 표결에서 밀리는 지점.
- **데이터**: `data/sft_marginal_13.jsonl` — 1,464문항 → 풀이 2,476개(문항당 최대 2, 짧은 것 우선).
  **검증셋 483문항과 겹침 0건** 확인.
- **학습**: r=16, lr 5e-5, 2에폭(310스텝), assistant 구간만 손실(exp59 수정 적용).

| 시드 | 베이스 | 경계구간 SFT | 차이 | pass@32 |
|---|---|---|---|---|
| 42 | 366 | 354 | **−12** | 424 → 422 |
| 43 | 364 | 355 | **−9** | 422 → 421 |
| **평균** | 365.0 | 354.5 | **−10.5문항 (−2.17%p)** | 거의 불변 |

- **판정**: 임계(±7문항)를 넘는 명확한 하락, 부호 2시드 일관 → **H31 기각. 학습 축 8전 전패.**
  이번은 "측정 불가"가 아니라 **"해롭다"** 가 확정된 사례다.

### 가설이 자기 전제에서 실패했다
**pass@32가 거의 안 움직였다**(−2, −1). 경계 구간을 학습시켜도 **정답을 만들어내는 능력이
늘지 않았다**는 뜻이다. "정답 쪽으로 확률질량을 밀어준다"는 전제 자체가 일어나지 않았고,
투표만 나빠졌다. 데이터 선택을 바꾸는 것으로는 이 벽을 넘지 못한다.

### 문헌이 미리 준 경고 (사후 확인)
4차 딥리서치가 AdaSTaR(NeurIPS 2025)를 찾아줬는데, 그 win statistic이 **문자 그대로 우리
x/32 통과율**이고 진단 서술도 동일했다. 그런데 **Qwen2.5-3B GSM8K에서 이득이 정확히 0**이었고,
그 논문의 AdaC 컴포넌트는 **"어려운 것만 뽑기"를 막으려고** 존재하며 쉬운 문항을 재혼합한다.
exp68은 정확히 그 논문이 방지하려는 설계였고, 결과도 그 방향과 일치했다.
(다만 AdaSTaR 수치는 전부 greedy라 우리 병목인 표차와 직접 대응하지는 않는다.)

**남은 변형**: 문헌이 실제로 지지하는 arm은 "경계구간 + 쉬운 문항 30% 리믹스"다.
예산 제약으로 이번에는 실행하지 않는다 — 순수 필터가 −10.5문항인데 리믹스가 그것을
+7문항 이상 뒤집을 근거가 없다.

## 실험 70·71: 로컬에서 진 후보를 리더보드로 판정 — 그리고 그 결론을 시드 하나가 뒤집었다 (2026-08-16~17)

### 동기
우리 정책은 "로컬은 오답의 23%가 라벨 의심인 흐린 자, 리더보드 831문항은 운영진 정밀 검수를
거친 깨끗한 자 — 더 좋은 측정기를 아낄 이유가 없다"인데, 실제로는 **로컬이 지면 제출을
안 해왔다.** 사용자 지적으로 이 누락을 메웠다.

### exp70 (8/16) — 로컬에서 진 셋이 전부 LB에서 기존 최고를 넘었다

| 후보 | 로컬 | LB |
|---|---|---|
| 교사증류 어댑터 | −1.79%p | **0.78820** |
| temp 1.1 | −1.04%p | **0.78700** |
| DPO 어댑터 | −0.14%p | 0.78580 |
| 기존 n32w | 기준 | 0.78459 |
| RFT 마스킹 | −1.79%p | 0.77135 |

당시 세운 가설: **pass@32를 지키거나 올린 셋은 전부 올랐고, 떨어진 하나만 LB에서도 떨어졌다.**
exp52b(어려운 문항일수록 로컬 gold가 깨져 있음)와 맞물리는 기제로 보였다.

### exp71 (8/17) — 그 가설을 정면으로 검증했더니 무너졌다

**핵심은 seed 43 제출이었다.** 교사 어댑터의 재실행 변동을 재지 않으면 0.78820을 해석할 수 없다.

| 제출 | 점수 |
|---|---|
| 교사 어댑터 **@ seed 42** | **0.78820** |
| 교사 어댑터 **@ seed 43** (설정 완전 동일) | **0.78098** |
| **두 값의 평균** | **0.78459** ← 기존 스택과 소수점까지 동일 |

**같은 설정에서 시드만 바꿔 0.72%p가 흔들린다.** 0.78820은 운 좋은 쪽 값이었다.

pass@n 가설을 겨냥한 나머지도 전부 기존 이하였다:

| 후보 | 합집합 pass@n | LB |
|---|---|---|
| 혼합 16:16 | **90.5%** (우리 최고) | **0.77978** |
| 혼합 8:24 | 높음 | 0.78219 |
| 교사 @ temp 1.1 | 두 기제 겹침 | 0.78339 |
| 혼합 24:8 | 높음 | 0.77737 |

**커버리지가 가장 높은 후보가 가장 낮은 축에 있다.** → pass@n 가설 기각.

### 이번에 얻은 것 — LB 재실행 변동의 새 실측값 0.72%p
기존 값은 exp34b의 **0.48%p**였는데 그건 베이스 단일 쌍이었다. 0.72%p면 지금까지 제출한
15건 중 **0.777~0.788 구간이 전부 서로 구분 불가**다. 우리 스택(0.78459)이 그 한가운데 있고,
2등(0.79542)과의 격차 1.08%p도 이 변동폭의 1.5배에 불과하다.

### 방법론 교훈
8/16에 "세 후보가 독립적으로 같은 방향으로 움직인 건 우연으로 설명하기 어렵다"고 기록했다.
**틀렸다.** 변동폭이 0.72%p면 충분히 설명된다. 판정 전에 **같은 설정의 재실행 변동부터 재는**
절차(exp48에서 로컬에 대해 확립한 것)를 리더보드에도 적용해야 했다.

### 부수 사고 — 워치독이 인스턴스를 부팅 직후 꺼뜨렸다
`/tmp/idle_count`가 부팅을 넘어 살아남아, 부팅 후 첫 cron 틱에서 곧바로 `shutdown -h now`가
실행됐다. sshd가 올라오는 30~60초 사이에 꺼지니 "running인데 22번이 안 열린다"는 증상이
됐고, SG·NACL·라우트·ENI를 전부 뒤진 뒤에야 원인을 찾았다(전부 정상이었다).
카운터 파일이 부팅 시각보다 오래되면 무시하도록 수정.

## 실험 55 / 55b / 55c: 투표 통계 기반 메타 선택기 — 신호는 있으나 배포 이득은 없다 (2026-08-17, GPU 0)

### 동기 — 자기검증 6종과 무엇이 다른가
지금까지 선택기는 전부 **"이 풀이가 수학적으로 맞나?"** 를 모델에게 물었고, 여섯 갈래가
판별력 ~0.6에 수렴했다. 여기서는 **풀이를 전혀 보지 않는다.** 투표 통계 18개 특징만으로
"언제 다수결 1위가 틀리는가"를 학습한다. exp50의 조건부 규칙(표차 하나로 분기하는
사람이 만든 규칙)과도 다르다 — **여러 약한 신호의 조합**을 학습한다.

겨냥한 헤드룸: exp50 측정으로 1위가 틀린 문항에서 정답이 2위인 것은 22개(접전 267문항 중).
필요한 것은 천재 검증자가 아니라 **뒤집을 수 있는 22개 중 8개를 찾는 작은 분류기**다.

### exp55 — 이진 분류 (그리고 내가 저지른 두 오류)
접전 267문항에서 OOF AUC **0.660**. 그런데 상위 k를 뒤집으면 순이득이 음수였다.

**오류 ①**: "상위 20개 중 14개가 진짜여야 +8"이라고 계산했다. **틀렸다.**
접전 문항은 세 부류이고 **neutral이 92개(34%)** 다:

| 부류 | 뒤집으면 | 개수 |
|---|---|---|
| rescue (top1 오답 & top2 정답) | **+1** | 22 |
| harm (top1 정답) | **−1** | 153 |
| neutral (둘 다 오답) | **0** | 92 |

neutral은 뒤집어도 손해가 없으므로 `8 rescue / 0 harm / 12 neutral`도 +8이다.
비-rescue를 전부 harm으로 친 계산이 결론을 실제보다 닫힌 것처럼 보이게 했다.

**오류 ②**: AUC 0.660을 "우리 최고 기록"이라 불렀다. **positive가 22개뿐이라 표준오차가
약 0.066**이고, 보상헤드 0.647과의 차이 0.013은 이 표본에서 의미가 없다.
정확한 서술은 "기존 판별 신호와 비슷하거나 다소 높은 수준"이다.

### exp55b — 비용 민감 재설계
최적화 대상을 P(flip)이 아니라 **기대 이득 = P(rescue) − P(harm)** 으로 바꾸고(2-head),
neutral을 학습에서 제외했다. 평가 지표도 AUC가 아니라 **max OOF net gain**으로.

| 특징셋 | CV 시드 8회 max OOF net gain | 평균 |
|---|---|---|
| 전체 18개 | 2, 2, 2, 4, 3, 1, 0, 0 | **+1.8** |
| 축소 6개 | 2, 1, 1, 1, 2, 1, 1, 0 | **+1.1** |

**신호는 실재한다.** 모집단 rescue:harm = 1:6.95인데 상위 k에서 1:1~1:1.5로 올라온다 —
**harm 대비 rescue가 4~7배 농축**된다. exp55에서 "최상위에도 신호가 없다"고 한 것은 부정확했다.

### exp55c — nested CV로 잰 **배포 가능한** 이득
`max OOF net gain`은 여러 k를 본 뒤 최적을 고른 값이라 상한에 가깝다. 실전에서는 그 k를
미리 알 수 없다. 그래서 임계값을 **안쪽 CV에서만** 고르고 한 번도 보지 않은 fold에 적용했다.

| 특징셋 | 시드별 배포 net gain | 평균 | 양수 시드 |
|---|---|---|---|
| 전체 18개 | −1, −2, +1, −2, −1, −4, +1, −3 | **−1.38** | 2/8 |
| 축소 6개 | −2, 0, 0, +1, 0, −2, −2, −2 | **−0.88** | 1/8 |

**exp55b의 +1.8은 전부 사후 k 최적화의 산물이었다.** 임계값을 정직하게 고르면 이득이
사라지고 음수가 된다.

### 결론 — 신호 부재가 아니라 효과 크기 부족
사전에 설정한 엄격한 무신호 종료 조건(`모든 threshold에서 net ≤ 0`)은 **충족되지 않았다.**
투표 통계에는 rescue를 4~7배 농축하는 신호가 분명히 존재한다. 그러나 배포 조건에서
평균 −0.9 ~ −1.4문항이고 목표는 +8이다. 따라서 **신호 부재가 아니라 효과 크기 부족**을
이유로 이 트랙을 종료한다.

최상위에서도 rescue:harm이 약 1:1에 머물러 순이득이 거의 발생하지 않는다. 목표 +8을
달성하려면 harm을 훨씬 강하게 억제해야 하는데 현재 특징으로는 그 정도의 분리가 관찰되지 않았다.

### 부수 — seed disagreement는 신호가 없었다
H22(다중 시드 96표 합산)의 실패는 "합치면 좋아진다"를 기각한 것이지
"시드 불일치 → 1위 오류 확률"까지 검증한 것은 아니었다. 그래서 feature로 넣어 쟀다:

- **단독 AUC 0.406** (0.5 미만 = 무신호)
- 18개 특징 중 계수 크기 **14위** (−0.102)

표본이 작으므로 "반대로 쓰면 0.594"라고 해석하지 않는다. **현재 데이터에서 유효한 신호를
확인하지 못했다**가 정확하다.

### 논문 관점에서의 값어치
`AUC ≈ 0.66`인데 `max(rescue − harm)`은 배포 조건에서 음수다.
**판별 성능이 통계적으로 존재하는 것과, 실제 의사결정에서 점수를 개선할 만큼 정밀한 것은
별개의 문제**라는 사례다. exp51에서 시뮬레이션이 부호까지 틀렸던 것(판별력 0.65에
+1.45%p 예측 vs 실제 −0.83%p)과 같은 이야기의 더 깨끗한 판본이다.

- 산출: `remote/meta_selector.py`, `remote/meta_selector_utility.py`, `remote/meta_selector_nested.py`

## 실험 72: SC-aware 한계기여 보상 — 오프라인 진단으로 RL 착수 전에 걸렀다 (2026-08-17, GPU 0)

- **가설**: GRPO 전패(2전)의 원인이 보상 설계일 수 있다. 이진 정오 보상은 그룹 내 보상이
  동일하면 이점(advantage)이 0이 되어 **그래디언트 자체가 사라진다.** 32개 표본 중 정답이
  하나도 없거나 전부 정답인 그룹이 그렇다. 대신 **투표 결과에 대한 한계기여**를 보상으로 쓰면:

  `M(S) = c_gold − max_{a≠gold} c_a`,  `r_i = M(S) − M(S \ i)`

- **왜 오프라인 진단을 먼저 했나**: GRPO 1회 학습은 GPU 수 시간이다. 보상을 바꿔서 **실제로
  학습 신호가 생기는 그룹이 늘어나는지**는 기존 덤프만으로 계산할 수 있다.

| 그룹 유형 | 이진 보상 사용가능 | SC-aware 사용가능 |
|---|---|---|
| SC 정답 | 29.2% | 29.4% |
| SC 오답·정답 있음 | 57.9% | **86.1%** |
| 정답 없음 | 0.0% | **65.7%** |

- **판정: 착수 안 함.** 늘어난 신호의 **69.4%가 "정답 없음" 구간**에서 나온다. 그 구간은
  gold가 표본에 아예 없으므로 `M`이 오답들 사이의 서열만 재는 꼴이고, 학습 표적으로 쓸 수 없다.
  쓸 수 있는 구간(SC 오답·정답 있음)의 분리도는 2.387 → 2.572로 **+7.8%**에 그쳤다.
- 산출: `remote/diag_sc_aware_reward.py`

## 실험 73: 시스템 합집합 오라클 — 이 프로젝트에서 처음으로 노이즈 바닥을 넘은 헤드룸 (2026-08-17, GPU 0)

- **가설**: 우리는 시스템을 항상 "전체 정확도"로 판정해 폐기해왔다. 그런데 정확도가 낮은
  시스템도 **베이스가 틀리는 문항을 맞힐** 수 있다면 폐기 기준이 틀린 것이다.
- **설정**: 로컬 `results/val_samples*.jsonl` 35종의 합집합 오라클(어느 하나라도 맞히면 정답).

| | 문항 |
|---|---|
| 최고 단일 시스템 (dpo) | 370 |
| 35종 전체 합집합 | **400 / 483 (82.8%)** |

- 탐욕 추가: dpo 370 → +teacher_s43 380 → +rftmask_s44 385 → +teacher_s42 389 → +rank_s42 392
  → +tp_base_t11_s42 394 (**+24문항**)
- **가장 잘 구제하는 것이 가장 못하는 시스템이다**: `teacher_s43`은 자체 정확도 332(최하위권)인데
  **+10문항 구제**. `teacher_s42`(338) +9, `rank_s42`(356) +8.

### 대조군 — 이게 시드 노이즈가 아님을 확인했다
합집합은 시스템을 더하기만 해도 오른다. 그래서 **같은 계열 시드끼리**의 합집합을 바닥으로 뒀다.

| 합집합 구성 | 베이스 366 대비 |
|---|---|
| 베이스 시드 42/43/44 | +4 |
| DPO 시드 42/43/44 | +4 |
| **베이스 / 교사 / RFT마스킹 (이종 계열)** | **+14** |

**이종 계열 상보성은 시드 노이즈 바닥의 3.5배**다. 세 층위(후보·top1/top2·시스템) 중
처음으로 노이즈 바닥을 확실히 넘은 헤드룸이다.

### 그러나 결합 규칙 5종은 전부 음수
| 규칙 | 베이스 366 대비 |
|---|---|
| 시스템 다수결 2/3 | 363 (−3) |
| 최고 1위 득표율 채택 | 364 (−2) |
| 최고 가중 득표율 채택 | 361 (−5) |
| 나머지 둘이 합의하면 역전 | 363 (−3) |
| 베이스 저확신일 때만 게이트 | 364 (−2) |

- **판정**: 헤드룸은 실재하나 **어느 시스템을 언제 믿을지 가리는 신호가 없다.** 다섯 규칙의
  공통점은 전부 **생성 이후 신호**(득표·logprob·합의)를 봤다는 것 — 판별력 0.6 벽과 같은 원인.
- 산출: `remote/system_oracle.py`

## 실험 74: 오류 모드 균질성 대리 진단 — 텍스트를 뽑기 전에 전제부터 쟀다 (2026-08-17, GPU 0)

- **가설(추론 클러스터 SC)**: 지배적 오답 13.4표가 "하나의 잘못된 사고를 반복한 것"이라면,
  표본을 **답이 아니라 풀이 방식으로 묶어** 클러스터당 1표씩 주면 다수결이 뒤집힌다.
- **왜 대리 진단부터**: 풀이 텍스트 재생성은 GPU 45분이다. 텍스트 없이 기존 덤프의
  **길이·logprob 분포**만으로 "오답 쪽이 정말 더 균질한가"를 먼저 잴 수 있다.
- **설정**: 회수 가능 구간(SC 오답 & 정답 존재) 중 양쪽 3표 이상인 32문항, 문항 내 대응 비교.

| 지표 | 정답 표 | 지배오답 표 | 오답이 더 균질한 문항 |
|---|---|---|---|
| 풀이 길이 변동계수 | 0.1671 | **0.1871** | 15/32 (46.9%) |
| logprob 표준편차 | 0.0203 | **0.0223** | 12/32 (37.5%) |

- **판정: 음성 — GPU 미투입.** 가설은 오답 쪽이 **더 균질**해야 한다는 것인데 평균은 반대
  방향이고 대응 승률도 절반 이하다.
- ⚠️ **주장 범위를 좁혀 적는다**: 이 결과가 말하는 것은 **"지배 오답이 길이·확신도 차원에서
  더 균질하다는 증거를 찾지 못했다"**까지다. "서로 다른 추론이 우연히 같은 답에 도달했다"는
  증명되지 않았다 — 길이와 logprob은 추론 방식의 거친 대리 지표이고, 서로 다른 길이로 같은
  오류를 범할 수도 있다. 다만 **GPU 45분을 쓰지 않는다는 의사결정에는 충분한 음성 신호**다.
- 산출: `remote/diag_error_mode_homogeneity.py`

## 실험 75: 문제 유형 기반 specialist router — 상보성은 "전문화"가 아니었다 (2026-08-17, GPU 0)

- **왜 이 실험이 필요했나**: exp73에서 이종 계열 상보성(+14)이 노이즈 바닥(+4)의 3.5배로
  실재함을 확인했지만, 결합 규칙 5종이 전부 음수였다. 그 다섯은 전부 **생성 이후 신호**를 봤다.
  그러면 **모델의 확신을 아예 보지 않고 문제 텍스트만 보는 라우터**는 어떤가?
  이건 exp55(같은 모델의 후보 A/B + 투표 통계)와 특징 공간이 완전히 다르다.
- **판별 질문**: teacher가 자체 정확도는 낮은데 +9~10을 구제하는 것이,
  **(A) 무작위로 다른 문제를 맞히는 것**인가 **(B) 특정 유형에 실제로 강한 것**인가.

### 설계 — 시드 대응과 다중비교 보정을 처음부터 넣었다
유형 16종 × 계열 5종 = **65칸**이다. 자유도가 이만큼이면 좋아 보이는 칸은 반드시 나온다.
그래서 ① 계열별 시드 42/43/44를 **베이스의 같은 시드와 짝지어** 효용 `u_k(x) = s_k(x) − base_k(x)`를
재고 ② 나머지 문항 대비 차이의 **부호가 3시드에서 일치**하는지 보고 ③ **최대 통계량 순열검정**으로
우연 확률을 쟀다.

| 계열 | 자체 (평균) | 전체 구제/훼손 (3시드) | 최대 |차이| | 순열검정 p |
|---|---|---|---|---|
| teacher | 357.7 | [7,7,7] / [16,14,17] | 0.073 | 0.414 |
| rftmask | 357.7 | [8,5,7] / [13,15,18] | 0.103 | **0.102** |
| dpo | 365.7 | [7,5,3] / [3,4,10] | 0.022 | 0.801 |
| mt3072 | 363.7 | [4,4,5] / [10,3,8] | 0.044 | 0.154 |
| masked | 359.7 | [7,6,6] / [10,16,13] | 0.082 | 0.228 |

**어느 계열도 사전 등록 기준(p ≤ 0.10)을 넘지 못했다.** 부호 일치 칸은 여럿 나왔지만
대부분 **음수**다 — 그 계열이 그 유형에서 특별히 나쁜 게 아니라 전반적으로 나쁜 것의 잔향이다.

### 배포 검정 — 규칙 선택과 평가에 다른 시드를 썼다
칸 표를 눈으로 보고 고르면 그게 곧 선택 후 평가다(exp55c에서 +4가 −1.4로 뒤집힌 함정).
그래서 **선택 시드 1개에서 순효용 양수인 칸만 채택 → 나머지 두 시드에서 평가**(leave-one-seed-out).

| 채택 임계 | 홀드아웃 6회 평균 순변화 | 양수 |
|---|---|---|
| 칸 순효용 ≥ +1 | **−0.83문항** | 2/6 |
| ≥ +2 | **−0.33문항** | 2/6 |
| ≥ +3 | **−0.67문항** | 0/6 |

참고 상한(평가 시드에서 직접 최고 칸을 고를 때) +2.3문항/시드 — **이 격차가 곧 과적합량**이다.

### 유일하게 반복된 칸도 다중비교를 못 넘었다
`방정식/다항식`(26문항)만 세 선택 시드 전부에서, 그리고 학습 데이터가 서로 다른 두 계열에서
나왔다: rftmask 구제 [2,1,1]/훼손 [0,0,0], masked [2,2,1]/[0,0,0] — **6회 실행에서 훼손 0**.
합계 +9문항(시드당 +1.50).

그러나 **65칸 최댓값 기준 순열검정 p = 0.247**(3시드 모두 양수까지 요구해도 0.202)이다.
65칸을 훑으면 이 정도 칸은 우연으로 넷 중 하나꼴로 나온다. 이 칸만 쓰는 라우터의
홀드아웃이 6/6 양수로 보이지만, 이 칸 자체를 **모든 시드를 본 뒤에** 고른 것이므로
그 홀드아웃은 독립적이지 않다.

- **판정: 전문화 없음 — 시스템 라우팅 축 종료.** (A) 무작위 상보성 쪽이다.
- 설령 이 칸이 진짜여도 기대 이득은 시드당 1~2문항 = 0.2~0.4%p로,
  **리더보드 재실행 변동 실측 0.72%p(exp71)의 절반 이하**다. 최종 스택 변경 근거가 못 된다.
- 산출: `remote/diag_specialist_localization.py`, `remote/diag_specialist_router.py`,
  `remote/diag_equation_cell.py`

### 세 층위 종합 — 헤드룸은 다 있는데 배포 이득이 전부 0 이하
| 층위 | 오라클 헤드룸 | 배포 가능 이득 |
|---|---|---|
| 후보 (pass@32 87.8% vs 채택 75.8%) | +12.0%p | 0 (exp56 등) |
| top-1/top-2 (접전 267문항) | +4.14%p | −1.38문항 (exp55c) |
| 시스템 (합집합 400 vs 366) | +34문항 | −0.33 ~ −0.83문항 (exp75) |

**현재 발표용으로 정확한 문장**:
> 세 층위 모두에서 오라클 헤드룸을 확인했지만, 지금까지 시험한 **생성 이후 confidence·vote
> 기반 선택 규칙**에서는 배포 가능한 이득을 얻지 못했다. 이종 모델 계열은 동일 계열 시드보다
> 훨씬 큰 상보성(+14 vs +4)을 보였으나, **그 상보성이 문제 유형별 전문화에서 비롯된다는
> 증거는 찾지 못했다**(65칸 순열검정 p = 0.10~0.80, 홀드아웃 배포 순변화 음수).

## 실험 76: 계열 상관 인지 앙상블 — 왜 헤드룸이 있는데 투표로 못 꺼내는지 기제를 찾았다 (2026-08-17, GPU 0)

- **가설**: exp73의 결합 규칙 5종이 실패한 이유가 **35개 시스템을 독립 표로 취급**했기 때문일 수
  있다. `base_s42/s43/s44`는 사실상 한 가족이므로 세 표를 주면 **같은 오류를 3배 세게 세는 꼴**이다.
  exp73 대조군(같은 계열 +4 vs 이종 계열 +14)은 **독립 정보의 단위가 sample도 system도 아니라
  family**임을 시사한다. 그렇다면 시드를 계열 내부에서 먼저 합치고, 계열 간 상관으로 중복을
  제거한 뒤 투표하면 달라져야 한다.
- **설정**: 35개 덤프를 11개 계열로 접고(계열 내부는 표본 풀링 후 가중투표), 계열 간
  오답 일치율 행렬을 실측한 뒤 결합. **가중치는 5-fold CV로 학습 폴드에서만** 뽑았다.

| 계열 | base | dpo | mt3072 | temp | prompt | masked | rftmask | teacher | marginal | rank | rfs3 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 정확도 | 368 | 367 | 368 | 365 | 364 | 360 | 358 | 357 | 356 | 356 | 355 |

| 결합 규칙 (5-fold CV) | 정답 | 베이스 계열 368 대비 |
|---|---|---|
| 계열당 1표 (동등) | 364 | **−4** |
| 신뢰도 가중 | 364 | **−4** |
| 로그오즈 가중 | 364 | **−4** |
| 신뢰도 × 다양성 | 365 | **−3** |
| 신뢰도 × 계열 확신도 | 365 | **−3** |
| (참고) 덤프 수 비례 | 365 | **−3** |

### 진짜 발견 — 오답 일치율 0.60~0.91
두 계열이 **동시에 틀릴 때 같은 오답을 낼 확률**을 전부 쟀다.

- dpo ↔ prompt **0.91**, temp ↔ prompt 0.87, base ↔ mt3072 0.85, base ↔ prompt 0.84
- 가장 낮은 쌍조차 masked ↔ rank **0.60**

**오답이 계열을 가로질러 공유된다.** 이건 앙상블 이론의 전제(오류 비상관)가 정면으로 깨진
상황이고, 왜 63종 집계 규칙 + 시스템 결합 5종 + 계열 결합 6종이 전부 실패했는지를 설명한다.

### 회수 불가능성을 수치로 확정했다
베이스 계열이 틀렸는데 다른 계열 중 맞힌 곳이 있는 문항 = **16개**. 그 16문항에서:

| 정답을 낸 계열 수 | 문항 |
|---|---|
| 1개 | 8 |
| 2개 | 2 |
| 3개 | 1 |
| 4개 | 3 |
| 5개 | 2 |
| **평균** | **2.31 / 11계열** |

- **과반(6계열 이상)이 정답인 문항: 0개 (0.0%)**
- 같은 문항에서 **하나의 오답을 공유하는 계열 수는 평균 6.69개** — 정답 편의 **2.9배**

즉 회수 가능 구간에서 정답 편은 항상 소수파다. **표를 세는 방식(어떤 가중치를 쓰든)으로는
구조적으로 질 수밖에 없다.** exp73의 합집합 오라클 +14는 "어느 하나라도 맞히면"이라는
oracle 접근이었고, 그 정보를 **투표로는 꺼낼 수 없다는 것이 여기서 증명된다.**

- **판정: 앙상블·투표 축 최종 종료.** 남은 길은 표를 세는 것이 아니라 **소수 정답을 식별하는
  판별기**인데, 그 판별력은 일곱 신호에서 0.59~0.66 벽에 걸려 있다(exp55c 등).
- 산출: `remote/diag_family_ensemble.py`

## 실험 77 / 79: 디코딩 개입 축 개시 — 전제는 맞는데 규칙이 틀렸다 (2026-08-19, GPU 0)

지금까지 모든 시도는 **완성된 풀이를 놓고 고르는 것**이었고, exp76에서 그게 왜 안 되는지
기제가 나왔다(계열 간 오답 일치율 0.60~0.91). 새 축은 방향이 반대다 —
**고르는 걸 포기하고 공통 오답이 만들어지기 전에 개입한다.**

사전등록: `experiments/prereg_exp77_decoding.md` (**코드 실행 전 작성**, 중복 확인·성공/중단 조건 포함).

### ⚠️ 먼저 자기 정정 — exp62의 "CCD 최종 배제"는 과한 표현이었다
사용자가 1순위로 지목한 CCD는 **arXiv 번호까지 같은 논문**(2602.18232)이고 exp62에서 다뤘다.
그런데 exp62가 실제로 잰 것은 (a) **저확신 토큰의 위치 분산**이 균등(0.276~0.289 vs 균등 0.29)이라
"오류가 특정 구간에 국소화된다"는 **내가 세운 파생 전제**가 깨진다는 것과,
(b) 토큰 확신도를 **투표 가중치**로 쓰면 최대 ±1문항이라는 것이다.

**둘 다 CCD의 핵심 기제(저확신 지점에서의 contrastive logit 교정)를 직접 시험한 것이 아니다.**
CCD는 저확신 토큰이 뭉쳐 있을 필요가 없다 — 흩어져 있어도 각 지점에서 개입할 수 있다.
정확한 상태는 **"CCD 배제"가 아니라 "국소화 전제 불성립 + 토큰 확신도는 선택 신호로 약함"**이고
**대조 개입 자체는 미검증**이다. 다만 exp62는 CCD의 **비용**을 확정한다: 저확신 토큰이 10.4~12.4%,
풀이 중앙값 388토큰이므로 **개입 지점이 풀이당 약 40~48개**이고 전 구간에 흩어져 있다.

### 실험 77 — First Finish Search (arXiv:2505.18149) 오프라인 진단

n개 흐름을 병렬로 띄우고 **가장 먼저 끝난 것을 그대로 채택**하는 규칙. exp06c(최단 풀이 **학습**)와
다른 **추론 규칙**이며, 집계 규칙 63종 중 길이를 쓴 것은 `drop_trunc`뿐이라 미탐색이 맞다.

| n | FFS | 무작위 1개 | 가중 SC | FFS − 현행(366.3) |
|---|---|---|---|---|
| 2 | 339.0 | 335.1 | 339.5 | **−27.3** |
| 4 | 339.4 | 335.1 | 351.1 | **−26.9** |
| 8 | 337.8 | 335.1 | 358.7 | **−28.5** |
| 16 | 335.0 | 335.2 | 362.8 | **−31.3** |
| 32 | 332.3 | 334.0 | 366.3 | **−34.0** |

세 시드 전부 음수, 부호 일관. **기각.**

#### 그런데 논문의 전제는 우리 데이터에서 성립한다
| | 정답 표본 | 오답 표본 |
|---|---|---|
| 평균 생성 토큰 수 | **385** | **819** |
| 문항내 대응(정답이 더 짧은 문항) | 61.0~68.6% (3시드) | |

**"짧은 추론이 더 정확하다"는 전제는 맞다. 그런데 규칙은 실패한다.** 왜냐하면
**최솟값은 꼬리 통계량**이기 때문이다 — 32개 중 가장 짧은 것은 분포의 극단이고, 거기에는
중간에 무너져 짧게 끝난 오답이 모인다. 실제로 n이 커질수록 FFS가 **더 나빠진다**(−27 → −34):
표본을 늘릴수록 최솟값이 더 극단으로 간다. 길이 신호는 **분포 이동**이지 표본별 규칙이 아니다.

시드42 n=32 단일 표본 채택: **최단 329 / 무작위 334 / 최장 313** — 최단이 무작위보다도 낮다.
(길이를 **가중치·필터**로 쓰는 방향은 exp44의 `길이trim`에서 이미 76.4% 상한에 걸렸다.)

### 실험 79 — Prefix-Confidence (arXiv:2507.18122) 오프라인 검증

exp62와 묻는 것이 다르다. exp62는 확신도에게 "A가 맞나 B가 맞나"를 물었고(AUC 0.610),
여기서는 **"어느 시작점에 예산을 더 쓸까"**만 묻는다 — 요구 판별 난도가 더 낮다는 것이 가설이었다.

`results/val_tok_s42.jsonl`(토큰 단위 logprob 483×32)로 **거의 완전한 시뮬레이션**이 가능했다.
우리 32개 표본은 같은 온도의 독립 완전 흐름이므로, 앞 P토큰을 접두로 보고 k개 중 최고를 골라
**그 표본의 최종 답을 채택**하면 실제 방법과 같은 일이 된다.

| 신호 | 문항내 AUC |
|---|---|
| 앞 8 / 16 / 32 / 64 / 128 토큰 | 0.506 / 0.494 / **0.486** / 0.510 / 0.515 |
| 풀이 전체 평균(현행) | 0.586 |

**접두에는 신호가 없다 — 32토큰 접두는 0.486으로 우연 이하다.** 정확도로 옮겨도 같다:

| k | 접두32 선택 | 접두128 선택 | 무작위 1개 | 가중 SC |
|---|---|---|---|---|
| 8 | 334.8 | 335.3 | **334.5** | 357.5 |
| 32 | 337.0 | 334.0 | 336.0 | 366.0 |

**접두 최고를 고르는 것과 아무거나 고르는 것이 구별되지 않는다.** 사전 등록 기준(AUC 0.55) 미달 → **기각.**

#### 기제 — 판별 신호는 풀이 **끝**에서만 생긴다
| 토큰 구간 | 앞에서 | 뒤에서 |
|---|---|---|
| 8토큰 | 0.506 | **0.578** |
| 32토큰 | 0.486 | 0.549 |
| 128토큰 | 0.515 | **0.581** |
| 풀이 전체 | 0.586 | |

**마지막 8토큰만으로도 AUC 0.578로 풀이 전체(0.586)에 거의 도달한다.** 반면 앞 128토큰은 0.515다.
접두 확신도의 문항내 표본 간 표준편차는 오히려 더 크다(앞8 0.058 vs 전체 0.023) —
**접두가 서로 비슷해서 신호가 없는 게 아니라, 다른데 그 차이가 순수 노이즈**다.

Prefix-Confidence는 **정보가 아직 존재하지 않는 지점에서 예산 결정을 요구**한다.
이 모델의 판별 정보는 답을 확정하는 마지막 순간에야 생긴다.

### 이 두 실험이 남은 축에 주는 함의
- **CCD(exp78)**: 개입 지점이 풀이 전체에 흩어져 있고(exp62), 저확신 위치의 판별력도 낮다.
  그래도 **개입 자체는 미검증**이므로 우선순위 1위는 유지한다.
- **exp80(실패 체크포인트를 anti-expert로)**: exp77·79와 달리 **선택 신호에 의존하지 않는다.**
  오답 일치율 0.60~0.91이라는 우리 실측을 직접 자원으로 쓰는 유일한 후보다.

- 산출: `remote/diag_first_finish.py`, `remote/diag_prefix_confidence.py`,
  `experiments/prereg_exp77_decoding.md`

## 실험 82: 8/31 최종 제출 신뢰성 감사 — 실측으로 치명 결함 2건을 찾았다 (2026-08-19, GPU 0)

성능 실험이 아니라 **운영** 감사다. 지금 상태에서 +1문항을 더 찾는 것보다,
366문항짜리 시스템이 당일 사고로 0점이 되는 경로를 없애는 기대값이 훨씬 크다.
모델 추론은 한 번도 실행하지 않았고 전부 기존 코드 분석 + mock/dry-run으로 검증했다.

### 심각도 1 — int64를 넘는 답이 실제로 투표에서 이긴다

`make_submission_from_dump.py`는 마지막에 `sub['answer'].astype('int64')`를 한다.
그런데 **우리 덤프에 int64를 넘는 답이 실재하고, 규칙이 그것을 채택하기도 한다.**

| 실측 | 값 |
|---|---|
| 전체 표본 | 795,682개 |
| int64 초과 답 | **582개 (0.073%)**, 최대 **2,937자리** |
| n=32에서 규칙이 그 값을 **채택한** 사례 | **6건** |
| n=16으로 줄이면 (시간 부족 시 런북이 권장하는 경로) | **66건** |

원인은 잘린 풀이(1.7~1.9%)에서 추출기가 마지막 숫자를 주워 오는 경로다.
결과는 **마지막 단계에서의 크래시(2.5시간 생성 직후) 또는 값 손상** 둘 중 하나다.

**수정**: `apply_rule_safe()` 신설 — 채택된 답이 범위를 벗어나면 **범위 안 후보만으로 재집계**하고
그 문항을 로그에 남긴다. 2,000자리 정수는 어떤 경우에도 정답이 아니므로 이 대체는 손해가 없다.

### 심각도 1 — 재개 시 파라미터가 달라도 조용히 섞인다

`dump_lb_samples.py`의 재개는 `done` 목록만 본다. 진행 파일에 **파라미터 지문이 없었다**
(`keys=['done']`만 확인). 즉 당일 2시간 30분짜리 작업이 중단돼 명령을 다시 타이핑할 때
`--n`·`--seed`·`--temp`·`--adapter` 중 하나만 달라져도 **앞뒤 설정이 다른 덤프**가 만들어지고
그 사실이 어디에도 남지 않는다.

**수정**: 지문(`n/seed/temp/top_p/max_tokens/adapter/test파일명/limit`)을 저장하고 재개 시 대조,
불일치면 **어느 값이 달라졌는지 출력하며 거부**한다(`--force-resume`으로만 강행).
시간 부족으로 `--n 32→16`을 낮추는 것은 정당한 예외이므로 메시지에 그 경로를 명시했다.

### 심각도 2 — 청크 중간 사망 시 중복 기록 / 컬럼명 / 표본 부족

- 청크 도중 죽으면 jsonl에는 쓰였는데 `done`에는 없어 **같은 문항이 두 번 기록**된다.
  → 재개 시 jsonl을 읽어 `done`을 **자동 보정**하도록 수정. (기존 덤프 7개에는 중복 0건 확인)
- 8/31 test 파일은 처음 보는 파일이다. 컬럼이 `id`/`question`이 아니면 KeyError로 죽는다.
  → `ID/Id/problem_id`, `Question/problem/text` 등 **자동 매핑 + 명확한 에러**, 중복 id 사전 차단.
- `--n 32`인데 어떤 문항의 표본이 8개뿐이어도 조용히 8개로 집계됐다. → **경고 출력**.

### 신설 — 제출 전 전수 검증기와 dry-run

문서의 체크리스트를 **사람 눈이 아니라 코드**로 옮겼다. 눈으로 보는 검증은 이미 한 번 실패했다
(2026-08-15, 811개가 0인 파일이 행수·결측·중복 검사를 전부 통과).

`remote/validate_submission.py` (GPU·pandas 불필요) — 헤더 소문자 `id,answer` / 행 수와
**id 집합 정확 일치** / 중복·결측 / int64 범위 / 0 비율 / 덤프 커버리지·표본 수 / 추출실패·잘림률.

**임계값을 실측으로 보정했다.** 처음에는 "0이 2% 초과면 실패"로 뒀는데 **0.78459점을 받은 실제
제출 파일이 실패로 나왔다.** 제출 25건을 전수 조사하니:

| | 0인 답 비율 |
|---|---|
| 정상 제출 24건 | **2.05 ~ 3.25%** (중앙 2.77%) |
| 2026-08-15 사고 파일 | **97.71%** |
| 학습셋 gold 자체 | 1.31% (0은 정당한 답이다) |

→ 경고 4.5% / 실패 8%로 재설정. 정상 파일 통과·사고 파일 차단을 각각 재확인했다.

`remote/dryrun_submission_path.py` — 병리 입력 17종을 일부러 주입해 가드가 실제로 작동하는지 본다
(거대 정수 다수결, 전부 거대, int64 경계값, 대문자 헤더, 행 수 부족, id 불일치, 0 대부분,
중복 id, 부분 덤프, 표본 부족). **17/17 통과.** GPU·vLLM·pandas 없이 수 초에 끝난다.

### 그 밖에 확인하고 문제가 없었던 것

| 항목 | 실측 | 판정 |
|---|---|---|
| 답 추출 실패율 | 0.001% (79,776 표본 중 1건) | 양호 |
| 잘림 비율 | 1.73~1.90% | 양호 |
| 덤프 중복 id | 7개 파일 전부 0건 | 양호 |
| 문항당 표본 수 균일성 | 전부 균일 | 양호 |
| 디스크 | 2,000문항 n=32 기준 약 **5~14MB** | 무시 가능 |
| 2,000문항 소요 | 4.44초/문항 → **2시간 28분** (exp30 실측) | 런북 유효 |

### 부수 — `make_submission.py` 오사용 차단
체크포인트가 없어 최종전에 쓰면 안 되는데 문서에만 적혀 있었다.
→ **기본적으로 실행 거부**하고 `--i-know-no-checkpoint` 로만 진행되게 했다.

- 산출: `remote/validate_submission.py`, `remote/dryrun_submission_path.py`,
  `remote/make_submission_from_dump.py`(수정), `remote/dump_lb_samples.py`(수정),
  `remote/make_submission.py`(수정), `docs/final-pipeline.md`(갱신)

## 실험 80a: 실패 체크포인트를 anti-expert로 쓰는 대조 디코딩 — NO-GO (2026-08-19, GPU)

exp76에서 계열 간 **오답 일치율 0.60~0.91**을 실측했다. 실패한 체크포인트 안에 base가 반복해서
빠지는 공통 오류 모드가 들어 있다는 뜻이다. 앙상블에서는 약점이었던 그 성질을 **빼는 방향**으로
쓰면 교정 신호가 되지 않을까 — 문헌 그대로가 아니라 **우리 실측에서 나온 가설(H35)**이다.

    score(t) = (1+λ)·log p_base(t) − λ·log p_amateur(t),   plausible = {t : p_base(t) ≥ α·max p_base}

### 준비 단계에서 함정을 하나 걸렀다 — amateur를 정확도로 고르면 안 되고, 유사도로도 안 된다

사전등록한 지표는 `A(s) = wrongAgreement(base,s) − 2·rescueRate(s)` 였다. 그런데 A 최고는
**dpo(A=0.797)**인데 dpo는 base와 답이 **24/483(5.0%)만 다르다.** 그러면

    log p_base − λ·log p_dpo ≈ (1−λ)·log p_base

가 되어 **사실상 온도 조절**이다. 새 방법을 시험한다면서 온도만 바꾸는 실험이 될 뻔했다.
그래서 `contrast ≥ 8%`(base와 답이 다른 문항 비율)를 사전 조건으로 **추가**했다.

| 후보 | 정확도 | wrongAgree | rescue율 | contrast | A(s) | |
|---|---|---|---|---|---|---|
| dpo | 367 | 0.832 | 0.017 | **5.0%** | 0.797 | 대조 여지 부족 → 제외 |
| **marginal** | **356** | 0.717 | **0.017** | **9.9%** | 0.682 | **선정** |
| teacher | 361 | 0.796 | 0.061 | 8.9% | 0.675 | rescue 큼 → 제외 |
| masked | 360 | 0.633 | 0.052 | 12.4% | 0.529 | |

또 하나 — `prompt`·`temp`·`mt3072`는 **별도 모델이 아니다**(베이스 + 프롬프트/샘플링 변형).
amateur로 쓸 수 없다. (다만 `prompt`는 MTI의 negative-prompt 후보로는 유효하다.)

### plausibility 제약은 필수임을 로컬에서 실증했다
`(1+λ)log p_base − λ log p_amateur` 형태는 base 항이 증폭돼 **약한 편향에는 스스로 버틴다.**
그러나 amateur가 어떤 토큰을 극단적으로 싫어하면 제약 없이는 그 토큰이 1위가 된다(순수 파이썬으로 확인).
→ α=0.1 고정. **⛔ 이 제약 없이 실패했다면 CD 가설을 기각할 수 없다**는 사전 조항은 충족됐다.

### 구현 검증 — λ=0 대조군이 base를 정확히 재현
같은 코드 경로에서 λ=0으로 돌리면 base 스택으로 환원된다. 스모크 3문항에서 답(120/2/2125)·
logprob 대역·길이가 모두 일치했다. **따라서 비교는 vLLM 덤프가 아니라 λ=0과 한다**(exp58 설계).

### 결과 — 층화 60문항(회수가능 24 / 저마진 18 / 고마진 18), n=8

| 구간 | 문항 | λ=0 | λ=0.5 |
|---|---|---|---|
| 회수가능 | 24 | 3 | 2 |
| base정답·저마진 | 18 | 9 | 9 |
| base정답·고마진 | 18 | 18 | 18 |
| **전체** | **60** | **30** | **29 (−1)** |

회수 가능 구간의 득표 지분 (24문항, λ=1.0은 시간 예산으로 이 구간만 완료):

| | λ=0 | λ=0.5 | λ=1.0 |
|---|---|---|---|
| gold 득표 지분 | 0.1139 | **0.1105 ↓** | **0.1333 ↑** |
| 지배오답 지분 | 0.5448 | **0.5647 ↑** | **0.5285 ↓** |
| 고유 답 개수 | 4.08 | 3.62 | 3.71 |

**판정: 두 λ 모두 NO-GO. 483문항 확대는 하지 않는다.**

### 읽을 때 조심할 것 — λ=1.0의 "옳은 방향"을 신호로 읽지 않는다
λ=1.0은 지분 조건을 만족한다(gold ↑, 지배오답 ↓). 그러나 **λ=0.5는 정확히 반대 방향**이었다.
실제 기제가 있다면 λ에 대해 단조여야 한다. 24문항 × n=8에서 지분 차이 0.019는 표본 노이즈와
구분되지 않으며, **비단조성 자체가 노이즈라는 증거**다. 순이득은 두 λ 모두 −1이다.

### 부수 발견 — 뺄셈이 다양성을 줄인다
고유 답 개수가 4.08 → 3.62(λ=0.5)로 줄었다. amateur가 base의 약화판이므로 빼면 base 자신의
선호가 증폭되고, 그 안에는 **지배적 오답도 포함**된다. exp69(sharpening이 항등원)의 연장선이며,
여기서는 항등원보다 나쁘다 — 오답 모드를 강화한다.

- 산출: `remote/cd_anti_expert.py`, `remote/select_amateur.py`, `remote/analyze_cd_pilot.py`,
  `experiments/prereg_exp77_decoding.md`

## 실험 83: 수정된 제출 경로 GPU 리허설 — 9/9 통과 (2026-08-19, GPU 약 12분)

exp82에서 고친 코드(int64 가드·파라미터 지문·자동 보정·컬럼 매핑·검증기)를 **GPU 경로로 실제
통과**시켰다. 8/31에 처음 실행되는 코드가 있어서는 안 되기 때문이다. 831문항을 다시 태우지 않고
(생성 코드는 안 바뀌었고 exp30이 처리량을 실측했다), **바뀐 로직만 40문항 × n=8로 전부 밟았다.**

| 검사 | 결과 |
|---|---|
| 파라미터 지문이 progress에 기록되는가 | ✅ |
| 다른 `--n`으로 재개 시 거부하는가 | ✅ 불일치 항목 `{'n': (8, 16)}`을 정확히 출력하며 거부 |
| 같은 설정 재개 시 재생성 없이 종료하는가 | ✅ |
| 청크 중간 사망 모사(progress에서 5건 삭제) 시 자동 보정하는가 | ✅ 중복 기록 0 |
| 부분 덤프(40/831)를 기본 거부하는가 | ✅ |
| 정상 경로에서 제출 파일이 생성되는가 | ✅ |
| 검증기 통과 (종료 코드 0) | ✅ |
| `make_submission.py` 오사용 차단 | ✅ |

**판정: 9/9 통과.** 8/31 제출 경로는 이제 "고쳤다고 믿는 코드"가 아니라 **실행으로 검증된 코드**다.
- 산출: `remote/rehearsal_fixed_pipeline.sh`, 서버 `chain2.log`

## 실험 78a: MTI (Minimal Test-Time Intervention) — 디코딩 개입 축 종료 (2026-08-19, GPU 38분)

- **가설(arXiv:2510.13940, ACL 2026)**: 고엔트로피 토큰에서만 selective CFG를 적용하면
  추론이 개선된다. `log P̂ = (1−ω)·log P(x|c̄) + ω·log P(x|c)`, c̄ = "OUTPUT ERROR" 주입 분기.
- **구현**: vLLM 미수정 — exp80a에서 검증된 HF 디코딩 루프 재사용, 무조건 분기는 KV 캐시에
  부정 문구만 덧붙여 forward 후 crop(개입 안 하는 위치 추가 비용 0).
  **하이퍼파라미터는 논문 값 그대로**(ω=1.5, τ∈{1.5, 0.5}) — 검증셋 탐색 없음.

### 먼저 나온 사실 — 우리 모델은 논문 모델보다 엔트로피가 절반이다
| | 논문 (long-CoT 추론 모델) | 우리 (Qwen2.5-3B short-CoT) |
|---|---|---|
| τ=1.5 개입 비율 | 4.2% | **2.3%** |
| τ=0.5 개입 비율 | 31.2% | **16.9%** |

exp62(저확신 토큰이 국소화되지 않음)와 합치면 그림이 완성된다: short-CoT instruct 모델은
**개입할 "불확실한 갈림길" 자체가 드물다.**

### 결과 — 층화 60문항 × n=8, 대조군 = exp80a의 λ=0 (같은 문항·같은 샘플링)
| 구간 | 대조 | MTI τ=0.5 | MTI τ=1.5 |
|---|---|---|---|
| 회수가능 (24) | 3 | 2 | 1 |
| 저마진 (18) | 9 | 8 | 11 |
| 고마진 (18) | 18 | 18 | 18 |
| **전체 (60)** | **30** | **28 (−2)** | **30 (±0)** |

| 회수구간 지분 | 대조 | τ=0.5 | τ=1.5 |
|---|---|---|---|
| gold | 0.1139 | 0.1145 | 0.1337 |
| 지배오답 | 0.5448 | 0.5509 | **0.5560 ↑** |

- τ=0.5 (개입 16.9%): 전체 −2, harm 3 (기준 ≤2 초과) → **NO-GO**
- τ=1.5 (개입 2.3%): 전체 ±0. gold 지분은 올랐으나 **지배오답 지분도 올라** 조건(gold↑ AND
  지배오답↓) 미달. 개입이 2.3%뿐이라 사실상 항등원에 가깝다 → **NO-GO**
- ⚠️ 대조군은 별도 스크립트 실행이라 완전 동일 실행 대조는 아니다(같은 층화·설정, 배치 구성만 다름).

### 디코딩 개입 축 종합 — 사전 계획대로 축을 닫는다
| | 방식 | 결과 |
|---|---|---|
| exp77 FFS | 가장 먼저 끝난 표본 채택 | −27~−34 (전제는 성립, 최솟값이 꼬리 통계량) |
| exp79 Prefix-Confidence | 유망한 시작점에 예산 집중 | 접두 AUC 0.486~0.515 (신호는 풀이 끝에만) |
| exp80a Anti-expert CD | 실패 LoRA 분포를 뺄셈 | λ 비단조, 다양성만 감소 |
| exp78a MTI | 고엔트로피 위치 selective CFG | −2 / ±0, 개입 지점 자체가 드묾 |

**공통 기제**: 이 모델의 오답은 "저확신 갈림길에서 잘못 꺾인 것"이 아니다. exp62(저확신 비국소화)
· exp79(판별 신호는 끝에만) · exp78a(고엔트로피 토큰 2.3%)가 모두 같은 방향이다 —
**short-CoT 3B의 오답은 확신에 찬 채로 처음부터 잘못된 길을 간다.** 그래서 "불확실한 순간에
개입"하는 계열 전체가 개입할 지점을 찾지 못한다. 사전 계획("여기까지 전부 실패하면 디코딩
개입 축도 닫는다")에 따라 **축 종료.**
- 산출: `remote/mti_decode.py`, `results/mti_pilot.jsonl`(서버·로컬)

## 실험 84: EDAS 오프라인 진단 — 모델 탐색 종료 선언 (2026-08-19, GPU 0)

- **가설(arXiv:2605.17333)**: RLVR에서 오답 롤아웃을 최종 답으로 묶어 **반복되는 지배 오답에는
  벌점을 증폭하고 희소한 탐색적 오답은 완화**하면(EDAS) 학습이 개선된다. 우리 최강 실패 현상
  (오답 일치율 0.60~0.91, 지배 오답 반복)을 정면으로 겨냥하는 마지막 미탐색 후보였다.
- 사전등록: prereg_exp77_decoding.md exp84 절 — **수식 원문·게이트 G1/G2를 실행 전에 고정**했고,
  수식만 보고 예상되는 구조(S=0 문제)도 예단이 아닌 검증 대상으로 미리 적어뒀다.

### [G1] 전원-오답 그룹 회복 — **실패 (0/59, 수식이 예측한 그대로)**

    S = (1/Nw)·Σ|Aᵢ_orig|   ← EDAS의 동적 스케일이 **기존 advantage 크기에 비례**

이진 보상 GRPO에서 전원-오답 그룹은 그룹 정규화로 A_orig가 전부 0 → S=0 → Δ=0 →
클리핑 상한 |A_orig|/κ도 0. **EDAS는 그래디언트가 없던 곳에 그래디언트를 만들지 않는다.**
59그룹 전부, 정규화 방식(std/평균중심)과 무관하게 회복 0. 논문이 DAPO의 dynamic sampling
(전원 동일 보상 그룹 **제거**)과 함께 쓰이는 이유가 이것이다 — **exp72가 멈춘 지점("정답 없는
그룹의 신호는 못 쓴다")을 EDAS는 해결하는 게 아니라 우회한다.** 제안 시의 기대("그 gradient를
어떻게 설계해야 하는가를 파고든다")는 혼합 그룹으로 좁혀 읽어야 한다.

### [G2] 혼합 그룹 재분배 — 통과 (방향 일치 130/150 = 86.7%)
| K>1 혼합 150그룹 | 배율 |
|---|---|
| 지배 오답 클래스 벌점 | ×**1.078** (증폭) |
| 희소 오답 클래스 벌점 | ×**0.915** (완화) |
| 회수 구간 53그룹만 | ×1.095 / ×0.921 (지배오답 평균 11.4표) |
| 정답 궤적 변화 | **0건** (논문 명세대로 불변) |

구현은 논문 의도대로 작동한다. 그러나 크기가 **±8~10%** — κ=2 클리핑 상한(±50%)에도 한참
못 미치는 미세 재분배다.

### 판정 — 사전등록 게이트대로 GPU 미투입, 축 종료
게이트는 **G1과 G2 모두**였다. G2만 통과 = **이미 존재하던 그래디언트의 ±8~10% 재분배**인데,
exp12/24는 바로 그 그래디언트로 1500스텝을 돌려도 보상 곡선이 평평했다. 재분배로 평평한
곡선이 서리라 기대할 근거가 없다.

### 부수 발견 — "동일 오답 반복"이 사는 곳이 특정됐다
전원-오답 59그룹의 오답 클래스 수: **K=1은 3그룹뿐**, 중앙값 K≈14, 최대 31.
즉 모델이 완전히 실패하는 문항에서는 오답이 **넓게 흩어진다**(exp74의 비균질성과 일치).
지배 오답 반복은 **혼합 그룹**(회수 구간, 지배오답 11.4표)과 **시스템 간**(exp76, 일치율
0.60~0.91)에서 일어난다. 한 문장으로: **풀 줄 모르면 흩어지고, 알 듯하면 같은 곳으로 틀린다.**

### 모델 탐색 종료 선언
사전 합의된 기준("EDAS까지 음성이면 8/31 전 합리적 모델 실험은 충분히 했다")에 도달했다.
학습 축 8전 + 디코딩 개입 4종 + EDAS 진단까지, 이후 탐색은 성능이 아니라 연구 주제 탐색이다.
DCO(arXiv:2502.07154)는 pass@N을 올리는 목적함수인데 우리 병목은 coverage(87.8%)가 아니라
선택이므로 **10월 논문 후속 연구 후보로만** 남긴다. Reasoning Arena는 판별기(0.6 벽) 재등장,
EVOL-RL은 majority 앵커(우리는 majority 자체가 오답으로 잠김)라 배제.
- 산출: `remote/diag_edas.py`, prereg exp84 절

## 실험 85: CDG + Consilience — 현상은 재현되나 신호는 tail의 재포장 (2026-08-19, GPU 0)

- **가설**: 확신도의 **변화량** ΔC = C_tail − C_head가 정오 신호다. CDG(arXiv:2605.25244,
  ICML 2026)와 Consilience(arXiv:2608.09898)가 독립적으로 같은 현상을 보고 — 좋은 추론은
  "초반 탐색(저확신) → 후반 수렴(고확신)"이고, 처음부터 균일하게 확신하면 confidently-wrong.
- **exp62/79와의 경계**: exp62는 절대값 가중(±1문항), exp79는 head/tail **각각**의 판별력.
  **변화량과 count^0.5 집계는 미탐색**이었다. 특히 exp79의 head AUC 0.486(0.5 아래 = 확신 높을수록
  오답 방향)은 이 논문들의 전제와 부호가 일치했으므로 확인할 가치가 실재했다.
- 사전등록: prereg exp85 절 — 게이트 G1/G2, "head가 순수 노이즈면 빼기가 tail 단독보다 나빠질
  가능성"까지 실행 전에 명시.

### [1] 현상은 실재한다
ΔC(10%) 평균: **정답 0.0958 vs 오답 0.0758**, 차이의 문항 부트스트랩 95% CI **[0.0114, 0.0297]**
(0 제외, 212문항). 논문들이 관찰한 "정답 궤적은 확신이 더 오른다"가 우리 short-CoT 3B에서도 성립.

### [2] 그러나 선택 신호로는 tail 절대값보다 약하다 — G1 실패
| 신호 | AUC |
|---|---|
| head 10/20/25% | 0.484 / 0.506 / 0.512 |
| **tail 10/20/25%** | 0.544 / **0.587** / 0.577 |
| **ΔC 10/20/25%** | 0.554 / 0.565 / 0.560 |
| Consilience S | 0.544 |
| 전체 평균(현행) | 0.586 |

max AUC(ΔC계열) 0.565 < max AUC(tail 단독) 0.587. **head를 빼는 것이 노이즈만 추가한다** —
사전등록에 적어둔 두 가능성 중 나쁜 쪽이 실현됐다. ΔC는 여덟 번째로 0.6 벽 아래 신호가 됐다.

### [3] 집계 — 논문 기본값 그대로, 사후 최적화 없음
| 규칙 | 정답 (기준 366 대비) |
|---|---|
| count^0.5·C̄ (√count 효과 분리 대조) | 366 (+0) |
| CDG (P=10, α=0.5, β=1) | 366 (+0) |
| Consilience (α_c=1) | **274 (−92)** |
| Consilience × count^0.5 | 349 (−17) |

Consilience 원식(count 미사용)은 −92로 붕괴 — 득표 수를 버리면 처참하다는 것 자체가
우리 데이터에서 **다수결이 담고 있는 정보량**의 방증. nested CV 배포 순이득 **+1문항**
(5폴드 일관되게 cdg β=0.5 선택) — exp62의 "마지막 128토큰 +1"과 같은 자리다.

### 판정 — G1 실패 → confidence dynamics 축 종료 (사전등록 규칙)
G2(+1>0)는 형식상 통과지만 게이트는 **G1 AND G2**였다. 논문들의 현상은 진짜지만 long-CoT
(수천 토큰의 탐색 구간)에서 유도된 방법이고, 우리 중앙값 388토큰 short-CoT에서는 변화량의
동적 범위가 작아 절대값을 넘어서지 못한다 — exp61(답 공간)·exp62(추론 길이)에 이은
**세 번째 모델 계열 외삽 실패 사례**. seed 42 단일이지만 G1은 방향 자체가 반대라 시드 확장 불필요.
- 산출: `remote/diag_confidence_dynamics.py`, prereg exp85 절

## 실험 86 / 86b: PPV 추론 기하 — 전제는 참인데 변환이 안 된다 (2026-08-19, GPU 약 35분)

- **가설(arXiv:2606.08098, PPV)**: 오답 다수파는 추론 공간에서 incoherent하고 정답 소수파는
  tight하다 — 그렇다면 위임(delegation)으로 다수결을 뒤집을 수 있다.
- **exp74 재해석이 출발점**: exp74의 "오답이 길이·logprob에서 더 흩어짐"은 클러스터링 가설에는
  음성이지만 **PPV 전제와는 부합 방향**이었다. 잰 적 없는 것은 추론 내용의 기하(임베딩)였고,
  지금까지 실패한 8개 신호가 전부 출력 통계였으므로 이것은 다른 공간이었다.
- 설계: 회수 가능 43문항 × 32표본 텍스트 재생성(vLLM, 같은 설정) → Qwen 자신의 은닉 상태
  평균 풀링 임베딩 → 답내 코사인. 사전등록 G1 + 길이·노이즈 통제.

### exp86 — G1 통과: 기하 전제는 실재한다 (**이 프로젝트 첫 양성 게이트**)
| | 답내 평균 코사인 |
|---|---|
| gold 소수파 (평균 5.0표) | **0.9788** |
| 지배오답 (평균 13.2표) | 0.9734 |
| 교차 (G↔D) | 0.9736 |

- gold가 더 tight한 문항 **18/27 = 66.7%** (기준 ≥60%), 평균 차이 +0.0055,
  부트스트랩 95% CI **[0.0011, 0.0107]** (0 제외)
- 길이 통제: 이 구간에선 길이가 비슷했고(639 vs 622), 대역 [200,700] 제한에서도 70.6%로 부호 유지
- **결정적 세부**: 지배오답 내부(0.9734) ≈ 교차(0.9736). **지배오답은 클리크가 아니다** —
  gold만 약한 클리크다. PPV의 dominant-set 동역학이 정확히 좋아하는 구조.

### exp86b — 그러나 위임이 그것을 점수로 바꾸지 못한다
같은 43문항에서 실제 집계 (γ는 사전 고정 3값, 사후 탐색 아님):

| 방법 | 정답/43 | 구제/훼손 |
|---|---|---|
| weighted(현행) | 1 | — |
| 응집도 가중 γ=1 / 10 | 1 / 1 | 1/1 · 1/1 |
| 응집도 가중 γ=50 | **2** | 2/1 |
| **replicator dynamics** | 1 | 1/1 |

**가장 유리한 구간(회수 가능만, harm측 미포함)에서 최대 +1/43.** 기제도 명확하다 —
유사도 바닥이 전역 ~0.97이라 그래프가 거의 균일하고, gold 클리크의 응집 우위(+0.005)는
득표 열세(5.0 vs 13.2)를 절대 못 넘는다. replicator dynamics조차 크기가 지배한다.
γ=50에서도 구제 2에 훼손 1 — 유일하게 기준선이 맞히던 문항을 뒤집었다.

### 판정 — 확장하지 않는다 (사전등록의 "확장 논의"에 대한 답)
전체 배포 낙관 상한: 구제율 2/43 ≈ 회수 구간 4.7% ≈ 전체 +2.7문항 = +0.56%p < LB 재실행 변동
0.72%p. 게다가 harm측(base정답 366문항)은 아직 재지도 않았는데 이 구간 안에서 이미 훼손이
나왔다. 대조 텍스트 덤프에 GPU를 더 쓰지 않는다.

### 아홉 번째 벽 — 이제 세 개의 신호 공간에서 같은 자리다
| 신호 공간 | 대표 값 |
|---|---|
| 출력 통계 (7종) | AUC/정확도 0.59~0.66 |
| 확신도 동역학 (ΔC, exp85) | AUC 0.565 |
| **추론 기하 (코사인, exp86)** | **승률 66.7%** |

전부 "실재하지만 0.6대"다. 논문 관점에서 이것이 이 프로젝트의 최종 형태다:
**세 층위의 오라클 헤드룸(후보·top2·시스템) × 세 공간의 판별 신호(통계·동역학·기하)가
모두 실재하고, 모두 배포 이득으로 변환되지 않는다.** 기제는 각각 규명됐다 —
오답 상관(0.60~0.91), 신호는 끝에만 존재, 유사도 바닥 0.97의 균일 그래프.
- 산출: `remote/gen_ppv_texts.py`, `remote/analyze_ppv_geometry.py`,
  `remote/ppv_delegation_test.py`, `results/ppv_texts.jsonl`(로컬 보존, 논문용),
  `results/ppv_emb.npz`

---

# 모델 탐색 동결 선언 (2026-08-19)

> **Model exploration frozen as of 2026-08-19.**
> 새로운 아이디어가 없어서 중단하는 것이 아니라, 사전등록된 여러 독립 축에서 추가 실험의
> 기대효용이 최종 제출 안정성보다 낮아졌기 때문에 중단한다.

탐색한 축: generation·SC scaling·SFT/RFT·GRPO·DPO·verifier/reward model·top2 selector·
system router·family ensemble·confidence dynamics·FFS·prefix confidence·contrastive decoding·
MTI·EDAS·reasoning geometry — 각 축은 단순 실패가 아니라 **기제 규명**과 함께 닫혔다.

이 커밋이 **release candidate**다. 8/31 전까지 코드 변경 금지. 당일 절차는
`docs/final-pipeline.md`만 따른다. 최종 제출은 사용자 확인 후에만.
