# 최종 test 실행 절차 (2026-08-31) — 확정본

> exp30 리허설(2026-08-10, 831문항 실측)로 검증된 절차. **당일에는 이 문서만 따른다.**
> 리허설 원본 수치는 `results/rehearsal_report.md`.

## 확정 스택

**Qwen2.5-3B-Instruct (무학습 베이스) + 확신도 가중 Self-Consistency n=32, temperature 0.7, top_p 0.8, seed=42**

- 어댑터 미사용 — 학습 5전(SFT×3, GRPO×2) 전부 베이스 이하로 확인돼 최종 스택에서 제외
- 근거: 로컬 검증 75.8%, 리더보드 0.78459 (참값은 재실행 변동 감안 시 0.780±0.005 — 아래 "선정 근거" 참고)

## LB 점수가 확인된 전체 제출 이력

| exp | 날짜 | 설정 | LB | 비고 |
|---|---|---|---|---|
| exp01 | 07-31 | greedy, boxed추출 버그 상태 | 0.648 | 구채점기준(1000문항), 신기준(831)과 직접 비교 불가 |
| exp06b | 08-06 | 베이스, SC n=8 가중 | 0.77015 | 로컬 74.7%와 정합, 최초 신기준 제출 |
| **exp26** | 08-07 | 베이스, **SC n=32 가중 seed42** | **0.78459** | 역대 최고점. 현재 확정 스택과 동일 설정 |
| exp29-1 | 08-10 | 베이스, SC n=64 가중 | 0.77978 | n32→64 확대 무의미(H11 LB 재확인), n32w 대비 −0.48%p |
| exp29-2 | 08-10 | 베이스, SC n=32 단순다수결(가중無) | 0.77737 | 가중 제거 시 −0.72%p, 가중 투표 유효성 재확인 |
| exp34b | 08-10 | **exp26과 완전 동일 설정, 표본만 재추출** | 0.77978 | **동일 설정 재실행만으로 0.48%p 변동** — 노이즈 하한 측정치 |
| exp41a | 08-11 | 3-시드(42/43/44) n32 병합 96표 가중 | 0.77617 | 분산축소 가설(H22) 실험군 |
| exp41b | 08-11 | 3-시드(45/46/47) n32 병합 96표 가중 | 0.78459 | H22 대조군 — exp41a와 0.84%p 차이(노이즈 하한보다 큼) → H22 기각 |
| exp38-w48 | 08-11 | 베이스, SC n=48 가중 | 0.78219 | n32w 대비 −0.24%p, 노이즈 범위 내 |
| exp38-dt32 | 08-11 | 베이스, SC n=32 + 잘린풀이 제외 | 0.77978 | 가중 투표와 사실상 동률 |

### 선정 근거 (LB 최고점이 아니라 원리로 선택)

- 단순 최고점 기준이면 exp26/exp41b(0.78459)이지만, **exp34b가 "완전히 동일한 설정"을 표본만 다시 뽑아 재실행한 결과 0.77978로 0.48%p 하락**했다 — 즉 0.78459는 참값이 아니라 노이즈 상한에 가까운 관측치. 하나의 관측 최고점을 쫓는 것은 과적합이다.
- 대신 **"가중 SC n=32, temp0.7/top_p0.8, 어댑터 없음"이라는 하나의 설정이 4번 관측**됐다(exp26 0.78459, exp34b 0.77978, exp41b 0.78459[동치], exp38-dt32 0.77978[근사 변형]) — 평균 0.780 근방으로 수렴하며, 이 값이 다른 축(n=64 확대, 가중 제거, 다중시드 병합)보다 항상 같거나 높다. **로컬 검증(75.8%)과도 정합**(오프셋 +2.7%p 일관).
- n=48(0.78219)·n=64(0.77978)·단순다수결(0.77737)·3시드병합(0.77617/0.78459) 등 다른 모든 변형은 n=32 가중 대비 개선이 없거나 노이즈 범위 안에 머물렀다 → **탐색한 축 중 "SC n=32 가중"이 유일하게 반복 검증된 안정적 선택지**.

## ⚠️ 반드시 dump 방식으로 실행할 것

`make_submission.py`를 직접 쓰면 **중단 시 처음부터 재실행**(최대 2.5시간 손실)이다. 리허설에서 확인된 최대 리스크.
아래 2단계 방식은 100문항마다 저장돼 **어디서 죽어도 이어서 재개**된다.

### 1단계 — 표본 생성 (약 2시간 30분, 재개 가능)

```bash
cd ~/work/deep-learning-challenge && git pull
nohup uv run python remote/dump_lb_samples.py \
  --n 32 --temp 0.7 --top-p 0.8 --seed 42 \
  --lb-csv deep-learning-challenge-2026/<최종test파일>.csv \
  --out results/final_samples.jsonl > dump_final.log 2>&1 &
```

- 진행 확인: `tail -f dump_final.log` → "진행 N/2000문항"
- **중단됐다면 같은 명령을 그대로 재실행** (완료분은 건너뜀). 재실행 전 `.progress`의 done 개수와 jsonl 줄 수 일치 확인

### 2단계 — 제출 파일 생성 (수 초, GPU 불필요)

```bash
uv run python remote/make_submission_from_dump.py \
  --dump results/final_samples.jsonl --rule weighted --n 32 \
  --lb-csv deep-learning-challenge-2026/<최종test파일>.csv --tag final
```

### 3단계 — 제출 전 검증 (필수)

- 행수 = 문항 수(2,000) / 컬럼 `id`,`answer` / answer 전부 정수 / 결측·중복 0건
- **`id`는 소문자** (대회 문서엔 `ID`로 적혀 있으나 채점기는 소문자 — 2026-07-31 첫 제출 ERROR로 확인된 사항)

### 4단계 — 제출

```bash
python -m kaggle competitions submit -c deep-learning-challenge-2026 \
  -f results/submission_final.csv -m "final | weighted SC n32 | local 75.8%"
```

**⚠️ 최종 제출은 사용자 확인 후에만. 자동 제출 금지.**

## 시간 계획

| 구간 | 소요 | 비고 |
|---|---|---|
| 표본 생성 | 2시간 30분 | 4.44초/문항 × 2,000 |
| 집계·검증 | 5분 | GPU 불필요 |
| **중단 1회 대비 여유** | +2시간 30분 | 재개되므로 실제로는 부분 손실만 |
| **권장 착수 시각** | **마감 6시간 전** | 최악의 경우도 흡수 |

## 사전 점검 (전날)

- [ ] 인스턴스 기동·GPU 인식 (`nvidia-smi`)
- [ ] `git pull`로 최신 스크립트 확보
- [ ] 최종 test CSV를 `deep-learning-challenge-2026/`에 배치, 컬럼명(`id`,`question`) 확인
- [ ] 디스크 여유 1GB 이상 (덤프 jsonl + 로그)
- [ ] Kaggle 토큰 유효성 확인 (`kaggle competitions submissions -c deep-learning-challenge-2026`)
- [ ] 일일 제출 한도(5회) 잔여 확인 — 최종일엔 재시도 여지를 남길 것

## 8/31 당일 체크리스트

- [ ] 마감 6시간 전 착수 (권장 착수 시각, 위 시간 계획 참고)
- [ ] 1단계 명령 실행 후 `dump_final.log`에서 "진행 N/2000문항" 로그가 정상적으로 늘어나는지 5분 내 확인
- [ ] 표본 생성 완료(2000/2000) 후 2단계 실행, `results/submission_final.csv` 생성 확인
- [ ] 3단계 검증 항목(행수·컬럼·정수·결측/중복) 전부 통과 확인
- [ ] **제출 전 사용자에게 파일 경로·검증 결과 보고하고 명시적 확인 받기** — 자동 제출 절대 금지
- [ ] 제출 후 `kaggle competitions submissions -c deep-learning-challenge-2026`로 접수 확인
- [ ] 여유 시간이 남으면(마감 2시간 이상 전) 동일 스택으로 재제출은 하지 않음 — exp34b로 노이즈만 추가될 뿐 개선 근거 없음. 대신 제출 이력·최종 점수를 EXPERIMENTS.md에 기록

## 사고 대응

| 상황 | 대응 |
|---|---|
| 생성 프로세스 사망 | 같은 명령 재실행 → 완료분 건너뛰고 재개 |
| 러너/서버 무응답 | AWS API로 재부팅 (SSH 불가해도 가능) → systemd가 러너 자동 기동 |
| SSH 차단 | 네트워크 문제 — AWS API·ntfy는 별개로 동작, 재부팅 경로로 우회 |
| GPU 메모리 부족 | `--chunk 50`으로 청크 축소 (리허설 최대 95% 사용, 여유 적음) |
| 시간 부족 | `--n 16`으로 축소 (로컬 75.6% — n32와 0.2%p 차이, 사실상 동등) |
